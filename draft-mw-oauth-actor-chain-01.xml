<?xml version="1.0" encoding="utf-8"?>
<!-- name="GENERATOR" content="github.com/mmarkdown/mmark Mmark Markdown Processor - mmark.nl" -->
<!DOCTYPE rfc SYSTEM 'rfc2629.dtd' []>
<rfc ipr="trust200902" submissionType="IETF" category="std" xml:lang="en" consensus="yes" docName="draft-mw-oauth-actor-chain-01">
<?rfc toc="yes"?><?rfc symrefs="yes"?><?rfc sortrefs="yes"?><?rfc compact="yes"?><?rfc subcompact="no"?><?rfc comments="no"?>
<front>
<title abbrev="OAUTH-ACTOR-CHAIN">Cryptographically Verifiable Actor Chains for OAuth 2.0 Token Exchange</title><author initials="A." surname="Prasad" fullname="A Prasad"><organization>Oracle</organization><address><postal><street></street>
</postal><email>a.prasad@oracle.com</email>
</address></author>
<author initials="R." surname="Krishnan" fullname="Ram Krishnan"><organization>JPMorgan Chase &amp; Co</organization><address><postal><street></street>
</postal><email>ramkri123@gmail.com</email>
</address></author>
<author initials="D." surname="Lopez" fullname="Diego R. Lopez"><organization>Telefonica</organization><address><postal><street></street>
</postal><email>diego.r.lopez@telefonica.com</email>
</address></author>
<author initials="S." surname="Addepalli" fullname="Srinivasa Addepalli"><organization>Aryaka</organization><address><postal><street></street>
</postal><email>srinivasa.addepalli@aryaka.com</email>
</address></author>
<date year="2026" month="June" day="15"></date>
<area>Security</area><workgroup>OAuth</workgroup><keyword>actor chain</keyword>
<keyword>oauth</keyword>
<keyword>rfc8693</keyword>
<keyword>token exchange</keyword>
<keyword>workload identity</keyword>
<keyword>delegation</keyword>
<keyword>AI agents</keyword>
<keyword>MCP</keyword>
<keyword>A2A</keyword>

<abstract><t>Multi-hop service-to-service and agentic workflows often exchange OAuth
access tokens across a sequence of actors. OAuth 2.0 Token Exchange permits
an <spanx style="verb">act</spanx> claim, including nested prior actors, but it does not define
interoperable rules for preserving, extending, disclosing, and validating a
delegation path across successive exchanges.</t>
<t>This document defines six actor-chain profiles for OAuth 2.0 Token Exchange:
Declared Full Disclosure, Declared Subset Disclosure, Declared Actor-Only
Disclosure, Verified Full Disclosure, Verified Subset Disclosure, and Verified
Actor-Only Disclosure. The profiles preserve the existing meanings of <spanx style="verb">sub</spanx>,
<spanx style="verb">act</spanx>, and <spanx style="verb">may_act</spanx>. They add explicit profile selection, a stable workflow
actor-chain identifier, profile-controlled actor disclosure, and, for verified
profiles, actor-signed step proofs with cumulative commitment state.</t>
</abstract>

</front>

<middle>

<section anchor="introduction" title="Introduction">
<t>OAuth 2.0 Token Exchange <xref target="RFC8693"></xref> is widely useful when one service,
workload, or agent receives a token and needs a new token to call another
service. In multi-hop workflows, however, each hop can lose standardized
continuity of the path by which the request arrived.</t>
<t>This specification defines an actor-chain profile family for OAuth 2.0 Token
Exchange with a deliberately narrow protocol surface. It standardizes how a
workflow actor-chain identifier is preserved, how actor progression is
appended or preserved across token exchanges, how much of that progression is
disclosed to each hop, and how verified profiles bind each accepted hop to
actor-signed evidence and
cumulative commitment state.</t>
<t>The core mechanism is intentionally narrow. A deployment selects one actor-chain
profile, preserves one workflow identifier, applies append-only or preserve-
state processing, discloses only the actor-chain information permitted by the
selected profile, and optionally uses actor-signed step proofs with cumulative
commitment state.</t>

<section anchor="document-organization" title="Document Organization">
<t>This document is organized into three labeled parts. The part labels are
organizational aids; the normative requirements are in the numbered sections
that follow.</t>
<t>Part I, &quot;Overview and Scope&quot;, introduces the problem, the RFC 8693 gap, the
actor-chain model, the profile family, and the scope of this specification.</t>
<t>Part II, &quot;Protocol Specification&quot;, defines the normative protocol requirements,
including terminology, conformance, profile selection, common processing,
validation, declared and verified profiles, preserve-state exchanges, receiver
acknowledgments, JWT/JWS binding, Security Considerations, Privacy Requirements
and Considerations, and IANA Considerations.</t>
<t>Part III, &quot;Informative Rationale, Examples, and Operational Guidance&quot;, is
informative. It provides examples, deployment context, threat model, audit
guidance, design rationale, relationship to other work, wire examples, test
vectors, future considerations, out-of-scope use cases, and a detailed RFC 8693
comparison. It does not define conformance requirements. Where Part III
summarizes requirements from Part II, Part II controls. This material is
organized so that it could be separated into an Informational companion draft
in a future revision.</t>
</section>
</section>

<section anchor="problem-statement" title="Problem Statement">
<t>A common workflow path is:</t>

<figure><artwork type="text">A -&gt; B -&gt; C -&gt; D
</artwork></figure>

<t>Actor <spanx style="verb">A</spanx> obtains a token for <spanx style="verb">B</spanx>. Later, <spanx style="verb">B</spanx> exchanges that token for a token
to call <spanx style="verb">C</spanx>. Then <spanx style="verb">C</spanx> may exchange again for <spanx style="verb">D</spanx>. Each exchange can be valid as
an OAuth token exchange, but the delegation path across the workflow is not, by
itself, standardized as an interoperable input to authorization policy and
audit.</t>
<t>Downstream recipients may need to know the full disclosed path, only an approved
subset, or only the immediate upstream actor. Some deployments also need later
proof that a specific actor participated in a specific hop. Without a profile,
implementations must invent local conventions for nested <spanx style="verb">act</spanx>, chain
continuity, disclosure control, and audit evidence.</t>
</section>

<section anchor="standards-gap-rfc8693" title="Standards Gap and Relationship to RFC 8693">
<t>OAuth 2.0 Token Exchange <xref target="RFC8693"></xref> defines the <spanx style="verb">act</spanx> claim for the
current actor. It also permits nested <spanx style="verb">act</spanx> values as a way to represent prior
actors. It does not define interoperable processing rules for constructing,
extending, preserving, disclosing, or validating an actor chain across a
sequence of token exchanges.</t>
<t>This specification preserves the existing RFC 8693 meanings of <spanx style="verb">sub</spanx>, <spanx style="verb">act</spanx>,
and <spanx style="verb">may_act</spanx>; those claim semantics are defined by <xref target="RFC8693"></xref> and are not
redefined here.</t>
<t>This specification uses <spanx style="verb">actp</spanx> as an explicit actor-chain profile identifier.
When <spanx style="verb">actp</spanx> is present and identifies a profile defined here, this
specification defines additional processing rules for actor-chain workflow
continuity and profile-controlled disclosure.</t>
<t>When <spanx style="verb">actp</spanx> is absent, ordinary RFC 8693 semantics remain unchanged. Nested
prior <spanx style="verb">act</spanx> values, if present, remain governed by RFC 8693 and by the
specification or local policy applicable to that token.</t>
<t>This specification does not redefine RFC 8693 generally and it does not change
the behavior of plain RFC 8693 token-exchange outputs outside this profile
family.</t>
</section>

<section anchor="solution-overview" title="Solution Overview">
<t>This specification defines three explicit protocol signals around OAuth token
exchange:</t>
<t>
<list style="symbols">
<t><spanx style="verb">actor_chain_profile</spanx>, a token request parameter selecting exactly one
actor-chain profile;</t>
<t><spanx style="verb">actp</spanx>, a token claim carrying the selected profile identifier; and</t>
<t><spanx style="verb">acti</spanx>, a stable actor-chain identifier minted once at workflow start and
preserved for the workflow instance.</t>
</list>
</t>
<t>A profiled access token may also carry <spanx style="verb">act</spanx>, using nested ActorID nodes as
the authoritative disclosed actor-chain fragment for that artifact. Verified
profiles additionally carry <spanx style="verb">actc</spanx>, a cumulative commitment object that links
the accepted hop to actor-signed step-proof evidence.</t>
<t>The declared profiles rely on the issuing Authorization Server to assert
actor-chain continuity and enforce disclosure policy. The verified profiles
add actor-signed step proofs and cumulative commitment state so that later
participants and auditors can validate stronger evidence of accepted hop
progression.</t>
<t>An optional receiver acknowledgment, <spanx style="verb">hop_ack</spanx>, lets a recipient provide signed
evidence that it accepted responsibility for a specific inbound hop without
appending itself to the actor chain.</t>
</section>

<section anchor="actor-chain-model" title="Actor-Chain Model">
<t>The actor chain advances only when an actor acts toward a later hop. Mere
receipt of a token does not append the recipient.</t>
<t>For example, if <spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx>, and <spanx style="verb">B</spanx> later calls <spanx style="verb">C</spanx>, then <spanx style="verb">B</spanx> is appended
when <spanx style="verb">B</spanx> performs token exchange to act toward <spanx style="verb">C</spanx>. <spanx style="verb">C</spanx> is not appended merely
because it received the token. If <spanx style="verb">C</spanx> later acts toward <spanx style="verb">D</spanx>, then <spanx style="verb">C</spanx> is
appended at that later exchange.</t>
<t>The selected profile controls what profiled access tokens disclose.
Full-disclosure profiles disclose the complete actor chain for the hop.
Subset-disclosure profiles disclose an ordered subset or omit <spanx style="verb">act</spanx> entirely
according to Authorization Server policy. Actor-only profiles disclose only the
current actor. Verified profiles also carry cumulative commitment state for
stronger continuity evidence.</t>
</section>

<section anchor="profile-summary" title="Profile Summary">
<t>The six profiles are the cross-product of two evidence models and three
actor-chain disclosure modes.</t>
<t>The evidence model determines how chain continuity is established:</t>
<t>
<list style="symbols">
<t>Declared profiles rely on Authorization-Server-asserted chain continuity.</t>
<t>Verified profiles add actor-signed step proofs and cumulative commitment
state.</t>
</list>
</t>
<t>The disclosure mode determines how much actor-chain information is visible in
profiled access tokens:</t>
<t>
<list style="symbols">
<t>Full Disclosure exposes the complete actor chain for the hop.</t>
<t>Subset Disclosure exposes an Authorization-Server-selected ordered subset,
or omits <spanx style="verb">act</spanx>.</t>
<t>Actor-Only Disclosure exposes only the current actor.</t>
</list>
</t>
<texttable>
<ttcol>Disclosure mode</ttcol>
<ttcol>Declared profile</ttcol>
<ttcol>Verified profile</ttcol>


<c>Full Disclosure</c>
<c>declared-full</c>
<c>verified-full</c>

<c>Subset Disclosure</c>
<c>declared-subset</c>
<c>verified-subset</c>

<c>Actor-Only Disclosure</c>
<c>declared-actor-only</c>
<c>verified-actor-only</c>

</texttable><t>Deployments whose recipient authorization depends on prior-path membership or
actor order need a profile and disclosure policy that disclose the required
path evidence at that hop. Actor-only or omitted-<spanx style="verb">act</spanx> outcomes are not
sufficient for path-sensitive authorization decisions.</t>
<t>Selection of a profile is a deployment concern. Informative selection guidance,
including when an actor-chain profile is needed at all, how single-domain and
multi-domain deployments commonly map to the six profiles, and how action
sensitivity can affect that choice, appears in {{deployment-context}}.</t>
</section>

<section anchor="scope-non-goals" title="Scope and Non-Goals">
<t>This specification defines actor-chain profiles for OAuth 2.0 Token Exchange.
It standardizes profile selection, workflow identifier continuity, disclosed
actor-chain structure, profile-specific disclosure behavior, verified-profile
step proofs and commitment state, preserve-state exchanges, optional receiver
acknowledgments, metadata, error handling, and JWT/JWS bindings.</t>
<t>This document does not define:</t>
<t>
<list style="symbols">
<t>a replacement for OAuth 2.0 grant processing or RFC 8693 Token Exchange;</t>
<t>a new actor-authentication, presenter-binding, token-binding, or transport
security mechanism;</t>
<t>a general call-graph language, branch merge semantics, or sibling-discovery
protocol;</t>
<t>application payload safety or business authorization policy;</t>
<t>recipient-protected selective-disclosure mechanisms, such as encrypted,
selectively revealable, or recipient-bound actor-chain presentations beyond
the base disclosed <spanx style="verb">act</spanx> representation; or</t>
<t>a cryptographic proof, inside preserved <spanx style="verb">actc</spanx> state, that a cross-domain
<spanx style="verb">sub</spanx> value and a locally re-issued <spanx style="verb">sub</spanx> alias identify the same underlying
subject.</t>
</list>
</t>
<t>Those topics remain local policy matters or candidates for companion
specifications.</t>
</section>

<section anchor="terminology" title="Terminology">
<t>The key words &quot;MUST&quot;, &quot;MUST NOT&quot;, &quot;REQUIRED&quot;, &quot;SHALL&quot;, &quot;SHALL NOT&quot;, &quot;SHOULD&quot;,
&quot;SHOULD NOT&quot;, &quot;RECOMMENDED&quot;, &quot;NOT RECOMMENDED&quot;, &quot;MAY&quot;, and &quot;OPTIONAL&quot; in this
document are to be interpreted as described in BCP 14 <xref target="RFC2119"></xref> <xref target="RFC8174"></xref>
when, and only when, they appear in all capitals, as shown here.</t>
<t>This document uses terminology from OAuth 2.0 Token Exchange
<xref target="RFC8693"></xref>. Informative background material also refers to the SPICE
Architecture <xref target="I-D.ietf-spice-arch"></xref> and the RATS Architecture <xref target="RFC9334"></xref>.</t>
<t>
<list style="symbols">
<t><spanx style="strong">Actor</spanx>: A workload, service, application component, agent, or other
authenticated entity that receives a token, performs work, and can
subsequently act toward another actor.</t>
<t><spanx style="strong">Current actor</spanx>: The authenticated entity presently performing token
exchange.</t>
<t><spanx style="strong">Authorization Server (AS)</spanx>: The OAuth 2.0 Authorization Server that issues
tokens and validates token exchange requests under OAuth 2.0 and OAuth 2.0
Token Exchange.</t>
<t><spanx style="strong">Disclosed current actor</spanx>: The actor identified by the outermost visible
<spanx style="verb">act</spanx> node of an artifact when such a node is present. Depending on the
selected profile and disclosure policy, the disclosed current actor MAY be
the same as the operational current actor or MAY be absent from the returned
profiled access token.</t>
<t><spanx style="strong">Presenting actor</spanx>: The actor that presents an inbound token to a
recipient.</t>
</list>
</t>
<t>Example: when <spanx style="verb">B</spanx> exchanges a token at the Authorization Server, <spanx style="verb">B</spanx> is the
  current actor. When <spanx style="verb">B</spanx> later presents the resulting token to <spanx style="verb">C</spanx>, <spanx style="verb">B</spanx> is the
  presenting actor.</t>
<t>Unless otherwise qualified, references in this specification to the current
  actor are about the operational current actor performing exchange, not about
  whether that actor is disclosed inline in <spanx style="verb">act</spanx> for the next hop.</t>
<t>
<list style="symbols">
<t><spanx style="strong">Recipient</spanx>: The actor or resource server identified as the intended target
of an issued token.</t>
<t><spanx style="strong">Actor chain</spanx>: The ordered sequence of actors that have acted so far in one
workflow instance.</t>
<t><spanx style="strong">Profiled access token</spanx>: The access token issued under this specification
for presentation to the next hop. It is distinct from step proofs, bootstrap
context handles, commitment objects, and receiver acknowledgments.</t>
<t><spanx style="strong">Disclosed actor chain</spanx>: The ordered actor sequence represented by the
disclosed <spanx style="verb">act</spanx> structure in an artifact when <spanx style="verb">actp</spanx> is present. It is visible
to any party that can read that artifact. In the base JWT/JWS binding, a
returned profiled access token is visible to the current actor and to the
next recipient. The outermost <spanx style="verb">act</spanx> identifies the disclosed current actor.
Any nested <spanx style="verb">act</spanx> members identify prior disclosed actors only.</t>
</list>
</t>
<t>The helper function <spanx style="verb">VisibleChain(act)</spanx> denotes the ordered ActorID sequence
  decoded from that disclosed <spanx style="verb">act</spanx> structure.</t>
<t>
<list style="symbols">
<t><spanx style="strong">Actor-visible chain</spanx>: The exact ordered actor sequence that the current
actor is permitted to know and extend for the next hop. In the verified
profiles, this is the signed actor-visible chain carried in the step proof.
It can be broader than the disclosed actor chain in the returned profiled
access token under subset-disclosure or actor-only operation.</t>
</list>
</t>
<t>Example: if <spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx>, and <spanx style="verb">B</spanx> later exchanges the inbound token to
  call <spanx style="verb">C</spanx>, <spanx style="verb">B</spanx> might verify an inbound disclosed actor chain <spanx style="verb">[A]</spanx> and sign a
  verified-profile step proof over the actor-visible chain <spanx style="verb">[A,B]</spanx>. Under Full
  Disclosure, the returned profiled access token for <spanx style="verb">C</spanx> discloses <spanx style="verb">[A,B]</spanx>.
  Under Subset Disclosure, that returned token might disclose any
  policy-selected ordered subsequence of <spanx style="verb">[A,B]</spanx>, or omit <spanx style="verb">act</spanx> where
  allowed. Under Actor-Only Disclosure, that returned token discloses exactly <spanx style="verb">[B]</spanx>.</t>
<t>
<list style="symbols">
<t><spanx style="strong">Authoritative workflow chain state</spanx>: Authorization-Server-retained state
for the accepted workflow instance. It MAY be richer than the disclosed actor chain
in any given issued token. It is used for continuity, branch
correlation, forensic review, legal audit, and any policy-controlled future
redisclosure. This specification does not require that such retained state be
disclosed inline in profiled access tokens.</t>
</list>
</t>
<t>In the full-disclosure profiles, the disclosed actor chain, the actor-visible
  chain, and the authoritative workflow chain state collapse to the same chain.
  They diverge only under subset and actor-only profiles, where the disclosed
  actor chain may be narrower than the actor-visible chain, which in turn may
  be narrower than the authoritative workflow chain state.</t>
<t>
<list style="symbols">
<t><spanx style="strong">Proof-bound chain state</spanx>: The cumulative cryptographic state carried in
<spanx style="verb">actc</spanx> that binds prior accepted chain state to a newly accepted hop.</t>
<t><spanx style="strong">Step proof</spanx>: A profile-defined proof signed by the current actor that
binds that actor's participation to the workflow, prior accepted state, the
profile-defined actor-visible chain for the hop, and target context.</t>
<t><spanx style="strong">Target context</spanx>: The canonical representation of the next-hop target that
a profile-defined proof or acknowledgment binds to. It always includes the
intended audience and MAY additionally include other target-selection inputs.
If no such additional inputs are used, it is the single-member JSON object
containing only <spanx style="verb">aud</spanx>.</t>
<t><spanx style="strong">Bootstrap context</spanx>: An opaque handle issued by the Authorization Server
only to start a verified profile workflow. It lets the initial actor redeem
bound bootstrap state at the token endpoint without carrying that state
inline.</t>
<t><spanx style="strong">Actor-chain identifier (<spanx style="verb">acti</spanx>)</spanx>: A stable identifier minted once at workflow
start and retained for the lifetime of the workflow instance.</t>
<t><spanx style="strong">Cross-domain re-issuance</spanx>: A second token exchange performed at another
domain's Authorization Server in order to obtain a local token trusted by the
next recipient, without extending the actor chain.</t>
<t><spanx style="strong">Home Authorization Server</spanx>: The Authorization Server at which the current
actor normally performs the chain-extending token exchange for the next hop.
In same-domain operation it is the issuer that validates prior chain state
and issues the next profiled access token.</t>
<t><spanx style="strong">Continuity</spanx>: The property that the presenting actor of an inbound
artifact is the same actor that the workflow state and any required disclosed
current-actor information identify as the expected presenter.</t>
<t><spanx style="strong">Append-only processing</spanx>: The rule that a new actor is appended to the
prior accepted workflow chain state, and to any disclosed actor chain
fragment derived from it, without insertion, deletion, reordering, or
modification of prior actors.</t>
<t><spanx style="strong">Terminal recipient</spanx>: A recipient that performs work locally and does not
extend the actor chain further.</t>
<t><spanx style="strong">Refresh-Exchange</spanx>: A token-exchange operation by the same current actor
that preserves accepted chain state while refreshing transport characteristics
such as expiry according to local policy.</t>
<t><spanx style="strong">Local policy</spanx>: Deployment-specific authorization and risk rules used when
this specification defers a decision to the Authorization Server or
recipient. Local policy can include trust relationships, authenticated client
identity, permitted audiences or resources, chain-depth limits, profile
support, and business or risk controls.</t>
</list>
</t>
</section>

<section anchor="conformance" title="Conformance">
<t>An implementation MAY support one or more actor-chain profiles defined by this
specification. An implementation that claims support for a profile identified by
an <spanx style="verb">actp</spanx> value MUST implement the common requirements that apply to that
profile, the evidence-model requirements for that profile, the disclosure-mode
requirements for that profile, and any profile-specific deltas.</t>
<t>Authorization Servers, current actors, recipients, and libraries MUST apply the
rules associated with the explicit <spanx style="verb">actp</spanx> value. They MUST NOT infer another
profile from the apparent shape of <spanx style="verb">act</spanx>, from the presence or absence of
<spanx style="verb">actc</spanx>, or from local naming conventions.</t>
<t>An implementation that receives an unsupported <spanx style="verb">actp</spanx>, an unsupported required
artifact type, or a profile-required artifact that is absent, malformed, or
invalid MUST reject that artifact for profile processing. Implementations MAY
support opaque access-token deployments only when a companion validation
interface or out-of-band agreement exposes the information needed to satisfy the
selected profile's validation rules.</t>
</section>

<section anchor="protocol-model" title="Protocol Model">

<section anchor="workflow-path-model" title="Workflow Path Model">
<t>The basic workflow path model is:</t>

<figure><artwork type="text">A -&gt; B -&gt; C -&gt; D
</artwork></figure>

<t>The first actor initializes the workflow. Each subsequent actor MAY:</t>
<t>
<list style="numbers">
<t>validate an inbound token;</t>
<t>perform work; and</t>
<t>exchange that token for a new token representing itself toward the next hop.</t>
</list>
</t>
</section>

<section anchor="workflow-progression" title="Workflow Progression">
<t>The actor chain advances only when an actor acts. Mere receipt of a token does
not append the recipient.</t>
<t>If <spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx>, and <spanx style="verb">B</spanx> later calls <spanx style="verb">C</spanx>, then:</t>
<t>
<list style="numbers">
<t><spanx style="verb">A</spanx> begins the workflow and becomes the initial actor.</t>
<t>When <spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx>, <spanx style="verb">B</spanx> validates a token representing <spanx style="verb">A</spanx>.</t>
<t>When <spanx style="verb">B</spanx> later exchanges that token to call <spanx style="verb">C</spanx>, <spanx style="verb">B</spanx> becomes the next
actor.</t>
<t><spanx style="verb">C</spanx> is not appended merely because <spanx style="verb">C</spanx> received a token. <spanx style="verb">C</spanx> is appended
only if <spanx style="verb">C</spanx> later acts toward another hop.</t>
</list>
</t>
<t>Complete flow examples are provided in {{compact-examples}}.</t>
</section>
</section>

<section anchor="rfc8693-claims" title="Relationship to RFC 8693 Claims">
<t>This specification extends OAuth 2.0 Token Exchange <xref target="RFC8693"></xref> without
changing the base meanings of <spanx style="verb">sub</spanx>, <spanx style="verb">act</spanx>, or <spanx style="verb">may_act</spanx>. It profiles
delegation-chain workflow continuity and profile-controlled disclosure across
token exchange hops.</t>
<t>When <spanx style="verb">actp</spanx> is absent, <spanx style="verb">act</spanx> has ordinary RFC 8693 semantics. Nested prior
<spanx style="verb">act</spanx> claims, if present, remain informational only for access-control
purposes, consistent with <xref target="RFC8693"></xref>.</t>
<t>The following rules apply when <spanx style="verb">actp</spanx> is present and points to a profile
defined by this specification:</t>
<t>
<list style="symbols">
<t><spanx style="verb">sub</spanx> continues to identify the subject of the issued token.</t>
<t><spanx style="verb">acti</spanx> identifies the stable workflow instance for the accepted actor-chain
state.</t>
<t>If <spanx style="verb">act</spanx> is present, it is the authoritative disclosed actor-chain fragment
for that artifact. The outermost <spanx style="verb">act</spanx>, when present, identifies the
disclosed current actor. Each nested <spanx style="verb">act</spanx> member, when present, identifies
the immediately prior disclosed actor.</t>
<t>Reduced actor disclosure, including omission of prior actors, omission of the
current actor from a disclosed subset, or omission of <spanx style="verb">act</spanx> entirely, does
not turn a profiled access token into an ordinary impersonation output. When <spanx style="verb">actp</spanx>
is present, the token remains a delegation-chain token under this
specification.</t>
<t>Plain RFC 8693 outputs that do not carry this specification's profile
signals remain outside this profile family whether or not they contain
<spanx style="verb">act</spanx>.</t>
</list>
</t>

<section anchor="actor-chain-disclosure-boundary" title="Actor-Chain Disclosure Boundary">
<t>For any selected <spanx style="verb">actp</spanx>, the Authorization Server is the policy decision and
policy enforcement point for actor-chain disclosure. The selected profile
defines the maximum actor-chain information, if any, that may be visible inline
to the next hop.</t>
<t>Accordingly:</t>
<t>
<list style="symbols">
<t>no other field, identifier encoding, compatibility form, or exchange option
may disclose more actor-chain information than the selected profile permits;</t>
<t>recipients and intermediaries MUST NOT infer hidden actors from omitted
entries, identifier structure, or other claims; and</t>
<t>when a profile hides prior actors or the workflow subject's globally useful
identifier, the Authorization Server MUST enforce that outcome across the
entire issued artifact, not only within <spanx style="verb">act</spanx>.</t>
</list>
</t>
<t>Nothing in this specification redefines RFC 8693 delegation or impersonation
semantics. With <spanx style="verb">actp</spanx> absent, this document adds no new requirements on how
nested <spanx style="verb">act</spanx> is interpreted. With <spanx style="verb">actp</spanx> present, this document defines an
explicit profile-controlled extension in which the disclosed <spanx style="verb">act</spanx> structure
becomes authoritative for actor-chain processing under that profile.</t>
</section>
</section>

<section anchor="profile-selection" title="Profile Selection and Workflow Immutability">
<t>This specification uses capability discovery plus explicit profile selection,
not interactive profile negotiation.</t>
<t>An actor requesting a token under this specification MUST select exactly one
<spanx style="verb">actor_chain_profile</spanx> value for that request. The Authorization Server MUST
either issue a token whose <spanx style="verb">actp</spanx> equals that requested profile identifier or
reject the request.</t>
<t>For a given workflow instance identified by <spanx style="verb">acti</spanx>, <spanx style="verb">actp</spanx> is immutable.
Accordingly, every accepted chain state within that workflow instance carries
the same <spanx style="verb">actp</spanx>. Any token exchange, cross-domain re-issuance, or
Refresh-Exchange that would change <spanx style="verb">actp</spanx> for that workflow instance MUST be
rejected. A current actor MUST reject any returned token whose <spanx style="verb">actp</spanx> differs
from the profile it requested or from the preserved profile state already
represented by the inbound token.</t>
<t>Profile switching therefore requires starting a new workflow instance with a
new <spanx style="verb">acti</spanx>, not continuing an existing accepted chain state.</t>
</section>

<section anchor="common-workflow-processing" title="Common Workflow Processing">
<t>The chain-extending profile flows have the same shape:</t>
<t>
<list style="numbers">
<t>an initial actor starts a workflow under exactly one selected profile;</t>
<t>the issuing Authorization Server mints a stable <spanx style="verb">acti</spanx> and a workflow
subject representation;</t>
<t>each recipient validates the inbound token before relying on it;</t>
<t>an actor that later acts toward another hop exchanges the inbound token for
a successor token targeted to the next recipient;</t>
<t>the Authorization Server performs profile-specific continuity,
disclosure, and replay or freshness checks; and</t>
<t>the returned profiled access token carries the same <spanx style="verb">acti</spanx>, the same
<spanx style="verb">actp</spanx>, the preserved workflow subject, and the disclosed actor-chain state
governed by the selected profile for the next hop.</t>
</list>
</t>
<t>A chain-extending exchange appends the authenticated current actor according to
the selected profile. Preserve-state exchanges, defined in
{{preserve-state-exchanges}}, do not append an actor.</t>
</section>

<section anchor="token-actor-identity" title="Common Token and Actor Identity Requirements">

<section anchor="common-token-requirements" title="Common Token Requirements">
<t>Unless stated otherwise, &quot;profiled access token&quot; below means the access token
issued to the current actor for presentation to the next hop.
This section is about those tokens, not about verified-profile step proofs,
bootstrap context handles, or <spanx style="verb">hop_ack</spanx> objects.</t>
<t>In the interoperable self-contained profiled access token binding defined by
this document, profiled access tokens issued under any profile:</t>
<t>
<list style="symbols">
<t>MUST be short-lived;</t>
<t>MUST contain:
<list style="symbols">
<t>an issuer claim <spanx style="verb">iss</spanx>;</t>
<t>a profile identifier claim <spanx style="verb">actp</spanx>;</t>
<t>an actor-chain identifier claim <spanx style="verb">acti</spanx>;</t>
<t>a subject claim <spanx style="verb">sub</spanx>;</t>
<t>a unique token identifier claim <spanx style="verb">jti</spanx>;</t>
<t>an audience value <spanx style="verb">aud</spanx>; and</t>
<t>an expiry value <spanx style="verb">exp</spanx>.</t>
</list></t>
</list>
</t>
<t>A profiled access token MAY additionally contain <spanx style="verb">act</spanx> according to
the selected profile and Authorization Server policy.</t>
<t>For interoperability and predictable freshness, deployments SHOULD use
profiled access token lifetimes on the order of minutes rather than hours; a
default range of 1 to 10 minutes is RECOMMENDED. Validators SHOULD allow only
modest clock skew when evaluating <spanx style="verb">exp</spanx>, typically no more than 60 seconds unless
local clock discipline justifies a tighter bound.</t>
<t>Profiled access tokens issued under verified profiles MUST also carry <spanx style="verb">actc</spanx>.</t>
<t>The token claims used by this document have these roles:</t>
<t>
<list style="symbols">
<t><spanx style="verb">iss</spanx> identifies the issuer namespace of the profiled access token and of that
token's top-level <spanx style="verb">sub</spanx>;</t>
<t><spanx style="verb">actp</spanx> identifies the selected actor-chain profile;</t>
<t><spanx style="verb">acti</spanx> identifies the workflow instance;</t>
<t><spanx style="verb">sub</spanx> identifies the workflow subject of the token;</t>
<t><spanx style="verb">act</spanx>, when present, carries the authoritative disclosed actor-chain
fragment for that artifact; and</t>
<t><spanx style="verb">actc</spanx>, when present, carries cumulative commitment state for stronger
tamper evidence, continuity, and auditability.</t>
</list>
</t>
<t>This base specification defines interoperable direct claim carriage for
self-contained profiled access tokens. Deployments that instead use opaque access
tokens MAY keep authoritative workflow state only at the issuing Authorization
Server and MAY disclose actor-chain information, if any, through a companion
token-validation interface such as token introspection <xref target="RFC7662"></xref>.</t>
<t>This specification does not define such companion interfaces. If the artifact
presented for validation does not expose enough information to satisfy the
selected profile's requirements, the implementation MUST treat that as a
profile-validation failure.</t>
<t>Accordingly, interoperability requirements in this base specification apply to
the self-contained profiled access token claim carriage defined here. Opaque-token
deployments depend on companion validation interfaces outside this document and
therefore require either out-of-band agreement or a companion specification for
interoperable behavior.</t>
<t>At workflow bootstrap, the issuing Authorization Server MUST establish the
workflow subject according to local policy and the selected disclosure profile.
The resulting <spanx style="verb">sub</spanx> value MAY be an ordinary subject identifier, a pairwise
identifier, or a workflow-local stable alias. For privacy-sensitive
subset-disclosure operation, the Authorization Server SHOULD choose a stable
representation that does not reveal a globally useful subject identifier to
recipients that are not entitled to learn it.</t>
<t>For same-domain token exchange and Refresh-Exchange, this specification
preserves that exact chosen <spanx style="verb">sub</spanx> representation within the same domain for the
lifetime of the workflow unless a later permitted cross-domain alias transition
occurs. Cross-domain re-issuance MAY translate <spanx style="verb">sub</spanx> only under the
cross-domain subject-handling rules in {{cross-domain-subject}}. This document
does not define a same-workflow subject-transition mechanism.</t>
<t>The chosen <spanx style="verb">sub</spanx> representation for a workflow MUST remain consistent with the
selected <spanx style="verb">actp</spanx> disclosure constraints. In particular, <spanx style="verb">sub</spanx> MUST NOT disclose
an actor identity or other actor-chain information that the selected profile is
intended to withhold from the relevant recipient class.</t>
<t>In this self-contained JWT/JWS binding, a returned profiled access token is visible
to the current actor that receives it and to the next recipient that validates
it. Therefore, when a profile returns a disclosed <spanx style="verb">act</spanx>, the Authorization Server
MUST NOT disclose in that returned token any actor identity that the current
actor is not permitted to learn. A future recipient-protected disclosure
mechanism or encrypted binding may support stronger recipient-only
redisclosure, but that is outside this base specification.</t>
</section>

<section anchor="actor-chain-identifier" title="Actor-Chain Identifier">
<t>The <spanx style="verb">acti</spanx> value:</t>
<t>
<list style="symbols">
<t>MUST be minted once at workflow start by the issuing Authorization Server;</t>
<t>MUST be generated using a cryptographically secure pseudorandom number
generator (CSPRNG) with at least 122 bits of entropy;</t>
<t>MUST remain unchanged for the lifetime of that workflow instance;</t>
<t>MUST NOT be used to signal profile selection;</t>
<t>MUST be unique within the issuing Authorization Server for at least the
retention period required to validate any artifact that references that
<spanx style="verb">acti</spanx>, including <spanx style="verb">actc</spanx>, step proofs, and <spanx style="verb">hop_ack</spanx>; and</t>
<t>SHOULD be generated so that collision probability across issuing
Authorization Servers is negligible, for example by using 128 bits of random
entropy where practical.</t>
</list>
</t>
<t>Reuse of an <spanx style="verb">acti</spanx> value within its required retention period would make
commitment continuity, step-proof verification, and <spanx style="verb">hop_ack</spanx> correlation
ambiguous. An Authorization Server MUST NOT reuse an <spanx style="verb">acti</spanx> within that period.</t>
<t>Implementation note: standard UUID version 4 (UUIDv4), which provides 122 bits
of random entropy, is acceptable for <spanx style="verb">acti</spanx> in this version. Deployments MAY
use stronger generation, for example full 128-bit random values, by local
policy.</t>
<t>Profile selection MUST be signaled explicitly using the token request parameter
<spanx style="verb">actor_chain_profile</spanx> and the corresponding token claim
<spanx style="verb">actp</spanx>.</t>
</section>

<section anchor="target-context" title="Target Context Requirements">
<t><spanx style="verb">target_context</spanx> is the canonical next-hop target value bound into verified
profile step proofs and, when used, into <spanx style="verb">hop_ack</spanx>.</t>
<t>In this base specification, <spanx style="verb">aud</spanx> identifies the intended recipient service or
audience of the issued token. An optional <spanx style="verb">resource</spanx> value can further identify
a narrower protected resource, API surface, or object within that audience.
When no finer-grained targeting inputs are used, the canonical target context is
still a JSON object that contains only <spanx style="verb">aud</spanx>.</t>
<t>The following normative requirements apply to <spanx style="verb">target_context</spanx>.</t>
<t>For every profile-defined signed, hashed, compared, retained, or communicated
use in this specification, <spanx style="verb">target_context</spanx> MUST be a JSON object.</t>
<t><spanx style="verb">target_context</spanx> MUST contain an <spanx style="verb">aud</spanx> member carrying the verified audience
information exactly in the profile-defined canonical representation. If <spanx style="verb">aud</spanx> is
a string, <spanx style="verb">target_context.aud</spanx> MUST be that same JSON string. If <spanx style="verb">aud</spanx> is an
array of strings, <spanx style="verb">target_context.aud</spanx> MUST be that exact JSON array, preserving
element order.</t>
<t>A chain-extending profiled access token MUST identify exactly one logical
chain-extending recipient. <spanx style="verb">target_context.aud</spanx> SHOULD therefore be a string. An
array value MAY be used only when the array is defined by local policy or an
applicable profile as one indivisible composite audience and no individual
audience member can independently extend the actor chain from that token. If
multiple independently callable recipients are intended, the Authorization
Server MUST issue separate successor tokens, each with its own <spanx style="verb">jti</spanx> and
distinct canonical <spanx style="verb">target_context</spanx>. If a unique logical chain-extending
recipient cannot be determined, profile validation MUST fail.</t>
<t>If no additional target-selection values are used, <spanx style="verb">target_context</spanx> MUST be the
single-member object <spanx style="verb">{ &quot;aud&quot;: aud }</spanx>.</t>
<t>A deployment MAY additionally include:</t>
<t>
<list style="symbols">
<t><spanx style="verb">resource</spanx>, when the hop is bound to a narrower protected resource or API
surface within the audience;</t>
<t><spanx style="verb">request_id</spanx>, when the deployment expects multiple distinct accepted
successors under the same prior state and the same nominal target; and</t>
<t>other local extension members used by same-domain local authorization policy.</t>
</list>
</t>
<t>Optional <spanx style="verb">target_context</spanx> members are optional only at construction time. Once a
profile-defined artifact is signed, hashed, compared, retained, or
communicated, the exact canonical <spanx style="verb">target_context</spanx> object carried in that
artifact is the value that is bound for verification. Verifiers MUST NOT add,
remove, infer, synthesize, or policy-fill omitted members during validation or
post-facto verification. A member omitted from the canonical object is not
cryptographically bound by that artifact.</t>
<t>This base specification assigns interoperable security semantics to <spanx style="verb">aud</spanx>,
optional <spanx style="verb">resource</spanx>, and optional <spanx style="verb">request_id</spanx>. Other <spanx style="verb">target_context</spanx> members
MAY be used within one domain by local policy. At a cross-domain boundary, a
member whose semantics are not understood under this specification, an
applicable Standards Track extension, a companion profile, or an explicit
bilateral semantic mapping is a non-droppable constraint: the re-issuing
Authorization Server MUST either preserve that member exactly with validation
behavior that continues to constrain the returned local token authority, or
reject the re-issuance. Unknown members MUST NOT be ignored, removed,
defaulted, or treated as non-security-relevant when determining equivalence or
narrowing.</t>
<t>When a deployment expects multiple distinct successors under the same prior
state and the same nominal target, it MUST include a request-unique
discriminator such as <spanx style="verb">request_id</spanx> inside <spanx style="verb">target_context</spanx>.</t>
<t><spanx style="verb">target_context</spanx> members MUST NOT disclose actor identities or other actor-chain
information that the selected <spanx style="verb">actp</spanx> would withhold from the relevant holder of
the artifact.</t>
<t>An Authorization Server that validates, preserves, or audits workflow
continuity using <spanx style="verb">target_context</spanx> MUST retain the exact canonical
<spanx style="verb">target_context</spanx> value for the accepted hop, or retained request material from
which the identical canonical JSON object can be deterministically reproduced,
including the presence and absence of every member. Reconstruction MUST NOT rely
on later policy inference, default insertion, or semantic approximation.</t>
<t>Whenever <spanx style="verb">target_context</spanx> is incorporated into a profile-defined signature or
commitment input in this JWT-based version, it MUST be represented as a JSON
object and canonicalized exactly once as part of the enclosing JSON
Canonicalization Scheme (JCS)-serialized payload object. Equality checks over
<spanx style="verb">target_context</spanx> MUST therefore compare the exact JSON object value after JCS
canonicalization. Implementations MUST NOT collapse an audience array to a
string, replace an object with a bare <spanx style="verb">aud</spanx> value, reorder array elements, or
otherwise rewrite the verified audience structure before signing or comparing
<spanx style="verb">target_context</spanx>.</t>
</section>

<section anchor="actor-identity" title="Actor Identity Representation">
<t>This specification requires a canonical representation for actor identity in
profile-defined disclosed-chain entries and step proofs.</t>
<t>Each canonical actor identifier used by this specification MUST be
represented as an ActorID structure containing exactly two members:</t>
<t>
<list style="symbols">
<t><spanx style="verb">iss</spanx>: the issuer identifier naming the namespace in which the actor subject
value is defined; and</t>
<t><spanx style="verb">sub</spanx>: the subject identifier of the actor within that issuer namespace.</t>
</list>
</t>
<t>An ActorID is a JSON object with members <spanx style="verb">iss</spanx> and <spanx style="verb">sub</spanx>, serialized using
JCS <xref target="RFC8785"></xref> when incorporated into profile-defined signed or hashed inputs.</t>
<t>An ActorID:</t>
<t>
<list style="symbols">
<t>MUST be stable for equality comparison within a workflow instance;</t>
<t>MUST be bound to the actor identity accepted under local policy for the
relevant exchange, proof-verification, or acknowledgment-validation step;</t>
<t>MUST be compared using exact equality of the pair (<spanx style="verb">iss</spanx>, <spanx style="verb">sub</spanx>); and</t>
<t>SHOULD support pairwise or pseudonymous subject values where deployment
policy allows.</t>
</list>
</t>
<t>When deriving an ActorID from a validated inbound token:</t>
<t>
<list style="symbols">
<t>for the token subject, use <spanx style="verb">{ &quot;iss&quot;: token.iss, &quot;sub&quot;: token.sub }</spanx>;</t>
<t>for a validated <spanx style="verb">act</spanx> claim that contains both <spanx style="verb">iss</spanx> and <spanx style="verb">sub</spanx>, use those two
values directly; and</t>
<t>for a validated <spanx style="verb">act</spanx> claim that contains <spanx style="verb">sub</spanx> but omits <spanx style="verb">iss</spanx>, use the
enclosing token's <spanx style="verb">iss</spanx> as the ActorID <spanx style="verb">iss</spanx> value and the <spanx style="verb">act.sub</spanx> value as
the ActorID <spanx style="verb">sub</spanx> value.</t>
</list>
</t>
<t>If no usable <spanx style="verb">act</spanx> claim is present and a profile needs the presenting actor,
that actor MUST be established from actor authentication or other locally
trusted inputs outside the scope of this specification and mapped into the same
ActorID representation the issuing Authorization Server uses for proof
construction.</t>
<t>When <spanx style="verb">actp</spanx> is present, the <spanx style="verb">act</spanx> structure used by this specification is an
ActorChainNode. An ActorChainNode is an ActorID object plus an OPTIONAL nested
member named <spanx style="verb">act</spanx> whose value is another ActorChainNode representing the
immediately prior disclosed actor. Newly issued profile-defined <spanx style="verb">act</spanx>
structures MUST carry explicit <spanx style="verb">iss</spanx> and <spanx style="verb">sub</spanx> in every disclosed node. Implementations
MUST be able to decode and normalize a validated inbound disclosed actor chain even
when a node omits <spanx style="verb">iss</spanx> and inherits the enclosing issuer according to the
derivation rule above.</t>
<t>The helper functions used throughout this document are therefore:</t>

<figure><artwork type="text">VisibleChain(act) = ordered list of ActorID values obtained by recursively
                    reading nested `act` from the innermost prior actor to the
                    outermost current actor.

EncodeVisibleChain([A]) = {&quot;iss&quot;: A.iss, &quot;sub&quot;: A.sub}
EncodeVisibleChain([A,B]) = {&quot;iss&quot;: B.iss, &quot;sub&quot;: B.sub, &quot;act&quot;:
                             {&quot;iss&quot;: A.iss, &quot;sub&quot;: A.sub}}
EncodeVisibleChain([A,B,C]) = {&quot;iss&quot;: C.iss, &quot;sub&quot;: C.sub, &quot;act&quot;:
                               {&quot;iss&quot;: B.iss, &quot;sub&quot;: B.sub, &quot;act&quot;:
                                {&quot;iss&quot;: A.iss, &quot;sub&quot;: A.sub}}}
</artwork></figure>

<t>In examples and formulas, <spanx style="verb">[A,B]</spanx> denotes an ordered ActorID sequence for
actors <spanx style="verb">A</spanx> and <spanx style="verb">B</spanx>, while <spanx style="verb">EncodeVisibleChain([A,B])</spanx> denotes the nested JSON
representation carried in <spanx style="verb">act</spanx>.</t>
</section>

<section anchor="issued-token-type" title="Issued Token Type">
<t>Unless another application profile explicitly states otherwise, tokens issued
under this specification are access tokens.</t>
<t>Token exchange responses MUST use the RFC 8693 token type fields consistently
with the underlying representation and deployment.</t>
</section>
</section>

<section anchor="common-validation" title="Common Validation Procedures">
<t>This section gives the short validation checklists that the profile sections
reuse. Detailed enforcement rules for actor authentication inputs, proof-key
binding, intended-recipient handling, and replay or freshness are collected in
{{security-enforcement}}.</t>
<t>Implementations can think of validation in three layers:</t>
<t>
<list style="symbols">
<t><spanx style="strong">Layer 1: Admission</spanx> -- JWT signature, issuer trust, expiry, audience,
intended-recipient checks, and any locally established presenting-actor or
current-actor continuity checks required for that processing step.</t>
<t><spanx style="strong">Layer 2: Profile authorization</spanx> -- interpretation of the disclosed nested
<spanx style="verb">act</spanx> according to <spanx style="verb">actp</spanx> and application of local authorization policy using
only the disclosed actor chain that the profile exposes.</t>
<t><spanx style="strong">Layer 3: Continuity and audit evidence</spanx> -- validation of <spanx style="verb">actc</spanx>, step
proofs, acknowledgments, and retained Authorization Server records.</t>
</list>
</t>
<t>Recipients that only consume an inbound token MAY apply Layer 3 according to
local policy. Current actors that extend a verified workflow, and
Authorization Servers that accept verified profile token exchange, MUST
validate the inbound <spanx style="verb">actc</spanx> and any required step proof before extending the
workflow.</t>

<section anchor="recipient-validation" title="Recipient Validation of an Inbound Token">
<t>Unless a profile states otherwise, a recipient validating an inbound actor-chain
token MUST verify:</t>
<t>
<list style="symbols">
<t>token signature;</t>
<t>issuer trust;</t>
<t>profile identifier (<spanx style="verb">actp</spanx>);</t>
<t>presence and correct format of profile-required structural claims (<spanx style="verb">actc</spanx>
and any profile-required disclosed <spanx style="verb">act</spanx> according to <spanx style="verb">actp</spanx>);</t>
<t>expiry and replay requirements;</t>
<t>intended-recipient requirements; and</t>
<t>any profile-specific disclosed-chain checks.</t>
</list>
</t>
<t>If a disclosed <spanx style="verb">act</spanx> is present, a recipient MUST decode the disclosed actor
chain as <spanx style="verb">VisibleChain(act)</spanx> and apply the profile-specific checks on that
disclosed actor chain. If the selected profile requires disclosed current-actor continuity and
the recipient establishes a presenting-actor identity for the inbound hop under
local policy, the recipient MUST also verify that the outermost <spanx style="verb">act</spanx> identifies
that same presenting actor. If the selected profile requires inline <spanx style="verb">act</spanx>
disclosure, omission of <spanx style="verb">act</spanx>, or presence of an <spanx style="verb">act</spanx> that violates the
profile's required disclosure rules, MUST be treated as a profile-validation
failure.</t>
<t>A recipient MUST use only the actor-chain information actually disclosed in the
inbound artifact for authorization. It MUST treat undisclosed prior actors, and
an undisclosed current actor, as unavailable from that artifact.</t>
</section>

<section anchor="as-token-exchange-validation" title="Authorization Server Validation of Token Exchange">
<t>Unless a profile states otherwise, an Authorization Server validating an
actor-chain token exchange MUST verify:</t>
<t>
<list style="symbols">
<t>the inbound token signature and issuer trust;</t>
<t>the authenticated identity of the current actor;</t>
<t>intended-recipient semantics for the inbound token;</t>
<t>the selected profile identifier and any profile-specific step-proof
requirements;</t>
<t>the preserved <spanx style="verb">acti</spanx> and <spanx style="verb">sub</spanx> continuity expected for the workflow; and</t>
<t>any profile-specific disclosed-chain checks on any inbound <spanx style="verb">act</spanx>.</t>
</list>
</t>
<t>If the inbound token contains disclosed actor-chain state, the Authorization
Server MUST decode <spanx style="verb">VisibleChain(act_in)</spanx> from the inbound token exactly as verified
for the current actor and MUST perform only append-only operations on that
disclosed actor chain fragment. If the selected profile requires disclosed
current-actor continuity, the Authorization Server MUST additionally verify
that the outermost <spanx style="verb">act</spanx> of the inbound token identifies the same actor that
authenticated as the current actor and is presenting that inbound hop for
exchange. If the selected profile requires inline <spanx style="verb">act</spanx> disclosure, omission of
<spanx style="verb">act</spanx>, or presence of an <spanx style="verb">act</spanx> that violates the profile's required disclosure
rules, MUST be treated as a profile-validation failure.</t>
<t>This specification does not define validation or handling rules for
<spanx style="verb">may_act</spanx>. Any effect of <spanx style="verb">may_act</spanx> is determined by RFC 8693, by the
specification governing the artifact that carries it, or by local policy. Such
use MUST NOT cause an artifact issued under this specification, or any related
protocol-visible outcome, to disclose actor-chain information that the
selected profile would withhold.</t>
<t>A token-exchange request under this specification MAY additionally carry the RFC
8693 <spanx style="verb">actor_token</spanx> and <spanx style="verb">actor_token_type</spanx> parameters. This specification does
not define whether or how <spanx style="verb">actor_token</spanx> contributes actor-related policy input.
Any such effect is determined by RFC 8693, by the specification governing that
artifact, or by local policy. Such use MUST NOT expand disclosure beyond the
selected profile and Authorization Server policy, and MUST NOT alter required
<spanx style="verb">acti</spanx>, <spanx style="verb">actp</spanx>, or <spanx style="verb">sub</spanx> continuity.</t>
<t>When <spanx style="verb">actor_token</spanx> is presented together with this specification's
verified-profile parameters, the Authorization Server MUST decide under local
policy which ActorID represents the current actor for that exchange. The
Authorization Server MUST reject the request with <spanx style="verb">invalid_grant</spanx> unless that
ActorID matches the ActorID represented in the submitted
<spanx style="verb">actor_chain_step_proof</spanx> and the proof-signing key is trusted for that ActorID
under local policy. The Authorization Server MUST NOT accept a step proof
attributed to one ActorID while appending a different ActorID to the accepted
actor-chain state.</t>
<t>If neither <spanx style="verb">may_act</spanx> nor <spanx style="verb">actor_token</spanx> contributes actor-related policy input,
the Authorization Server applies local policy. For this specification, local
policy can include trust relationships, authenticated client identity,
permitted target contexts, chain-depth limits, profile support, and business or
risk controls.</t>
</section>

<section anchor="returned-token-validation" title="Current-Actor Validation of a Returned Token">
<t>Unless a profile states otherwise, a current actor validating a returned token
MUST verify:</t>
<t>
<list style="symbols">
<t>token signature and issuer trust;</t>
<t>that the returned <spanx style="verb">actp</spanx> equals the requested profile identifier and any
preserved active profile state;</t>
<t>that the returned <spanx style="verb">acti</spanx> equals the actor-chain identifier already represented by
the inbound token or bootstrap state;</t>
<t>that the returned <spanx style="verb">sub</spanx> equals the expected preserved workflow-subject value;</t>
<t>expiry and replay requirements; and</t>
<t>any profile-specific disclosed-chain or commitment checks.</t>
</list>
</t>
<t>If a returned token discloses <spanx style="verb">act</spanx>, the current actor MUST verify that the
disclosed <spanx style="verb">act</spanx> conforms to the selected profile and Authorization Server
policy for that hop. If the selected profile requires inline <spanx style="verb">act</spanx> disclosure,
omission of <spanx style="verb">act</spanx>, or presence of an <spanx style="verb">act</spanx> that violates the profile's
required disclosure rules, MUST be treated as a profile-validation failure.</t>
<t>For full-disclosure profiles, the current actor MUST verify that the
returned disclosed actor chain equals the complete chain expected for that
hop. For
subset-disclosure profiles, if the returned token discloses <spanx style="verb">act</spanx>, the current
actor MUST verify that the returned disclosed actor chain is an ordered subsequence of
the exact verified actor-visible chain that the actor signed or otherwise
caused to be asserted for that hop. For actor-only profiles, the current actor
MUST verify that the returned <spanx style="verb">act</spanx> consists only of the outermost current
actor and that this actor is the current actor that requested or caused the
hop.</t>
</section>
</section>

<section anchor="profiles" title="Profiles">
<t>This specification defines six explicit actor-chain profiles. For readability,
this section specifies them as the cross-product of two evidence models and
three disclosure modes:</t>

<figure><artwork type="text">Profile = EvidenceModel x DisclosureMode

EvidenceModel = declared | verified
DisclosureMode = full | subset | actor-only
</artwork></figure>

<t>This factoring is editorial. It does not make token shape a substitute for the
explicit <spanx style="verb">actp</spanx> value. Validators, Authorization Servers, current actors, and
recipients MUST apply the rules for the selected <spanx style="verb">actp</spanx> and MUST NOT infer a
different profile from the apparent shape of <spanx style="verb">act</spanx>.</t>
<t>For example, a subset-disclosure token that happens to disclose the full
disclosed actor chain is still a subset-disclosure token, not a full-disclosure token.
Likewise, a subset-disclosure token that happens to disclose only the current
actor is still a subset-disclosure token, not an actor-only token. Full
Disclosure and Actor-Only Disclosure are profile-level commitments expressed
by <spanx style="verb">actp</spanx>, not properties inferred from a single token instance.</t>

<section anchor="profile-identifiers" title="Profile Identifiers and Parameters">
<t>Each value in the following table is used both as the
<spanx style="verb">actor_chain_profile</spanx> token request parameter value and as the <spanx style="verb">actp</spanx> token
claim value.</t>
<texttable>
<ttcol>actp value</ttcol>
<ttcol>Evidence model</ttcol>
<ttcol>Disclosure mode</ttcol>


<c>declared-full</c>
<c>Declared</c>
<c>Full Disclosure</c>

<c>declared-subset</c>
<c>Declared</c>
<c>Subset Disclosure</c>

<c>declared-actor-only</c>
<c>Declared</c>
<c>Actor-Only Disclosure</c>

<c>verified-full</c>
<c>Verified</c>
<c>Full Disclosure</c>

<c>verified-subset</c>
<c>Verified</c>
<c>Subset Disclosure</c>

<c>verified-actor-only</c>
<c>Verified</c>
<c>Actor-Only Disclosure</c>

</texttable><t>The evidence model determines how chain continuity is established:</t>
<t>
<list style="symbols">
<t><spanx style="strong">Declared</spanx> profiles rely on Authorization-Server-asserted chain continuity
and profiled access token signature validation.</t>
<t><spanx style="strong">Verified</spanx> profiles add actor-signed step proofs and cumulative commitment
state in <spanx style="verb">actc</spanx>.</t>
</list>
</t>
<t>The disclosure mode determines how much actor-chain information is visible in
profiled access tokens.</t>
</section>

<section anchor="disclosure-modes" title="Disclosure Modes">
<t>Disclosure mode is applied to a profile-defined chain for the hop. For declared
profiles, that chain is the Authorization Server's accepted actor-chain state
after appending the authenticated current actor. For verified profiles, that
chain is the actor-visible chain for the hop that is bound into the submitted
step proof.</t>
<t>The following abstract disclosure function describes the output shape:</t>

<figure><artwork type="text">Disclose(chain, mode, current_actor, policy):

  if mode == full:
      return EncodeVisibleChain(chain)
      act is mandatory

  if mode == subset:
      return EncodeVisibleChain(S), where S is an ordered subsequence of chain
      or omit act entirely when policy permits

  if mode == actor-only:
      return EncodeVisibleChain([current_actor])
      act is mandatory
</artwork></figure>

<t>This function is explanatory. The normative rules for each mode are below.</t>

<section anchor="full-disclosure" title="Full Disclosure">
<t>Under Full Disclosure, <spanx style="verb">act</spanx> is mandatory in every profiled access token issued under
the profile. The disclosed actor chain, obtained from <spanx style="verb">VisibleChain(act)</spanx>,
MUST equal the complete profile-defined chain for the hop. The Authorization
Server MUST NOT omit <spanx style="verb">act</spanx>, disclose only a subset, or disclose only the
current actor under a Full Disclosure profile.</t>
<t>Recipients and current actors MAY use the complete disclosed actor chain for
profile-specific authorization and continuity decisions. A token issued under a
subset profile that happens to disclose the complete chain MUST NOT be
processed as a Full Disclosure profile token unless its <spanx style="verb">actp</spanx> selects a Full
Disclosure profile.</t>
</section>

<section anchor="subset-disclosure" title="Subset Disclosure">
<t>Under Subset Disclosure, the Authorization Server MAY disclose a
recipient-specific <spanx style="verb">act</spanx> chain or MAY omit <spanx style="verb">act</spanx> entirely according to local
policy and the selected profile. When <spanx style="verb">act</spanx> is present, the disclosed actor
chain, obtained from <spanx style="verb">VisibleChain(act)</spanx>, MUST be an ordered
subsequence of the profile-defined chain for the hop.</t>
<t>A recipient or current actor MUST use only the actor-chain information actually
disclosed in the artifact. It MUST treat undisclosed prior actors, an
undisclosed current actor, hidden prefixes, adjacency, absence, and exact chain
length as unavailable from that artifact. It MUST NOT infer hidden actors or
chain structure from omitted entries, identifier structure, <spanx style="verb">actc</spanx>, or other
claims.</t>
<t>If an inbound token under a subset profile omits <spanx style="verb">act</spanx>, or discloses <spanx style="verb">act</spanx>
without the current actor, this specification provides no inline current-actor
disclosure for that hop. Any additional authorization inputs are determined by
local policy and are outside the scope of this specification.</t>
<t>A singleton current-actor-only disclosure is syntactically possible under
Subset Disclosure, but it is not an Actor-Only Disclosure profile commitment.
Deployments that require actor-only behavior to be explicit and machine-
readable SHOULD use an Actor-Only Disclosure profile.</t>
</section>

<section anchor="actor-only-disclosure" title="Actor-Only Disclosure">
<t>Under Actor-Only Disclosure, <spanx style="verb">act</spanx> is mandatory in every profiled access token issued
under the profile. The disclosed actor chain, obtained from
<spanx style="verb">VisibleChain(act)</spanx>, MUST contain exactly one ActorID identifying the current
actor represented by that token. Prior actors MUST NOT be disclosed inline in
profiled access tokens under an Actor-Only Disclosure profile.</t>
<t>For actor-chain authorization, recipients MUST use only the disclosed current
actor and local policy.
They MUST NOT infer prior actors, chain length, or hidden chain membership from
omission, identifier structure, <spanx style="verb">actc</spanx>, or other claims. A token issued under a
subset profile that happens to disclose only the current actor MUST NOT be
processed as an Actor-Only Disclosure profile token unless its <spanx style="verb">actp</spanx> selects
an Actor-Only Disclosure profile.</t>
</section>
</section>

<section anchor="authorization-rules-by-disclosure-mode" title="Authorization Rules by Disclosure Mode">
<t>The following table summarizes the actor-chain information available for
next-hop authorization under the selected profile.</t>
<texttable>
<ttcol>Disclosure mode</ttcol>
<ttcol>Profiled access token authorization input</ttcol>


<c>Full Disclosure</c>
<c>The complete disclosed actor chain for the hop.</c>

<c>Subset Disclosure</c>
<c>The disclosed ordered subset only, if any. Undisclosed actors are unavailable from the artifact.</c>

<c>Actor-Only Disclosure</c>
<c>The disclosed current actor only. Prior actors are unavailable from the artifact.</c>

</texttable><t>Deployments in which recipient authorization depends on prior-path membership
or order MUST select a profile and disclosure policy that disclose the required
prior-path evidence at that hop. Actor-only outcomes and omitted-<spanx style="verb">act</spanx> outcomes
are not sufficient inputs for path-sensitive authorization decisions.</t>
</section>

<section anchor="security-properties-by-evidence-model-and-disclosure-mode" title="Security Properties by Evidence Model and Disclosure Mode">
<t>The following list summarizes the security result of the factored profile
model. The detailed normative processing rules remain in the evidence-model and
profile-delta sections below.</t>
<t>
<list style="symbols">
<t>Declared Full Disclosure provides Authorization-Server-asserted continuity
over the complete disclosed actor chain under the non-collusion assumption.</t>
<t>Declared Subset Disclosure provides Authorization-Server-asserted continuity
for the accepted chain state, with profiled access token authorization limited
to the disclosed subset, if any.</t>
<t>Declared Actor-Only Disclosure provides Authorization-Server-asserted
current-actor continuity. Prior actors are available only through retained
Authorization Server records or other out-of-band evidence.</t>
<t>Verified Full Disclosure uses actor-signed step proofs and cumulative <spanx style="verb">actc</spanx>
to bind the complete disclosed actor chain for each hop.</t>
<t>Verified Subset Disclosure uses actor-signed step proofs and cumulative
<spanx style="verb">actc</spanx> to bind the actor-visible chain for each hop, while profiled access
tokens disclose only an ordered subset or omit <spanx style="verb">act</spanx>.</t>
<t>Verified Actor-Only Disclosure keeps actor-signed step proofs and cumulative
<spanx style="verb">actc</spanx> available for later review, while profiled access tokens disclose
exactly the current actor.</t>
</list>
</t>
</section>
</section>

<section anchor="declared-profiles" title="Declared Profiles">
<t>The declared profiles use the declared evidence model with one of the three
disclosure modes. They rely on Authorization-Server-asserted chain continuity,
profile-specific disclosure policy, profiled access token signature validation,
and the common validation procedures in this document. They do not use actor-signed
step proofs or <spanx style="verb">actc</spanx>.</t>
<t>Declared profiles assume that an actor does not collude with its home
Authorization Server. Under that non-collusion assumption, prior actors MUST
NOT be silently inserted, removed, reordered, or altered during token exchange
for the disclosed chain state governed by the selected profile.</t>

<section anchor="declared-common-processing" title="Declared Common Processing">

<section anchor="declared-bootstrap" title="Declared Bootstrap">
<t>At workflow start, the initial actor <spanx style="verb">A</spanx> MUST request a token from the
Authorization Server with at least:</t>
<t>
<list style="symbols">
<t><spanx style="verb">grant_type=client_credentials</spanx>;</t>
<t><spanx style="verb">actor_chain_profile</spanx> set to one of the declared profile identifiers; and</t>
<t>the requested OAuth targeting parameters (<spanx style="verb">audience</spanx>, <spanx style="verb">resource</spanx>, or both)
sufficient to identify the initial target context.</t>
</list>
</t>
<t>If the Authorization Server accepts the request, it MUST authenticate the
initial actor, bind that actor to its ActorID representation, establish the
workflow subject according to local policy, mint a fresh <spanx style="verb">acti</spanx>, and create the
initial accepted actor-chain state <spanx style="verb">[A]</spanx>. The Authorization Server MUST then
issue a profiled access token containing at least <spanx style="verb">iss</spanx>, <spanx style="verb">actp</spanx>, <spanx style="verb">acti</spanx>, <spanx style="verb">sub</spanx>,
<spanx style="verb">jti</spanx>, <spanx style="verb">aud</spanx>, and <spanx style="verb">exp</spanx>, plus <spanx style="verb">act</spanx> as required by the selected disclosure
mode.</t>
<t>At bootstrap and at each later exchange, the chosen <spanx style="verb">sub</spanx> representation MUST
remain consistent with the selected <spanx style="verb">actp</spanx> disclosure constraints. In
particular, <spanx style="verb">sub</spanx> MUST NOT disclose an actor identity or other actor-chain
information that the selected profile is intended to withhold from the relevant
recipient class.</t>
</section>

<section anchor="declared-chain-extension" title="Declared Chain Extension">
<t>For a declared chain-extending token exchange, the current actor MUST submit at
least:</t>
<t>
<list style="symbols">
<t><spanx style="verb">grant_type=urn:ietf:params:oauth:grant-type:token-exchange</spanx>;</t>
<t><spanx style="verb">actor_chain_profile</spanx> set to the active declared profile identifier;</t>
<t>the inbound token as the RFC 8693 <spanx style="verb">subject_token</spanx>;</t>
<t><spanx style="verb">subject_token_type=urn:ietf:params:oauth:token-type:access_token</spanx>; and</t>
<t>the requested OAuth targeting parameters (<spanx style="verb">audience</spanx>, <spanx style="verb">resource</spanx>, or both as
needed by local policy) sufficient to identify the next target context.</t>
</list>
</t>
<t>The current actor MAY additionally submit RFC 8693 <spanx style="verb">actor_token</spanx> and
<spanx style="verb">actor_token_type</spanx> parameters subject to the common validation rules in
{{as-token-exchange-validation}}.</t>
<t>The Authorization Server MUST perform token-exchange validation as described in
{{as-token-exchange-validation}}. It MUST verify that the
requested profile equals the inbound token's <spanx style="verb">actp</spanx>, authenticate the current
actor, verify intended-recipient semantics for the inbound token, preserve
<spanx style="verb">acti</spanx> and <spanx style="verb">sub</spanx> continuity, and enforce profile-specific disclosure rules.</t>
<t>The Authorization Server MUST derive the next-hop accepted chain state from the
accepted workflow state for the inbound hop, append the authenticated current
actor, and issue a returned profiled access token whose <spanx style="verb">act</spanx> is determined by the
selected disclosure mode. It MUST NOT insert, reorder, delete, or alter prior
actors in any disclosed <spanx style="verb">act</spanx>.</t>
<t>Because the returned profiled access token is visible to the current actor in this
base JWT/JWS binding, the Authorization Server MUST NOT disclose in that
returned token any actor identity that the current actor is not permitted to
learn.</t>
</section>

<section anchor="declared-validation-and-authorization" title="Declared Validation and Authorization">
<t>A recipient of a declared-profile token MUST perform recipient validation as
described in {{recipient-validation}} and MUST apply the
disclosure-mode rules for the selected <spanx style="verb">actp</spanx>.</t>
<t>A current actor validating a returned declared-profile token MUST perform
current-actor returned-token validation as described in
{{returned-token-validation}} and MUST apply the disclosure-mode rules for the
selected <spanx style="verb">actp</spanx>.</t>
<t>For declared subset and declared actor-only profiles, the current actor and
downstream recipient validate only the actor-chain information disclosed to
them. They do not independently validate hidden undisclosed portions of the
Authorization Server's retained workflow chain state.</t>
<t>The Authorization Server MAY retain authoritative workflow chain state richer
than the profiled access token <spanx style="verb">act</spanx> disclosure for audit, forensics, legal review,
and branch reconstruction. Such retained state is not available to a recipient
from the profiled access token unless it is disclosed by the selected profile and
policy or made available through another mechanism outside this specification.</t>
</section>
</section>

<section anchor="declared-full" title="Declared Full Disclosure Profile">
<t>The profile identifier for this profile is <spanx style="verb">declared-full</spanx>.</t>
<t>This profile uses the declared evidence model and Full Disclosure mode.
Accordingly, <spanx style="verb">act</spanx> is mandatory in every profiled access token and
<spanx style="verb">VisibleChain(act)</spanx> MUST equal the complete accepted actor-chain state for the
hop. At bootstrap, the profiled access token MUST contain
<spanx style="verb">act=EncodeVisibleChain([A])</spanx>. At a later hop by current actor <spanx style="verb">N</spanx>, the
returned profiled access token MUST disclose the prior accepted chain state with <spanx style="verb">N</spanx>
appended.</t>
<t>A recipient MUST use the complete disclosed actor chain for actor-chain authorization
where actor-chain authorization is required. A current actor validating a
returned token MUST verify that the returned disclosed actor chain is exactly the
previously verified disclosed actor chain with that current actor appended.</t>
</section>

<section anchor="declared-subset" title="Declared Subset Disclosure Profile">
<t>The profile identifier for this profile is <spanx style="verb">declared-subset</spanx>.</t>
<t>This profile uses the declared evidence model and Subset Disclosure mode.
Accordingly, the Authorization Server MAY disclose a recipient-specific ordered
subsequence of the accepted chain state for the hop, or MAY omit <spanx style="verb">act</spanx>
entirely according to local policy. If <spanx style="verb">act</spanx> is present, <spanx style="verb">VisibleChain(act)</spanx>
MUST be an ordered subsequence of the accepted chain state for that hop.</t>
<t>A recipient MUST authorize using only the disclosed actor chain, if any, and
MUST treat undisclosed actors as unavailable. If the token omits <spanx style="verb">act</spanx>, or
discloses <spanx style="verb">act</spanx> without the current actor, this specification provides no
inline current-actor disclosure for that hop.</t>
<t>Cross-domain re-issuance under this profile preserves only the disclosed
asserted chain state carried in the inbound token unless a trusted companion
mechanism
explicitly transfers additional hidden state. This profile does not provide the
step-proof-based accountability or cumulative commitment state of the verified
profiles.</t>
</section>

<section anchor="declared-actor-only" title="Declared Actor-Only Disclosure Profile">
<t>The profile identifier for this profile is <spanx style="verb">declared-actor-only</spanx>.</t>
<t>This profile uses the declared evidence model and Actor-Only Disclosure mode.
Accordingly, <spanx style="verb">act</spanx> is mandatory in every profiled access token and
<spanx style="verb">VisibleChain(act)</spanx> MUST equal <spanx style="verb">[current_actor]</spanx>. The Authorization Server MUST
NOT disclose prior actors inline in profiled access tokens under this profile.</t>
<t>A recipient MUST authorize only on the disclosed current actor and local policy.
Prior actors remain available only through Authorization Server records or
other out-of-band evidence.</t>
</section>
</section>

<section anchor="verified-proof-input" title="Verified Profile Proof Input Model">

<section anchor="proof-input-template" title="Proof Input Template">
<t>The verified profiles use a limited number of proof-input templates. This
section defines them once so that the verified profile sections can state only
their profile-specific substitutions. Canonicalization, hashing, and commitment
construction are defined in {{canonicalization-commitment}}.</t>
<t>Let:</t>
<t>
<list style="symbols">
<t><spanx style="verb">profile</spanx> be the active <spanx style="verb">actp</spanx> value;</t>
<t><spanx style="verb">acti</spanx> be the stable actor-chain identifier;</t>
<t><spanx style="verb">prev_state</spanx> be either the returned base64url <spanx style="verb">initial_chain_seed</spanx> from
bootstrap or the verified prior commitment digest string from <spanx style="verb">actc.curr</spanx>, as
required by the profile;</t>
<t><spanx style="verb">visible_actor_chain_for_hop</spanx> be the exact ordered actor-visible chain for
the hop after appending the authenticated current actor;</t>
<t><spanx style="verb">workflow_sub</spanx> be the exact preserved workflow <spanx style="verb">sub</spanx> string for the hop being
extended within the current issuer domain;</t>
<t><spanx style="verb">TC_next</spanx> be the canonical <spanx style="verb">target_context</spanx> for the next hop, often the
object <spanx style="verb">{ &quot;aud&quot;: aud }</spanx> but extended when local policy needs finer-grained
target binding;</t>
<t><spanx style="verb">iat_N</spanx> be the JWT NumericDate at which actor <spanx style="verb">N</spanx> signs the step proof; and</t>
<t><spanx style="verb">[N]</spanx> denote the canonical ActorID JSON object representation of the
authenticated current actor.</t>
</list>
</t>
<t>Symbols such as <spanx style="verb">TC_B</spanx>, <spanx style="verb">TC_C</spanx>, and <spanx style="verb">TC_next</spanx> denote the canonical
<spanx style="verb">target_context</spanx> for the corresponding next hop.</t>
<t>Proof-bound profiles instantiate the following generic step-proof payload
template:</t>

<figure><artwork type="json">Sign_N({
  &quot;ctx&quot;: ds(profile),
  &quot;acti&quot;: acti,
  &quot;prev&quot;: prev_state,
  &quot;sub&quot;: workflow_sub,
  &quot;act&quot;: EncodeVisibleChain(visible_actor_chain_for_hop),
  &quot;target_context&quot;: TC_next,
  &quot;iat&quot;: iat_N
})
</artwork></figure>

<t>The domain-separation string <spanx style="verb">ds(profile)</spanx> is profile-specific:</t>
<t>
<list style="symbols">
<t><spanx style="verb">actor-chain-verified-full-step-sig-v1</spanx> for <spanx style="verb">verified-full</spanx>;</t>
<t><spanx style="verb">actor-chain-verified-subset-step-sig-v1</spanx> for <spanx style="verb">verified-subset</spanx>; and</t>
<t><spanx style="verb">actor-chain-verified-actor-only-step-sig-v1</spanx> for <spanx style="verb">verified-actor-only</spanx>.</t>
</list>
</t>
<t>These strings remain distinct even though the verified branch step-proof
payload members are structurally aligned. The signed step-proof payload does
not carry <spanx style="verb">actp</spanx> or another explicit profile identifier, and the meaning of the
<spanx style="verb">act</spanx> member remains profile-dependent. Distinct domain-separation strings are
therefore REQUIRED to bind the proof to the intended verified profile semantics
and to prevent cross-profile proof confusion or accidental proof reuse.</t>
<t>The trailing <spanx style="verb">-v1</spanx> suffix in each domain-separation string identifies the
step-proof payload version defined by the initial standardized version of this
specification. During Internet-Draft development before that version is
standardized, changes to the step-proof payload can still be incorporated into
<spanx style="verb">-v1</spanx>. After the initial standardized version is published, any revision that
changes the membership, semantics, or canonicalization of the step-proof payload
in a non-compatible way MUST allocate a new suffix, such as <spanx style="verb">-v2</spanx>. Verifiers
MUST reject a step proof whose <spanx style="verb">ctx</spanx> value does not exactly match an expected,
implemented domain-separation string for the active <spanx style="verb">actp</spanx>. Verifiers MUST NOT
accept a step proof signed under a different version suffix as equivalent.</t>
<t>In same-domain verified chain-extension hops, the step proof also binds the
exact preserved workflow <spanx style="verb">sub</spanx> string for that hop. This protects against
same-domain silent subject substitution without requiring this base
specification to cryptographically bind later cross-domain <spanx style="verb">sub</spanx> aliasing.</t>
<t>For verified profiles, <spanx style="verb">visible_actor_chain_for_hop</spanx> is the actor-visible chain
that the current actor is permitted to know and extend for that hop. It is not
necessarily the same as the disclosed <spanx style="verb">act</spanx> in the returned profiled access
token.</t>
<t>The profile-specific meaning of <spanx style="verb">visible_actor_chain_for_hop</spanx> is:</t>
<t>
<list style="symbols">
<t>for <spanx style="verb">verified-full</spanx>, the complete disclosed actor chain for the hop after appending the
authenticated current actor;</t>
<t>for <spanx style="verb">verified-subset</spanx>, the exact inbound disclosed actor chain verified by the
current actor, or the empty chain if no <spanx style="verb">act</spanx> was disclosed, with the current
actor appended; and</t>
<t>for <spanx style="verb">verified-actor-only</spanx>, the exact inbound disclosed actor chain verified
by the current actor, with the current actor appended, even though the
returned profiled access token later discloses only <spanx style="verb">[N]</spanx>.</t>
</list>
</t>
<t>For verified subset and verified actor-only profiles, the actor-visible chain
signed by the current actor can be broader than the disclosed <spanx style="verb">act</spanx> in the
returned profiled access token. The returned profiled access token's <spanx style="verb">act</spanx> is therefore
not necessarily the complete signed proof input.</t>
</section>
</section>

<section anchor="verified-profiles" title="Verified Profiles">
<t>The verified profiles use the verified evidence model with one of the three
disclosure modes. They add actor-signed step proofs and cumulative commitment
state in <spanx style="verb">actc</spanx> to the common actor-chain model. Each verified profile inherits
the common model, token requirements, proof input model, validation procedures,
and common verified processing below except as modified by its disclosure mode
or profile-specific delta.</t>

<section anchor="verified-common-processing" title="Common Processing for the Verified Branch">
<t>This section defines the bootstrap, proof, commitment, token-exchange, and
returned-token rules shared by the three verified profiles. In this branch,
profiled access tokens still carry the hop-to-hop token state, but each chain-
extending hop is also backed by an actor-signed step proof and cumulative
commitment state.</t>

<section anchor="common-parameters" title="Common Parameters">
<t>Each verified profile supplies the following profile-specific parameters to the
common processing below.</t>
<texttable>
<ttcol>Profile</ttcol>
<ttcol>actp value</ttcol>
<ttcol>Step-proof domain-separation string</ttcol>
<ttcol>Returned act</ttcol>


<c>Verified Full Disclosure</c>
<c>verified-full</c>
<c>actor-chain-verified-full-step-sig-v1</c>
<c>Complete verified disclosed actor chain</c>

<c>Verified Subset Disclosure</c>
<c>verified-subset</c>
<c>actor-chain-verified-subset-step-sig-v1</c>
<c>Ordered subsequence, or omitted act</c>

<c>Verified Actor-Only Disclosure</c>
<c>verified-actor-only</c>
<c>actor-chain-verified-actor-only-step-sig-v1</c>
<c>Singleton current actor</c>

</texttable><t>For Verified Full Disclosure, the proof-bound actor-visible chain is the
complete actor-visible chain for the hop after appending the current actor. For
Verified Subset Disclosure and Verified Actor-Only Disclosure, the proof-bound
actor-visible chain is the exact inbound disclosed actor chain verified by the
current actor, with that actor appended.</t>
<t>The profile identifier, step-proof domain-separation string, and the
profile-specific interpretation of <spanx style="verb">act</spanx> remain aligned across the verified
branch.</t>
</section>

<section anchor="verified-bootstrap-context" title="Common Bootstrap Context Request">
<t>At workflow start, the initial actor MUST request a bootstrap context from the
Authorization Server's <spanx style="verb">actor_chain_bootstrap_endpoint</spanx> using at least:</t>
<t>
<list style="symbols">
<t><spanx style="verb">grant_type=urn:ietf:params:oauth:grant-type:actor-chain-bootstrap</spanx>;</t>
<t>the selected verified profile <spanx style="verb">actor_chain_profile</spanx>; and</t>
<t>the requested OAuth targeting parameters (<spanx style="verb">audience</spanx>, <spanx style="verb">resource</spanx>, or both)
sufficient to identify the initial target context.</t>
</list>
</t>
<t>The Authorization Server MUST authenticate the initial actor, bind that actor
to its ActorID representation, select a commitment hash algorithm, mint a fresh
<spanx style="verb">acti</spanx>, choose a stable workflow-subject representation for <spanx style="verb">sub</spanx>, derive the
bootstrap target context from the requested targeting parameters, and return a
bootstrap response containing at least:</t>
<t>
<list style="symbols">
<t><spanx style="verb">actor_chain_bootstrap_context</spanx>, an opaque bootstrap handle;</t>
<t><spanx style="verb">acti</spanx>;</t>
<t><spanx style="verb">sub</spanx>;</t>
<t><spanx style="verb">halg</spanx>;</t>
<t><spanx style="verb">target_context</spanx>, the exact canonical bootstrap-bound target context against
which the initial step proof will be validated; and</t>
<t><spanx style="verb">initial_chain_seed</spanx>, a base64url value to be used as <spanx style="verb">prev_state</spanx> for the
initial verified step proof.</t>
</list>
</t>
<t>The <spanx style="verb">initial_chain_seed</spanx> MUST be generated using a CSPRNG with at least 128
bits of entropy, MUST be unique per workflow instance, and MUST NOT be derived
solely from <spanx style="verb">acti</spanx> or other predictable inputs.</t>
<t>The bootstrap context MUST be integrity-protected by the Authorization Server,
bound to the authenticated initial actor, the selected verified profile, the
chosen <spanx style="verb">acti</spanx>, <spanx style="verb">sub</spanx>, <spanx style="verb">halg</spanx>, and the bootstrap target context, and be
short-lived. It authorizes issuance of initial verified profiled access tokens for
that workflow instance while it remains valid.</t>
<t>For a given canonical chosen initial <spanx style="verb">target_context</spanx>, the Authorization Server
MUST treat repeated redemption of the same bootstrap handle as an idempotent
retry and MUST return the previously accepted initial state, or an equivalent
token representing that same accepted initial state. It MUST NOT issue a second
distinct accepted initial state for that same canonical chosen initial
<spanx style="verb">target_context</spanx>.</t>
<t>The Authorization Server MAY accept additional redemptions of the same
bootstrap handle for distinct canonical chosen initial <spanx style="verb">target_context</spanx> values,
thereby minting multiple accepted initial successors that share the same <spanx style="verb">acti</spanx>
and <spanx style="verb">initial_chain_seed</spanx>. If a deployment expects multiple distinct initial
successors under the same nominal target, the chosen initial <spanx style="verb">target_context</spanx>
MUST include a unique <spanx style="verb">request_id</spanx>.</t>
</section>

<section anchor="verified-initial-step" title="Common Initial Actor Step Proof and Bootstrap Issuance">
<t>After receiving the bootstrap response, the initial actor <spanx style="verb">A</spanx> MUST choose the
initial target context to be used for the first issued token. That chosen
initial target context MUST be identical to, or a locally authorized narrowing
of, the canonical bootstrap-bound <spanx style="verb">target_context</spanx> returned in the bootstrap
response. Because there are no prior actors at workflow start, the
profile-defined actor-visible chain for that initial hop is <spanx style="verb">[A]</spanx> for all
verified profiles. <spanx style="verb">A</spanx> MUST then sign an <spanx style="verb">actor_chain_step_proof</spanx> over that
initial actor-visible chain, the exact preserved workflow <spanx style="verb">sub</spanx> string returned
in the bootstrap response, the chosen initial target context, and a signing
<spanx style="verb">iat</spanx>, and redeem the bootstrap context at the token endpoint using at least:</t>
<t>
<list style="symbols">
<t><spanx style="verb">grant_type=client_credentials</spanx>;</t>
<t>the selected verified profile <spanx style="verb">actor_chain_profile</spanx>;</t>
<t><spanx style="verb">actor_chain_bootstrap_context</spanx>;</t>
<t><spanx style="verb">actor_chain_step_proof</spanx>; and</t>
<t>the requested OAuth targeting parameters (<spanx style="verb">audience</spanx>, <spanx style="verb">resource</spanx>, or both)
sufficient to identify that chosen initial target context.</t>
</list>
</t>
<t>The Authorization Server MUST verify the bootstrap context and verify that the
authenticated actor redeeming it is the same actor to which the bootstrap
context was issued. It MUST also verify that the requested profile matches the
bound bootstrap profile, that the chosen target context is identical to or
narrower than the bootstrap-bound target context according to local policy,
that the submitted step proof <spanx style="verb">ctx</spanx> equals the active profile's domain-
separation string, that the submitted step proof <spanx style="verb">sub</spanx> equals the bound
workflow <spanx style="verb">sub</spanx> string, that the submitted step proof <spanx style="verb">target_context</spanx> equals
the chosen target context, that the submitted step proof <spanx style="verb">iat</spanx> is acceptable
under {{jwt-step-proof}}, and that the submitted step proof is otherwise valid.
It then creates the initial <spanx style="verb">actc</spanx> and issues a profiled access token containing
at least <spanx style="verb">iss</spanx>, <spanx style="verb">actp</spanx>, <spanx style="verb">acti</spanx>, <spanx style="verb">sub</spanx>, <spanx style="verb">jti</spanx>, <spanx style="verb">aud</spanx>, <spanx style="verb">exp</spanx>, and <spanx style="verb">actc</spanx>, plus
<spanx style="verb">act</spanx> as required by the selected disclosure mode. The issued token target
claims MUST be identical to or narrower than the chosen target context.</t>
<t>Because the initial verified hop has no prior actors, the exact verified
actor-visible chain for that hop is <spanx style="verb">[A]</spanx>. The returned profiled access token
MUST disclose that chain as <spanx style="verb">act=EncodeVisibleChain([A])</spanx> for <spanx style="verb">verified-full</spanx>
and <spanx style="verb">verified-actor-only</spanx>. For <spanx style="verb">verified-subset</spanx>, the Authorization Server MAY
either disclose <spanx style="verb">act=EncodeVisibleChain([A])</spanx> or omit <spanx style="verb">act</spanx> entirely according
to local policy. When <spanx style="verb">verified-subset</spanx> omits bootstrap <spanx style="verb">act</spanx>, subsequent
disclosed visible-predecessor continuity begins from the empty disclosed chain,
while commitment continuity remains anchored by the initial verified step proof
and <spanx style="verb">actc</spanx>.</t>
</section>

<section anchor="common-hop-processing" title="Common Hop Processing">
<t>When a current actor <spanx style="verb">N</spanx> receives an inbound verified-profile token, it MUST:</t>
<t>
<list style="symbols">
<t>validate the inbound token and any required <spanx style="verb">actc</spanx>;</t>
<t>determine, under local policy, any presenting-actor identity for the inbound
hop needed by the selected profile;</t>
<t>derive the profile-defined actor-visible chain for the new hop;</t>
<t>sign <spanx style="verb">actor_chain_step_proof</spanx> over that actor-visible chain, the preserved
workflow <spanx style="verb">sub</spanx> string for the hop, the next target context, and a signing
<spanx style="verb">iat</spanx>; and</t>
<t>submit the inbound token plus the step proof in a token exchange request to
its home Authorization Server.</t>
</list>
</t>
</section>

<section anchor="common-token-exchange" title="Common Token Exchange">
<t>For a chain-extending verified-profile token exchange, the current actor MUST
submit at least:</t>
<t>
<list style="symbols">
<t><spanx style="verb">grant_type=urn:ietf:params:oauth:grant-type:token-exchange</spanx>;</t>
<t>the selected verified profile <spanx style="verb">actor_chain_profile</spanx>;</t>
<t>the inbound token as the RFC 8693 <spanx style="verb">subject_token</spanx>;</t>
<t><spanx style="verb">subject_token_type=urn:ietf:params:oauth:token-type:access_token</spanx>;</t>
<t><spanx style="verb">actor_chain_step_proof</spanx>; and</t>
<t>the requested OAuth targeting parameters sufficient to identify the next
target context.</t>
</list>
</t>
<t>The current actor MAY additionally submit RFC 8693 <spanx style="verb">actor_token</spanx> and
<spanx style="verb">actor_token_type</spanx> parameters subject to the common validation rules in
{{as-token-exchange-validation}}.</t>
<t>The Authorization Server MUST validate the inbound token and the submitted step
proof for the active profile. It MUST derive the accepted target context from
the token-exchange request and verify that the step proof <spanx style="verb">target_context</spanx>
equals that accepted target context, unless the Authorization Server explicitly
narrows the requested authority. If the Authorization Server narrows the
authority, the narrowed accepted target context MUST be identical to or narrower
than the proof-bound <spanx style="verb">target_context</spanx>.</t>
<t>The Authorization Server MUST verify that the step proof <spanx style="verb">ctx</spanx> equals the
active profile's domain-separation string, that the step proof <spanx style="verb">sub</spanx> equals the
preserved workflow <spanx style="verb">sub</spanx> string for the hop being extended, that the step proof
<spanx style="verb">iat</spanx> is acceptable under {{jwt-step-proof}}, and that the step proof's <spanx style="verb">act</spanx>
equals the profile-defined actor-visible chain for the hop. It MUST verify
append-only processing of that actor-visible chain, update <spanx style="verb">actc</spanx>, and return a
profiled access token whose disclosed <spanx style="verb">act</spanx> representation matches the selected
disclosure mode and whose target claims, including <spanx style="verb">aud</spanx> and any <spanx style="verb">resource</spanx> or
target-context representation used by the deployment, are identical to or
narrower than the accepted target context. The Authorization Server MUST NOT
issue a profiled access token whose target authority is broader than, or
semantically unrelated to, the proof-bound <spanx style="verb">target_context</spanx>. Replay,
idempotency, and multiple-successor handling for submitted step proofs are
defined in {{replay-freshness}}.</t>
<t>For <spanx style="verb">verified-subset</spanx> and <spanx style="verb">verified-actor-only</spanx>, step proofs and <spanx style="verb">actc</spanx> values
MUST be computed over the exact actor-visible chain for the hop, not over a
hidden canonical full chain that the current actor was not permitted to see.</t>
</section>

<section anchor="common-returned-token-validation" title="Common Returned-Token Validation">
<t>When validating a returned verified-profile token, the current actor MUST
perform the common returned-token validation in {{returned-token-validation}}
and MUST also verify:</t>
<t>
<list style="symbols">
<t>that <spanx style="verb">actc</spanx> is present and valid;</t>
<t>that the returned token preserves <spanx style="verb">acti</spanx>, <spanx style="verb">actp</spanx>, and <spanx style="verb">sub</spanx> as required;</t>
<t>that the embedded <spanx style="verb">actc</spanx> payload preserves the expected <spanx style="verb">acti</spanx>, <spanx style="verb">actp</spanx>, and
<spanx style="verb">halg</spanx> values for the active workflow state;</t>
<t>that <spanx style="verb">actc.prev</spanx> equals the previously verified prior commitment digest, or
the <spanx style="verb">initial_chain_seed</spanx> for the initial verified hop;</t>
<t>that <spanx style="verb">actc.step_hash</spanx> equals <spanx style="verb">b64url(Hash_halg(step_proof_bytes))</spanx> computed
over the exact compact JWS string submitted as <spanx style="verb">actor_chain_step_proof</spanx> for
that hop;</t>
<t>that the returned token target claims are identical to or narrower than the
proof-bound target context for that hop; and</t>
<t>any profile-specific disclosed-chain checks for the active disclosure mode.</t>
</list>
</t>
<t>If the returned artifact or companion validation interface does not expose
enough target information to perform the target check above, the current actor
MUST treat that condition as a profile-validation failure.</t>
<t>Because <spanx style="verb">actc</spanx> is cumulative commitment state carried inline in profiled
access tokens issued under verified profiles, every later actor and every
Authorization Server that
extends the verified workflow relies on that inline <spanx style="verb">actc</spanx> value. Online
validation of an inbound or returned <spanx style="verb">actc</spanx> proves issuer-signed commitment
continuity and internal commitment consistency. It does not by itself prove the
semantic meaning of <spanx style="verb">step_hash</spanx> against the underlying step proof unless the
exact step proof bytes and related verification material are also retained or
discoverable for later audit. Deployments MAY vary on whether every terminal
recipient performs synchronous <spanx style="verb">actc</spanx> validation at admission time, but chain
extension MUST NOT proceed without successful validation of the inbound <spanx style="verb">actc</spanx>.</t>
<t>For non-repudiation, participation, or later semantic interpretation,
deployments SHOULD retain the exact step-proof artifact and associated
verification context used when validating commitment linkage.</t>
<t>For <spanx style="verb">verified-subset</spanx> and <spanx style="verb">verified-actor-only</spanx>, the current actor's signature
provides non-repudiation only over the exact actor-visible chain for that hop
and the linked prior commitment digest, not over hidden prefix semantics that
the actor was not permitted to verify.</t>
</section>
</section>

<section anchor="verified-full" title="Verified Full Disclosure Profile">
<t>The profile identifier for this profile is <spanx style="verb">verified-full</spanx>.</t>
<t>This profile uses the verified evidence model and Full Disclosure mode.
Accordingly, <spanx style="verb">act</spanx> is mandatory in every profiled access token and
<spanx style="verb">VisibleChain(act)</spanx> MUST equal the complete verified actor-visible chain for
the hop.</t>
<t>For a current actor <spanx style="verb">N</spanx>, let <spanx style="verb">chain_in</spanx> be the complete disclosed actor chain verified from
the inbound token. If <spanx style="verb">N</spanx> establishes a presenting actor for the inbound hop
under local policy, <spanx style="verb">N</spanx> MUST verify that the last actor in <spanx style="verb">chain_in</spanx> is that
same presenting actor. The exact actor-visible chain for the hop is:</t>

<figure><artwork type="text">visible_hop_N = chain_in + [N]
</artwork></figure>

<t>The Authorization Server MUST issue <spanx style="verb">EncodeVisibleChain(visible_hop_N)</spanx> as
the disclosed <spanx style="verb">act</spanx> in the returned token. A recipient MUST use the complete
disclosed actor chain for actor-chain authorization where actor-chain
authorization is required.</t>
<t>A claim that actor <spanx style="verb">V</spanx> participated in the chain MUST fail unless a valid step
proof for <spanx style="verb">V</spanx> can be produced and verified against the corresponding prior
commitment state and <spanx style="verb">acti</spanx>. If an actor is omitted from a later disclosed actor chain,
that omitted actor MAY prove prior participation by presenting an earlier token
showing the prior chain state and the corresponding commitment state and
verifiable step proof, or an immutable Authorization Server exchange record. A
denial of participation by actor <spanx style="verb">X</spanx> MUST fail if a valid step proof for <spanx style="verb">X</spanx> is
available and verifies.</t>
</section>

<section anchor="verified-subset" title="Verified Subset Disclosure Profile">
<t>The profile identifier for this profile is <spanx style="verb">verified-subset</spanx>.</t>
<t>This profile uses the verified evidence model and Subset Disclosure mode.
Accordingly, the Authorization Server MAY disclose a recipient-specific ordered
subsequence of the verified actor-visible chain for the hop, or MAY omit <spanx style="verb">act</spanx>
entirely according to local policy. If <spanx style="verb">act</spanx> is present, <spanx style="verb">VisibleChain(act)</spanx>
MUST be an ordered subsequence of the verified actor-visible chain for that
hop.</t>
<t>For a current actor <spanx style="verb">N</spanx>, let <spanx style="verb">chain_in</spanx> be the exact disclosed inbound visible
chain that <spanx style="verb">N</spanx> verified from the inbound token, or the empty chain if the
inbound token disclosed no <spanx style="verb">act</spanx>. If <spanx style="verb">chain_in</spanx> is non-empty and <spanx style="verb">N</spanx>
establishes a presenting actor for the inbound hop under local policy, <spanx style="verb">N</spanx> MUST
verify that its last actor is that same presenting actor. The exact verified
actor-visible chain for the hop is:</t>

<figure><artwork type="text">visible_hop_N = chain_in + [N]
</artwork></figure>

<t>The current actor <spanx style="verb">N</spanx> MUST append only itself and MUST NOT insert, delete, or
reorder prior actors within <spanx style="verb">chain_in</spanx>. The Authorization Server MUST verify
that the submitted actor-visible chain equals the exact inbound disclosed chain
previously verified by <spanx style="verb">N</spanx>, with <spanx style="verb">N</spanx> appended. An omitted inbound <spanx style="verb">act</spanx>
corresponds to the empty <spanx style="verb">chain_in</spanx> value.</t>
<t>When validating a returned token, <spanx style="verb">N</spanx> MUST verify, if the returned token
discloses <spanx style="verb">act</spanx>, that the disclosed actor chain is an ordered subsequence of
the exact verified actor-visible chain that <spanx style="verb">N</spanx> signed for that hop.</t>
<t>A recipient MAY use the verified disclosed actor chain for authorization
decisions, but MUST use only the disclosed subset and MUST treat undisclosed
prior actors as unavailable. If the token omits <spanx style="verb">act</spanx>, or discloses <spanx style="verb">act</spanx>
without the current actor, this specification provides no inline current-actor
disclosure for that hop.</t>
<t>Different recipients MAY receive different valid disclosed subsets derived from
the same verified actor-visible chain according to local disclosure policy.
That alone does not constitute an integrity failure. A malicious or compromised
Authorization Server could still attempt to issue a disclosed subset
inconsistent with the verified actor-visible chain. Such an inconsistency MUST
fail if the retained step proof for that hop or an immutable Authorization
Server exchange record is later checked.</t>
<t>An actor omitted from a disclosed chain MAY still prove prior participation by
presenting the corresponding step proof or immutable Authorization Server
exchange record for the verified actor-visible chain for the relevant hop.
When this profile omits <spanx style="verb">act</spanx>, or discloses only a narrow subset, later
reconstruction of the full accepted chain from step proofs alone is not
guaranteed. Such reconstruction can require retained Authorization Server
records or other authoritative workflow evidence.</t>
</section>

<section anchor="verified-actor-only" title="Verified Actor-Only Disclosure Profile">
<t>The profile identifier for this profile is <spanx style="verb">verified-actor-only</spanx>.</t>
<t>This profile uses the verified evidence model and Actor-Only Disclosure mode.
Accordingly, <spanx style="verb">act</spanx> is mandatory in every profiled access token and
<spanx style="verb">VisibleChain(act)</spanx> MUST equal <spanx style="verb">[current_actor]</spanx>. Prior actors MUST NOT be
disclosed inline in profiled access tokens under this profile. The returned profiled access
token MUST still carry the updated cumulative <spanx style="verb">actc</spanx> state.</t>
<t>For each hop under this profile, the current actor MUST construct the verified
actor-visible chain as the exact inbound disclosed actor chain that the actor
verified, with that actor appended. The step proof MUST carry that exact
actor-visible chain in its <spanx style="verb">act</spanx> member, even though the returned profiled access
token later discloses only the current actor.</t>
<t>Upon receipt of the returned token, the current actor MUST verify that the
returned <spanx style="verb">act</spanx> consists only of the current actor and that the returned
<spanx style="verb">actc</spanx> matches the accepted successor state for the verified hop.</t>
<t>Upon receipt of the token, the next recipient MUST perform recipient validation
and, for actor-chain authorization, use only the disclosed current actor and
local policy. If the
recipient establishes a presenting actor for the inbound token under local
policy, it MUST also confirm that the disclosed current actor matches that same
presenting actor.</t>
<t>If a returned profiled access token under this profile discloses any prior actor
inline, the current actor MUST reject it. If a recipient under this profile
attempts to infer hidden prior actors from omission, identifier structure, or
<spanx style="verb">actc</spanx> alone, that behavior is out of profile and MUST NOT be treated as a
conforming authorization decision.</t>
</section>
</section>

<section anchor="domain-branching" title="Same-Domain, Cross-Domain, and Branching Behavior">

<section anchor="same-cross-domain-hops" title="Same-Domain and Cross-Domain Hops">
<t>Within one trust domain, the current actor exchanges its inbound token at its
home Authorization Server, meaning the Authorization Server that validates the
prior chain state for that actor and issues the next profiled access token.</t>
<t>Across a trust boundary, if the next recipient does not trust the current
Authorization Server directly, the current actor performs a second token
exchange at the next domain's Authorization Server. That second exchange
preserves the already-established chain state and does not append the next
recipient.</t>
<t>The trust/evidence differences among profiles are summarized in
{{profile-summary}} and discussed further in {{trust-audit}}. The special
preserve-state cases for cross-domain re-issuance and Refresh-Exchange are
defined in {{preserve-state-exchanges}}.</t>
</section>

<section anchor="branching-non-goals" title="Branching and Non-Goals">
<t>Application logic may branch, fan out, and run in parallel. This document
standardizes one disclosed path per issued token, not a full call-graph language.
An Authorization Server MAY mint multiple accepted successor tokens from one
prior accepted state, each with its own <spanx style="verb">jti</spanx> and canonical <spanx style="verb">target_context</spanx>.
Such successor tokens MAY share the same <spanx style="verb">acti</spanx> and earlier workflow history.
Deployments that require strict linear continuation MAY instead enforce a local
single-successor policy under which the Authorization Server accepts at most one
successor from any accepted prior state. This base specification does not
define an interoperable on-the-wire signal for single-successor mode, sibling
invalidation, or proof that no parallel successor exists. This document does
not define merge semantics, sibling-discovery semantics, or inline
branch-selection semantics.</t>
<t>Accordingly, a valid token under this specification proves only one accepted
disclosed path for that token. It does not by itself prove branch uniqueness for
the workflow instance or prove that no other accepted sibling successor exists.</t>
<t>Post-facto reconstruction of branching is a forensic or legal-audit concern,
not a normal online authorization requirement. Retained Authorization Server
records, timestamps, commitment state, and causal links among presenting
actors, current actors, and subsequent actors can often reveal much of the
effective call graph, but this base specification alone does not guarantee a
complete standardized graph across all branches.</t>
<t>Each chain-extending issued token and each step proof binds exactly one logical
next-hop target context, as defined in {{target-context}}. Additional target
contexts or independently callable recipients require additional successor
tokens.</t>
<t>Repeated ActorID values within one workflow instance are permitted. A sequence
such as <spanx style="verb">[A,B,C,D,A,E]</spanx> denotes that actor <spanx style="verb">A</spanx> acted more than once in the
same workflow instance. Collecting all accepted hop evidence for one <spanx style="verb">acti</spanx>,
such as retained tokens, proofs, commitments, and exchange records, can
therefore reconstruct the accepted hop sequence, including repeated-actor
revisits.</t>
</section>
</section>

<section anchor="preserve-state-exchanges" title="Special Preserve-State Exchanges">
<t>A preserve-state exchange is a token exchange that preserves previously
accepted actor-chain state without appending a new actor. This document defines
two preserve-state exchanges: cross-domain re-issuance and Refresh-Exchange.
They are easiest to read after the profile flows.</t>
<t>A token-exchange request under this specification MUST NOT set both
<spanx style="verb">actor_chain_cross_domain=true</spanx> and <spanx style="verb">actor_chain_refresh=true</spanx>. These
parameters select different preserve-state processing modes. Setting both would
make subject handling, issuer handling, target-context narrowing, and returned-
token validation ambiguous. The Authorization Server MUST reject any request
that sets both rather than choosing one interpretation.</t>
<t>For preserve-state exchanges (<spanx style="verb">actor_chain_cross_domain=true</spanx> or
<spanx style="verb">actor_chain_refresh=true</spanx>), the Authorization Server MUST apply an explicit
preserve-state authorization policy. Unless deployment policy explicitly
selects transport-only renewal semantics, the Authorization Server MUST
re-evaluate current authorization for the authenticated requester and the
requested target context before issuing the preserved-state token. If current
policy does not permit issuance, the Authorization Server MUST reject the
request.</t>
<t>For any preserve-state exchange, the Authorization Server MUST establish the
ActorID represented by the preserved current-actor state and MUST verify that
the authenticated requester is that ActorID or is explicitly authorized to
preserve state for that ActorID. The represented ActorID MAY be established
from disclosed <spanx style="verb">act</spanx>, a confirmation or token-binding mechanism,
issuer-retained state, introspection, or a standardized presenter proof. If the
represented ActorID cannot be established, or if the requester is neither that
ActorID nor an authorized delegate, the Authorization Server MUST reject the
request with <spanx style="verb">invalid_grant</spanx>.</t>

<section anchor="cross-domain-reissuance" title="Cross-Domain Re-Issuance">

<section anchor="request-format" title="Request Format">
<t>If the next hop does not trust the current Authorization Server directly, the
current actor MUST perform a second token exchange at the next domain's
Authorization Server.</t>
<t>A cross-domain re-issuance request MUST include:</t>
<t>
<list style="symbols">
<t><spanx style="verb">grant_type=urn:ietf:params:oauth:grant-type:token-exchange</spanx>;</t>
<t><spanx style="verb">actor_chain_cross_domain=true</spanx>;</t>
<t><spanx style="verb">actor_chain_profile</spanx> set to the active profile identifier carried by the
inbound token;</t>
<t>the current inbound actor-chain token as the RFC 8693 <spanx style="verb">subject_token</spanx>;</t>
<t><spanx style="verb">subject_token_type=urn:ietf:params:oauth:token-type:access_token</spanx>; and</t>
<t>any requested OAuth targeting parameters (<spanx style="verb">audience</spanx>, <spanx style="verb">resource</spanx>, or both)
for the local target context to be minted by the re-issuing Authorization
Server.</t>
</list>
</t>
<t>The re-issuing Authorization Server MUST ensure that any locally minted target
context is semantically equivalent to, or narrower than, the target context
authorized by the inbound token according to local trust policy and audience
mapping rules. When the earlier chain-extending hop bound a <spanx style="verb">target_context</spanx>
richer than plain <spanx style="verb">aud</spanx>, the re-issuing Authorization Server MUST evaluate
that equivalence or narrowing against the exact retained or otherwise
recoverable prior <spanx style="verb">target_context</spanx>, not against <spanx style="verb">aud</spanx> alone. It MUST NOT issue
a local token whose target context is broader than, or semantically unrelated
to, the target context authorized by the inbound token. Unknown or unmapped
<spanx style="verb">target_context</spanx> members MUST be handled according to {{target-context}}; they
MUST NOT be ignored during cross-domain equivalence or narrowing.</t>
<t>A cross-domain re-issuance request MUST NOT append the chain and MUST NOT
submit <spanx style="verb">actor_chain_step_proof</spanx>, because this exchange preserves rather than
extends the accepted chain state. The <spanx style="verb">actor_chain_cross_domain</spanx> parameter is
the explicit wire signal that the request is for preservation and local
re-issuance rather than ordinary same-domain chain extension.</t>
</section>

<section anchor="preservation-rules" title="Preservation Rules">
<t>The cross-domain Authorization Server MUST:</t>
<t>
<list style="symbols">
<t>validate the inbound token signature and issuer trust according to local
policy;</t>
<t>validate the selected actor-chain profile;</t>
<t>validate the preserved disclosed-chain structure;</t>
<t>preserve <spanx style="verb">actp</spanx>;</t>
<t>preserve <spanx style="verb">acti</spanx>;</t>
<t>preserve <spanx style="verb">actc</spanx>, if present, exactly as verified;</t>
<t>enforce preserve-state authorization policy for this re-issuance request as
required above;</t>
<t>continue to represent the same established current ActorID; and</t>
<t>not append the next recipient.</t>
</list>
</t>
<t>Disclosed <spanx style="verb">act</spanx> handling during cross-domain re-issuance is profile-specific:</t>
<t>
<list style="symbols">
<t>for the Full Disclosure profiles and the Actor-Only profiles, the re-issuing
Authorization Server MUST preserve the disclosed <spanx style="verb">act</spanx> exactly, except that if
an inbound disclosed node omitted <spanx style="verb">iss</spanx>, the re-issuing Authorization Server
MAY materialize that <spanx style="verb">iss</spanx> explicitly only to preserve the same ActorID
semantics;</t>
<t>for the Subset Disclosure profiles, if the inbound token discloses <spanx style="verb">act</spanx>, the
re-issuing Authorization Server MAY preserve that disclosed <spanx style="verb">act</spanx> exactly,
disclose any ordered subsequence of that inbound disclosed actor chain, or omit <spanx style="verb">act</spanx>
entirely according to local policy; it MUST NOT introduce any actor not
present in the inbound disclosed actor chain, reorder actors, or use hidden retained
state to broaden disclosure; when a returned disclosed node would otherwise
rely on inherited issuer context, the re-issuing Authorization Server MAY
materialize the corresponding <spanx style="verb">iss</spanx> explicitly only to preserve the same
ActorID semantics; and</t>
<t>if the inbound token omits <spanx style="verb">act</spanx>, the re-issuing Authorization Server MUST
NOT synthesize a disclosed <spanx style="verb">act</spanx> from hidden retained state.</t>
</list>
</t>
</section>

<section anchor="cross-domain-subject" title="Subject Handling">
<t>For top-level <spanx style="verb">sub</spanx>, the re-issuing Authorization Server SHOULD preserve the
exact inbound value when doing so preserves the same underlying subject
semantics and does not broaden disclosure. If exact preservation would change
subject semantics under the new issuer namespace or would disclose more than the
inbound token disclosed, the re-issuing Authorization Server MAY translate
<spanx style="verb">sub</spanx> into a local alias that denotes the same underlying subject and MUST
retain an audit binding between the old and new subject representations. If the
re-issuing Authorization Server cannot establish same-subject semantic
continuity without broader disclosure, it MUST reject the request. Once such a
local alias is accepted in cross-domain re-issuance, that returned <spanx style="verb">sub</spanx> value
becomes the preserved workflow-subject representation for later same-domain
validation within the new issuer domain.</t>
<t>This specification cryptographically binds same-domain workflow-subject
continuity through verified step proofs. It does not cryptographically bind
cross-domain <spanx style="verb">sub</spanx> alias continuity in preserved <spanx style="verb">actc</spanx>. Accordingly,
preserved <spanx style="verb">actc</spanx> continuity across cross-domain re-issuance MUST NOT be
interpreted as cryptographic proof that the pre-translation and post-
translation <spanx style="verb">sub</spanx> values are bound by this specification to the same subject
representation. In this version, cross-domain subject-alias continuity remains
a matter of Authorization-Server policy and retained audit evidence.</t>
<t>Future companion specifications MAY define privacy-preserving Authorization
Server-to-Authorization Server transfer of additional hidden workflow state.
Such mechanisms are outside this base specification. This document's cross-
domain rules preserve only the disclosed state allowed by policy together with
any preserved <spanx style="verb">actc</spanx> state defined here.</t>
</section>

<section anchor="actorid-namespace-handling" title="ActorID Namespace Handling">
<t>Each disclosed <spanx style="verb">act</spanx> entry uses ActorID semantics over (<spanx style="verb">iss</spanx>, <spanx style="verb">sub</spanx>). If an
inbound <spanx style="verb">act</spanx> node omitted <spanx style="verb">iss</spanx>, the re-issuing Authorization Server MUST
preserve the same ActorID semantics by emitting an explicit <spanx style="verb">iss</spanx> equal to the
inbound token's issuer together with the same actor <spanx style="verb">sub</spanx>, rather than relying
on the new local token issuer as an implicit namespace. Internal canonicalization
for proof, comparison, and ordered-subsequence evaluation in this document
therefore always uses fully materialized ActorID pairs.</t>
<t>The cross-domain Authorization Server MAY mint a new local <spanx style="verb">jti</spanx>, apply a new
local expiry, change token format or envelope, and add local trust or policy
claims. If cross-domain re-issuance narrows or locally rewrites the target
context, retained step proofs and preserved <spanx style="verb">actc</spanx> continue to reflect the
target context that was bound during the original chain-extending hop, not the
narrower or rewritten token audience issued by the re-issuing Authorization Server.</t>
<t>Accordingly, when such narrowing or rewriting occurs, the current re-issued
token's audience or other local target representation MUST NOT be interpreted
as if it were itself the target context cryptographically bound by the earlier
step proof or by preserved <spanx style="verb">actc</spanx>. In this case, the re-issued token carries a
current presentation context for local use, while the preserved proof-bound
context remains that of the original chain-extending hop.</t>
<t>A recipient or current actor in the new domain that trusts the re-issuing
Authorization Server MAY rely on that enclosing token signature as attestation
that any preserved foreign <spanx style="verb">actc</spanx> was validated and carried forward unchanged.
Such a recipient need not independently validate a foreign Authorization
Server's JWS signature on the preserved <spanx style="verb">actc</spanx> unless local policy or audit
requires it.</t>
<t>The base specification does not define a portable cryptographic lineage field
that identifies the exact inbound token instance from which a returned cross-
domain re-issued token was derived. Accordingly, exact input-token provenance
for a re-issued token remains a matter of Authorization-Server records or of a
future companion specification that defines such a lineage field.</t>
</section>

<section anchor="returned-token-validation-1" title="Returned-Token Validation">
<t>When validating a token returned by cross-domain re-issuance, the current actor
does not recompute a new commitment object from a new step proof. Instead, it
MUST verify the token signature and MUST verify that preserved chain-state
fields, including <spanx style="verb">actp</spanx>, <spanx style="verb">acti</spanx>, and <spanx style="verb">actc</spanx>, preserve the same accepted chain
state as the inbound token except where this specification explicitly permits
cross-domain re-issuance changes such as local <spanx style="verb">sub</spanx> aliasing under the
semantic-equivalence rule, local <spanx style="verb">jti</spanx>, local <spanx style="verb">exp</spanx>, token format or envelope,
approved local trust and policy claims, or explicit <spanx style="verb">iss</spanx> materialization in
<spanx style="verb">act</spanx> solely to preserve the same ActorID semantics when an inbound node had
omitted <spanx style="verb">iss</spanx>. For Full Disclosure and Actor-Only profiles, any returned
disclosed <spanx style="verb">act</spanx> MUST preserve the inbound disclosed <spanx style="verb">act</spanx> exactly except for
such permitted <spanx style="verb">iss</spanx> materialization. For Subset Disclosure profiles, any
returned disclosed <spanx style="verb">act</spanx>, if present, MUST be an ordered subsequence of the
inbound disclosed actor chain and MUST NOT introduce any actor not disclosed in
the inbound token. The returned token target authority MUST be identical to or
narrower than the prior accepted target context, subject to the cross-domain
mapping rules in {{target-context}} and {{cross-domain-reissuance}}.</t>
</section>
</section>

<section anchor="refresh-exchange" title="Refresh-Exchange">
<t>A current actor MAY use token exchange to refresh a short-lived transport token
without appending the actor chain or regenerating a step proof.</t>

<section anchor="request-format-1" title="Request Format">
<t>A Refresh-Exchange request MUST include:</t>
<t>
<list style="symbols">
<t><spanx style="verb">grant_type=urn:ietf:params:oauth:grant-type:token-exchange</spanx>;</t>
<t><spanx style="verb">actor_chain_refresh=true</spanx>;</t>
<t><spanx style="verb">actor_chain_profile</spanx> set to the active profile identifier carried by the
inbound token;</t>
<t>the current inbound actor-chain token as the RFC 8693 <spanx style="verb">subject_token</spanx>;</t>
<t><spanx style="verb">subject_token_type=urn:ietf:params:oauth:token-type:access_token</spanx>;</t>
<t>the authenticated requester that is continuing that accepted chain state as
the represented current ActorID or as an authorized delegate; and</t>
<t>any requested OAuth targeting parameters (<spanx style="verb">audience</spanx>, <spanx style="verb">resource</spanx>, or both).
If omitted, the requested target context is the same as the inbound token's
target context as represented by <spanx style="verb">aud</spanx> and any locally retained
target-selection context associated with that accepted token state.</t>
</list>
</t>
<t>A Refresh-Exchange request MUST NOT include <spanx style="verb">actor_chain_step_proof</spanx>, because
Refresh-Exchange preserves rather than extends the accepted chain state.</t>
<t>A Refresh-Exchange request MUST NOT broaden the active profile, represented
current ActorID, disclosed actor chain state visible to the current actor,
commitment state, or target context. The requested target context MUST be
identical to, or a locally authorized narrowing of, the target context already
represented by the inbound token and any associated retained target-selection
state according to local policy. Such narrowing MUST preserve the same
recipient identity; it MUST NOT retarget the token to an audience or resource server not
already present in the inbound token's canonical <spanx style="verb">target_context</spanx>.</t>
</section>

<section anchor="processing-rules" title="Processing Rules">
<t>When processing Refresh-Exchange, the Authorization Server MUST:</t>
<t>
<list style="symbols">
<t>validate the inbound token and establish the represented current ActorID as
required for preserve-state exchanges;</t>
<t>verify that the requested profile identifier exactly matches the inbound
token's <spanx style="verb">actp</spanx>;</t>
<t>verify intended-recipient semantics as applicable;</t>
<t>enforce preserve-state authorization policy for this refresh request as
required above;</t>
<t>verify that the request does not append the chain, alter preserved chain
state, broaden target context, or change recipient identity; and</t>
<t>issue a replacement token with a new <spanx style="verb">jti</spanx> and refreshed <spanx style="verb">exp</spanx>.</t>
</list>
</t>
<t>For Refresh-Exchange, the Authorization Server MUST preserve <spanx style="verb">acti</spanx>,
<spanx style="verb">actp</spanx>, <spanx style="verb">sub</spanx>, <spanx style="verb">act</spanx>, if present, and <spanx style="verb">actc</spanx>, if present,
exactly as verified for the current actor. A new step proof MUST NOT be
required, and a new commitment object MUST NOT be created. If Refresh-Exchange
narrows the target context, retained step proofs and preserved <spanx style="verb">actc</spanx> continue
to reflect the target context that was bound during the original
chain-extending hop, not the narrower refreshed target context.</t>
<t>This specification does not define or require any particular token-binding,
presenter-binding, or key-transition mechanism for Refresh-Exchange. If a
deployment changes such transport or authentication properties during refresh,
that handling is governed by local policy and any companion specifications
rather than by this document. Historical step proofs remain bound to the keys
used when those proofs were created and MUST be verified against those
historical bindings, not against later local authentication material.</t>
<t>A recipient or coordinating component MUST treat a token obtained by
Refresh-Exchange as representing the same accepted chain state as the inbound
token from which it was refreshed. If local policy records a presenter-binding
or key-transition event, later verifiers rely on those local records or other
retained evidence for that event itself.</t>
</section>

<section anchor="returned-token-validation-1-1" title="Returned-Token Validation">
<t>When validating a token returned by Refresh-Exchange, the current actor does
not recompute a new commitment object from a new step proof. Instead, it MUST
verify the token signature and MUST verify that preserved chain-state fields,
including <spanx style="verb">actp</spanx>, <spanx style="verb">acti</spanx>, <spanx style="verb">sub</spanx>, <spanx style="verb">act</spanx>, and <spanx style="verb">actc</spanx>, are unchanged from the
inbound token except where this specification explicitly permits refresh-
specific changes such as <spanx style="verb">jti</spanx>, <spanx style="verb">exp</spanx>, or locally managed transport metadata.</t>
</section>
</section>
</section>

<section anchor="hop-ack" title="Optional Receiver Acknowledgment Extension">
<t>A recipient MAY produce a receiver acknowledgment artifact, called <spanx style="verb">hop_ack</spanx>,
for an inbound actor-chain token. This OPTIONAL extension does not alter chain
progression semantics.</t>
<t>A valid <spanx style="verb">hop_ack</spanx> provides signed evidence that the recipient accepted
responsibility for the identified hop, bound to the actor-chain identifier, the identified inbound hop
state, recipient, target context, and the acknowledged inbound token instance
via <spanx style="verb">inbound_jti</spanx>. For verified profiles, the stronger workflow-level
correlation anchors are <spanx style="verb">acti</spanx> together with the inbound commitment digest for
that hop; <spanx style="verb">inbound_jti</spanx> remains the token-instance trace field. If the
deployment establishes a presenter ActorID for the acknowledged hop under local
policy, <spanx style="verb">hop_ack</spanx> MAY additionally bind that presenter. For declared profiles,
the inbound hop state is the validated disclosed <spanx style="verb">act</spanx> from the inbound token
when that profile disclosed <spanx style="verb">act</spanx> on the acknowledged hop. For verified profiles,
that inbound hop state is the verified inbound commitment digest extracted from
the inbound token's <spanx style="verb">actc.curr</spanx>.</t>
<t>A recipient can issue a valid <spanx style="verb">hop_ack</spanx> only if it can either deterministically
derive or receive the exact canonical <spanx style="verb">target_context</spanx> value for the
acknowledged hop. When <spanx style="verb">target_context</spanx> extends beyond plain <spanx style="verb">aud</spanx>, the caller
or a coordinating component MUST communicate that exact canonical JSON value to
the recipient by an integrity-protected application mechanism before expecting
a matching <spanx style="verb">hop_ack</spanx>.</t>
<t>A <spanx style="verb">hop_ack</spanx> is ordinarily returned to the presenting actor or to a coordinating
component acting for that hop. It MAY later be archived, forwarded, or made
available to an audit service or other authorized dispute-resolution component
under local policy.</t>
<t><spanx style="verb">hop_ack</spanx> MUST NOT by itself append the recipient to the actor chain.</t>
<t>A recipient MUST NOT emit <spanx style="verb">hop_ack</spanx> with status <spanx style="verb">accepted</spanx> until it has either:</t>
<t>
<list style="symbols">
<t>completed the requested operation; or</t>
<t>durably recorded sufficient state to recover, retry, or otherwise honor the
accepted request according to local reliability policy.</t>
</list>
</t>
<t>A deployment MAY require <spanx style="verb">hop_ack</spanx> for selected hops, including terminal hops.
Deployments that require evidence of terminal acceptance SHOULD require a valid
<spanx style="verb">hop_ack</spanx> for those terminal hops.
When <spanx style="verb">hop_ack</spanx> is required by policy, the calling actor and any coordinating
component MUST treat that hop as not accepted unless a valid <spanx style="verb">hop_ack</spanx> is
received and verified.</t>
<t>Deployments that rely on <spanx style="verb">hop_ack</spanx> for later audit or dispute resolution SHOULD
ensure that the caller side and the recipient side each retain the artifact, or
records sufficient to validate and contextualize it, for the applicable audit
period. For example, a downstream agent might bill the presenting agent only
for work that it accepted. In that case, the recipient-signed <spanx style="verb">hop_ack</spanx> helps
both sides later prove exactly which hop, target context, and accepted inbound
artifact are in scope.</t>
<t><spanx style="verb">hop_ack</spanx> does not by itself prove successful completion or correctness of the
requested operation.</t>
<t>Recipients are not required to issue <spanx style="verb">hop_ack</spanx> for rejected, malformed,
abusive, unauthorized, or rate-limited requests. Absence of <spanx style="verb">hop_ack</spanx> prevents proof of acceptance under this extension.</t>
<t>When a deployment needs <spanx style="verb">hop_ack</spanx> to acknowledge multiple distinct operations
performed under the same inbound token and the same nominal target, it MUST
include a request-unique <spanx style="verb">request_id</spanx> inside <spanx style="verb">target_context</spanx>.</t>
<t>The acknowledgment payload MUST include at least:</t>
<t>
<list style="symbols">
<t><spanx style="verb">ctx</spanx> = <spanx style="verb">actor-chain-hop-ack-v1</spanx>;</t>
<t><spanx style="verb">acti</spanx>;</t>
<t><spanx style="verb">actp</spanx>;</t>
<t><spanx style="verb">jti</spanx>, a unique identifier for the <spanx style="verb">hop_ack</spanx> JWT itself;</t>
<t><spanx style="verb">inbound_jti</spanx>, copied from the acknowledged inbound token;</t>
<t>OPTIONAL <spanx style="verb">presenter</spanx>, the presenting actor's ActorID when established under
local policy for that hop;</t>
<t><spanx style="verb">recipient</spanx>, the acknowledging recipient's ActorID;</t>
<t><spanx style="verb">target_context</spanx>;</t>
<t><spanx style="verb">iat</spanx>, a JWT NumericDate recording when the acknowledgment was issued;</t>
<t><spanx style="verb">exp</spanx>, a short-lived JWT NumericDate;</t>
<t>for declared profiles, OPTIONAL inbound <spanx style="verb">act</spanx> when that profile disclosed
<spanx style="verb">act</spanx> on the acknowledged hop;</t>
<t>for verified profiles, <spanx style="verb">inbound_commitment</spanx>, the verified inbound
commitment digest copied directly from the inbound token's <spanx style="verb">actc.curr</spanx>; and</t>
<t><spanx style="verb">ack</spanx>, whose value MUST be <spanx style="verb">accepted</spanx>.</t>
</list>
</t>
<t>A <spanx style="verb">hop_ack</spanx> MUST be signed by the recipient using JWS.</t>

<section anchor="receiver-acknowledgment-validation" title="Receiver Acknowledgment Validation">
<t>A caller or coordinating component that receives <spanx style="verb">hop_ack</spanx> and relies on it for
acceptance processing MUST verify at least:</t>
<t>
<list style="symbols">
<t>the JWS signature using the recipient identity and keying material expected by
local trust policy;</t>
<t>the JWS protected header contains <spanx style="verb">typ=act-hop-ack+jwt</spanx>;</t>
<t><spanx style="verb">ctx=actor-chain-hop-ack-v1</spanx>;</t>
<t><spanx style="verb">acti</spanx> equals the actor-chain identifier of the inbound token for which
acknowledgment is being evaluated;</t>
<t><spanx style="verb">actp</spanx> equals the active profile of that inbound token;</t>
<t><spanx style="verb">jti</spanx> is unique for the acknowledgment artifact under local replay policy;</t>
<t><spanx style="verb">inbound_jti</spanx> equals the <spanx style="verb">jti</spanx> of the inbound token that was actually sent to
the recipient;</t>
<t>if <spanx style="verb">presenter</spanx> is present, it equals the presenting actor established under
local policy for the acknowledged hop;</t>
<t><spanx style="verb">recipient</spanx> equals the recipient from which acknowledgment is expected;</t>
<t><spanx style="verb">target_context</spanx> equals the exact canonical target context that was
requested, communicated, or deterministically derived for the acknowledged
hop;</t>
<t><spanx style="verb">iat</spanx> is present and acceptable under local clock policy;</t>
<t><spanx style="verb">exp</spanx> has not expired;</t>
<t>for declared profiles, if <spanx style="verb">act</spanx> is present, the carried <spanx style="verb">act</spanx> equals the
inbound <spanx style="verb">act</spanx> for the acknowledged hop and MUST NOT disclose more than that
acknowledged hop disclosed;</t>
<t>for verified profiles, <spanx style="verb">inbound_commitment</spanx> equals the verified inbound
commitment digest copied from the inbound token's <spanx style="verb">actc.curr</spanx>; and</t>
<t>the <spanx style="verb">ack</spanx> member is present and its value equals <spanx style="verb">accepted</spanx>.</t>
</list>
</t>
<t>When the inbound token being acknowledged was obtained by cross-domain
re-issuance or Refresh-Exchange, the <spanx style="verb">target_context</spanx> compared here is the
exact canonical value for that acknowledged presentation. Any preserved step
proofs and <spanx style="verb">actc</spanx> from an earlier chain-extending hop continue to reflect the
target context of that earlier hop, not a later locally rewritten audience,
unless those values are identical.</t>
</section>
</section>

<section anchor="canonicalization-commitment" title="Canonicalization and Commitment Processing">

<section anchor="canonicalization" title="Canonicalization">
<t>All profile-defined signed or hashed inputs MUST use a canonical serialization
defined by this specification.</t>
<t>In this version of the specification, <spanx style="verb">CanonicalEncode(x)</spanx> means JCS
<xref target="RFC8785"></xref> applied to the JSON value <spanx style="verb">x</spanx>.</t>
<t><spanx style="verb">Hash_halg(x)</spanx> denotes the raw hash output produced by applying the selected
commitment hash algorithm <spanx style="verb">halg</spanx> to the octet sequence <spanx style="verb">x</spanx>.</t>
<t><spanx style="verb">b64url(x)</spanx> denotes the base64url encoding of the octet sequence <spanx style="verb">x</spanx> without
trailing padding characters, as defined by <xref target="RFC7515"></xref> Appendix C.</t>
<t>Canonical profile-defined proof payloads MUST be serialized using JCS <xref target="RFC8785"></xref>.</t>
</section>

<section anchor="commitment-hash-algorithms" title="Commitment Hash Algorithms">
<t>Proof-bound profiles use a named hash algorithm for construction of
<spanx style="verb">actc</spanx>. Commitment hash algorithm identifiers are values from the IANA Named
Information Hash Algorithm Registry <xref target="RFC6920"></xref> <xref target="IANA.Hash.Algorithms"></xref>.</t>
<t>The following requirements apply:</t>
<t>
<list style="symbols">
<t><spanx style="verb">halg</spanx> MUST be a text string naming a hash algorithm from the IANA Named
Information Hash Algorithm Registry.</t>
<t>Implementations supporting verified profiles MUST implement <spanx style="verb">sha-256</spanx>.</t>
<t>Implementations SHOULD implement <spanx style="verb">sha-384</spanx>.</t>
<t>Every <spanx style="verb">actc</spanx> object and every verified profile bootstrap context MUST carry
an explicit <spanx style="verb">halg</spanx> value. Verifiers MUST NOT infer or substitute <spanx style="verb">halg</spanx> when
it is absent.</t>
<t>Verifiers MUST enforce a locally configured allow-list of acceptable
commitment hash algorithms and MUST NOT accept algorithm substitution based
solely on attacker-controlled inputs.</t>
<t>Hash algorithms with truncated outputs, including truncated <spanx style="verb">sha-256</spanx>
variants, MUST NOT be used with this specification.</t>
<t>Additional registry values MAY be used only if they are permitted by this
specification or a future Standards Track update to it, and verifiers MUST
reject locally deprecated or disallowed algorithms.</t>
<t>An Authorization Server MUST NOT initiate a new workflow using a locally
deprecated or disallowed algorithm. Whether an already-issued workflow using
such an algorithm may continue is a matter of local policy.</t>
</list>
</t>
</section>

<section anchor="commitment-function" title="Commitment Function">
<t>Proof-bound profiles use <spanx style="verb">actc</spanx> to bind each accepted hop to the
prior accepted state. The commitment hash algorithm is selected once for the
workflow by the issuing Authorization Server during bootstrap and remains fixed
for the lifetime of that workflow instance.</t>
<t>Each <spanx style="verb">actc</spanx> value is a signed commitment object whose payload
contains:</t>
<t>
<list style="symbols">
<t><spanx style="verb">ctx</spanx>: the context string <spanx style="verb">actor-chain-commitment-v1</spanx>. This context string
identifies the commitment payload version defined by the initial standardized
version of this specification. During Internet-Draft development before that
version is standardized, changes to the commitment payload can still be
incorporated into <spanx style="verb">-v1</spanx>. After the initial standardized version is published,
any revision that changes commitment payload membership, semantics, or
canonicalization in a non-compatible way MUST allocate a new version suffix.
Verifiers MUST reject commitments whose <spanx style="verb">ctx</spanx> value does not exactly match an
expected, implemented version;</t>
<t><spanx style="verb">iss</spanx>: the issuer identifier of the Authorization Server that signs this
commitment object;</t>
<t><spanx style="verb">acti</spanx>: the actor-chain identifier;</t>
<t><spanx style="verb">actp</spanx>: the active profile identifier;</t>
<t><spanx style="verb">halg</spanx>: the hash algorithm identifier;</t>
<t><spanx style="verb">prev</spanx>: the prior commitment digest, or the bootstrap <spanx style="verb">initial_chain_seed</spanx> at
workflow start;</t>
<t><spanx style="verb">step_hash</spanx>: <spanx style="verb">b64url(Hash_halg(step_proof_bytes))</spanx>; and</t>
<t><spanx style="verb">curr</spanx>: <spanx style="verb">b64url(Hash_halg(CanonicalEncode({ctx, iss, acti, actp, halg, prev, step_hash})))</spanx>.</t>
</list>
</t>
<t>The <spanx style="verb">curr</spanx> value MUST be computed over exactly the seven members <spanx style="verb">ctx</spanx>, <spanx style="verb">iss</spanx>,
<spanx style="verb">acti</spanx>, <spanx style="verb">actp</spanx>, <spanx style="verb">halg</spanx>, <spanx style="verb">prev</spanx>, and <spanx style="verb">step_hash</spanx>, excluding <spanx style="verb">curr</spanx> itself. The
resulting digest is then inserted as the transported <spanx style="verb">curr</spanx> member.</t>
<t>Let <spanx style="verb">prev_digest</spanx> denote the prior commitment-state digest for the step being
processed: at bootstrap it is the <spanx style="verb">initial_chain_seed</spanx>, and for later steps it
is the verified <spanx style="verb">curr</spanx> value extracted from the inbound <spanx style="verb">actc</spanx>.
For the JWT binding defined in this version, let <spanx style="verb">step_proof_bytes</spanx> denote the
ASCII bytes of the exact compact JWS string submitted as
<spanx style="verb">actor_chain_step_proof</spanx>.
Let <spanx style="verb">as_issuer_id</spanx> denote the issuer identifier that the Authorization Server
places into the commitment object's <spanx style="verb">iss</spanx> member, typically its issuer value.
The commitment hash therefore binds the transmitted step-proof artifact, not
merely its decoded payload.</t>
<t>Accordingly, <spanx style="verb">step_hash</spanx> and <spanx style="verb">curr</spanx> are commitments to the exact compact-JWS
proof bytes accepted for that hop. Two semantically equivalent decoded proofs
MAY therefore produce different commitment values when their compact-JWS bytes
differ.</t>
<t>When a profile-defined proof input refers to a prior
<spanx style="verb">actc</spanx>, the value incorporated into the proof input MUST be
that prior commitment's verified <spanx style="verb">curr</spanx> digest string, copied directly from the
validated <spanx style="verb">actc</spanx> payload, not the raw serialized commitment object.</t>
<t>The abstract function used throughout this document is therefore:</t>

<figure><artwork type="text">Commit_AS(as_issuer_id, acti, actp, prev_digest, step_proof_bytes, halg)
  = Authorization-Server-signed commitment object over payload {
      ctx,
      iss,
      acti,
      actp,
      halg,
      prev = prev_digest,
      step_hash = b64url(Hash_halg(step_proof_bytes)),
      curr = b64url(Hash_halg(CanonicalEncode({ctx, iss, acti, actp, halg, prev, step_hash})))
    }
</artwork></figure>

<t>The exact wire encoding of the signed commitment object is defined in {{jwt-jws-binding}}.</t>
<t>In calls to <spanx style="verb">Commit_AS</spanx>, the <spanx style="verb">iss</spanx> input is the issuer identifier of the
Authorization Server signing the new commitment object, and <spanx style="verb">acti</spanx> and <spanx style="verb">actp</spanx>
are the workflow and profile values being preserved for that workflow state.</t>
</section>
</section>

<section anchor="artifact-typing" title="Artifact Typing">

<section anchor="jwt-artifact-types" title="JWT Artifact Types">
<t>JWT-based artifacts defined by this specification MUST use explicit <spanx style="verb">typ</spanx>
values.</t>
<t>The following JWT <spanx style="verb">typ</spanx> values are defined:</t>
<t>
<list style="symbols">
<t><spanx style="verb">act-step-proof+jwt</spanx></t>
<t><spanx style="verb">act-commitment+jwt</spanx></t>
<t><spanx style="verb">act-hop-ack+jwt</spanx></t>
</list>
</t>
<t>Verifiers MUST enforce mutually exclusive validation rules based on artifact
type and MUST NOT accept one artifact type in place of another. They MUST verify
the expected JWT <spanx style="verb">typ</spanx>, exact <spanx style="verb">ctx</spanx> value where applicable, and
artifact-specific payload structure defined by the relevant binding section of
this specification.</t>
<t><spanx style="verb">typ</spanx> matching for profile-defined artifacts MUST be exact string equality,
without case folding, prefix matching, or media-type alias mapping. Missing or
unexpected <spanx style="verb">typ</spanx> values MUST cause rejection of the artifact for that
processing path.</t>
<t>If a JOSE protected header contains <spanx style="verb">crit</spanx>, verifiers MUST reject unless every
listed header parameter is understood and processed according to its
specification. This document defines no profile-specific use of unencoded JWS
payloads; profile-defined artifacts in this version therefore use the ordinary
base64url-encoded JWS payload form.</t>
</section>
</section>

<section anchor="security-enforcement" title="Common Security and Enforcement Requirements">
<t>This section collects enforcement requirements that all profiles rely on but
that need not be read before the main profile flows. Implementations still
MUST satisfy these requirements even when they are consulted later in a first
reading pass.</t>

<section anchor="jose-and-parser-hardening" title="JOSE and Parser Hardening">
<t>Implementations validating profiled access tokens, step proofs, <spanx style="verb">hop_ack</spanx>, and
<spanx style="verb">actc</spanx> MUST apply strict JOSE and JSON parsing behavior to avoid
implementation-differential acceptance.</t>
<t>At minimum, validators MUST:</t>
<t>
<list style="symbols">
<t>reject duplicate member names in any JSON object that contributes to
profile-defined validation, signature verification, canonicalization, or
commitment inputs;</t>
<t>reject malformed compact JWS syntax, including malformed base64url segments;</t>
<t>reject unsupported critical JOSE headers as defined in {{artifact-typing}} and
{{jwt-jws-binding}};</t>
<t>enforce exact expected claim types for profile-defined claims before
semantic processing (for example, <spanx style="verb">actp</spanx>, <spanx style="verb">acti</spanx>, <spanx style="verb">iss</spanx>, <spanx style="verb">sub</spanx>, and <spanx style="verb">jti</spanx>
as strings; <spanx style="verb">target_context</spanx> as an object where required by this
specification; and <spanx style="verb">actc</spanx> as a compact JWS string when required);</t>
<t>treat type mismatch as a profile-validation failure; and</t>
<t>for verified profiles, treat missing <spanx style="verb">actc</spanx> as a profile-validation failure.</t>
</list>
</t>
</section>

<section anchor="actor-authentication" title="Actor Authentication and Presenter Binding">
<t>This specification does not define or require any particular actor-
authentication, presenter-binding, or token-binding mechanism. Authorization
Servers and recipients MAY establish current-actor or presenting-actor identity
using any locally trusted method. Recipient authorization policy based on such
inputs is outside the scope of this specification.</t>
<t>When deployment policy depends on a specific presenter-binding or token-binding
mechanism class (for example mTLS-anchored or DPoP-anchored processing), that
mechanism class MUST be fixed by local policy or a companion profile and MUST
NOT be inferred from actor-chain artifacts alone.</t>
</section>

<section anchor="proof-keys" title="Actor and Recipient Proof Keys">
<t>For verified profiles and for <spanx style="verb">hop_ack</spanx>, any signature used as a
profile-defined proof MUST be generated with an asymmetric key whose
verification material is trusted under local policy for the actor or recipient
identity represented in that artifact.</t>
<t>For a verified profile step proof, the ActorID represented in the proof and the
key used to sign the proof MUST be bound to the same actor identity under local
trust policy. The Authorization Server MUST verify the proof using trusted
verification material for that actor identity before accepting the proof.</t>
<t>For <spanx style="verb">hop_ack</spanx>, the recipient ActorID and the key used to sign the acknowledgment
MUST likewise be bound to the same recipient identity under local trust policy.
If <spanx style="verb">presenter</spanx> is included in a <spanx style="verb">hop_ack</spanx>, that value MUST be established under
local policy and MUST NOT expand disclosure beyond the selected profile.</t>
<t>Shared client secrets MUST NOT be the sole basis for independently verifiable
step proofs or receiver acknowledgments.</t>
<t>Deployments that rely on later verification of archived step proofs or
acknowledgments MUST retain, or be able to recover, the verification material
and identity-binding records needed to validate those signatures during the
applicable audit period. Deployments that claim verified-profile auditability
beyond Authorization-Server-only trust SHOULD also retain, or be able to
recover, the exact compact JWS step-proof artifacts and their associated
workflow context for the applicable audit period, because commitment digests
alone do not prove which actor signed which hop.</t>
</section>

<section anchor="intended-recipient" title="Intended Recipient Validation">
<t>When a current actor submits an inbound token as a <spanx style="verb">subject_token</spanx> in token
exchange, the accepting Authorization Server MUST normally verify that the
authenticated current actor was an intended recipient of that inbound token
according to local audience, resource, or equivalent validation rules. For
<spanx style="verb">actor_chain_refresh=true</spanx> and <spanx style="verb">actor_chain_cross_domain=true</spanx>, this intended-
recipient check does not apply in the same way because the exchange preserves
rather than extends previously accepted chain state. The Authorization Server
MUST instead apply the preserve-state actor-establishment rule in
{{preserve-state-exchanges}}.</t>
<t>Possession of an inbound token alone is insufficient.</t>
</section>

<section anchor="replay-freshness" title="Replay and Freshness">
<t>Recipients and Authorization Servers MUST enforce replay and freshness checks on
inbound tokens according to local policy.</t>
<t>For profiles that use actor-signed step proofs, the accepting Authorization
Server:</t>
<t>
<list style="symbols">
<t>MUST detect replay of a previously accepted step proof within its
replay-retention window;</t>
<t>MUST treat an exact replay of a previously accepted compact-JWS step proof
for the same authenticated actor and same prior state as an idempotent retry,
not as a distinct successor;</t>
<t>MUST, for such an idempotent retry, return the previously accepted
successor state, or an equivalent token representing that same accepted
successor state, while any required retry record is retained;</t>
<t>SHOULD, during that retry-retention window, retain the exact previously
issued response or otherwise ensure that a retried response carries the same
accepted chain state, because recomputing with probabilistic signatures can
change wire bytes even when the decoded accepted state is equivalent; and</t>
<t>MUST reject a different attempted successor for the same
<spanx style="verb">(acti, prior_state, target_context)</spanx> tuple unless local policy explicitly
authorizes replacement or supersession; this base specification does not
standardize how multiple accepted successors that share earlier history are
correlated or later merged. Deployments that expect multiple distinct
same-target successors SHOULD distinguish them by including a unique
<spanx style="verb">request_id</spanx> in <spanx style="verb">target_context</spanx>.</t>
</list>
</t>
</section>

<section anchor="chain-depth-limits" title="Chain Depth Limits">
<t>Authorization Servers MUST enforce a configurable maximum chain depth for
accepted workflow state. A RECOMMENDED default is 10 entries.</t>
<t>A token exchange that would cause the accepted chain depth to exceed the
Authorization Server's configured maximum MUST be rejected with <spanx style="verb">invalid_grant</spanx>.</t>
<t>Recipients MAY enforce stricter local limits on disclosed actor-chain depth. A
recipient that receives an inbound token whose disclosed actor chain exceeds
its configured maximum MUST treat that condition as a profile-validation
failure.</t>
<t>Error responses and logs MUST NOT disclose actor identities beyond what the
selected profile permits.</t>
</section>
</section>

<section anchor="as-metadata" title="Authorization Server Metadata">
<t>Actor-chain capability discovery uses OAuth 2.0 Authorization Server Metadata
<xref target="RFC8414"></xref>. This specification does not define a new discovery endpoint.
Clients retrieve Authorization Server metadata from the RFC 8414 well-known
metadata endpoint derived from the issuer, verify that the returned <spanx style="verb">issuer</spanx>
matches the configured issuer, and then process the actor-chain-specific
metadata values defined below.</t>
<t>An Authorization Server supporting this specification SHOULD publish metadata
describing its supported actor-chain capabilities.</t>
<t>This specification defines the following Authorization Server metadata values:</t>
<t>
<list style="symbols">
<t><spanx style="verb">actor_chain_bootstrap_endpoint</spanx>:
URL of the Authorization Server endpoint used to mint verified profile
bootstrap context for initial actors;</t>
<t><spanx style="verb">actor_chain_profiles_supported</spanx>:
array of supported actor-chain profile identifiers. Each value MUST be the
exact identifier string used both as the <spanx style="verb">actor_chain_profile</spanx> token request
parameter value and as the <spanx style="verb">actp</spanx> token claim value;</t>
<t><spanx style="verb">actor_chain_commitment_hashes_supported</spanx>:
array of supported commitment hash algorithm identifiers;</t>
<t><spanx style="verb">actor_chain_receiver_ack_supported</spanx>:
boolean indicating whether the Authorization Server supports processing and
policy for <spanx style="verb">hop_ack</spanx>;</t>
<t><spanx style="verb">actor_chain_refresh_supported</spanx>:
boolean indicating whether the Authorization Server supports Refresh-Exchange
processing under this specification; and</t>
<t><spanx style="verb">actor_chain_cross_domain_supported</spanx>:
boolean indicating whether the Authorization Server supports cross-domain
re-issuance processing under this specification.</t>
</list>
</t>
<t>Client behavior is:</t>
<t>
<list style="numbers">
<t>obtain or configure the Authorization Server issuer;</t>
<t>fetch RFC 8414 metadata from the corresponding well-known endpoint;</t>
<t>verify the returned <spanx style="verb">issuer</spanx> exactly matches the configured issuer;</t>
<t>check <spanx style="verb">actor_chain_profiles_supported</spanx> and any other needed actor-chain
capability fields; and</t>
<t>fail closed if the required profile or capability is absent, unless local
policy explicitly allows fallback to plain RFC 8693 behavior.</t>
</list>
</t>
<t>If omitted, clients MUST NOT assume support for any actor-chain profile beyond
out-of-band agreement.</t>
</section>

<section anchor="error-handling" title="Error Handling">
<t>Token exchange errors in this specification build on OAuth 2.0 and OAuth 2.0
Token Exchange.</t>
<t>An Authorization Server processing a token exchange request applies the
following mapping:</t>
<texttable>
<ttcol>OAuth error code</ttcol>
<ttcol>Actor-chain failure class</ttcol>


<c>invalid_request</c>
<c>Malformed request, unsupported profile binding, conflicting preserve-state parameters, or structurally malformed profile-defined input.</c>

<c>invalid_target</c>
<c>Requested audience, recipient, resource, or canonical target context is not permitted or not supported.</c>

<c>invalid_grant</c>
<c>Subject-token, continuity, proof, replay, disclosure, or preserve-state validation failed.</c>

</texttable><t><spanx style="verb">invalid_request</spanx> includes malformed or missing profile-defined parameters,
malformed bootstrap context, malformed ActorID values, malformed commitment
objects, unsupported profile bindings, both <spanx style="verb">actor_chain_cross_domain=true</spanx> and
<spanx style="verb">actor_chain_refresh=true</spanx>, and structurally malformed inline <spanx style="verb">act</spanx>.</t>
<t><spanx style="verb">invalid_target</spanx> includes cases in which the requested audience, canonical
target context, or recipient is not permitted or not supported, a
Refresh-Exchange attempts to retarget to a different recipient, no unique
logical chain-extending recipient can be determined, or a cross-domain
<spanx style="verb">target_context</spanx> member cannot be preserved or mapped safely.</t>
<t><spanx style="verb">invalid_grant</spanx> includes failures such as:</t>
<t>
<list style="symbols">
<t>failed <spanx style="verb">subject_token</spanx> validation;</t>
<t>intended-recipient failure;</t>
<t>actor-chain continuity failure at token exchange;</t>
<t>replay or freshness failure;</t>
<t><spanx style="verb">actor_chain_step_proof</spanx> verification failure;</t>
<t>bootstrap-context reuse that is neither an idempotent retry nor an authorized
distinct initial successor;</t>
<t>required inline <spanx style="verb">act</spanx> disclosure absent;</t>
<t>profile-disclosure rule failure;</t>
<t>verified step-proof target context inconsistent with the accepted target
context;</t>
<t>issued token target authority would be broader than the proof-bound target
context;</t>
<t>preserve-state authorization policy failure;</t>
<t>represented current actor for preserve-state exchange could not be
established or was not authorized for the requester;</t>
<t>submitted prior state inconsistent with the claimed profile state; and</t>
<t>actor-chain depth limit exceeded.</t>
</list>
</t>
<t>Implementations SHOULD provide additional machine-readable or human-readable
diagnostics that distinguish actor-chain-specific failure classes that map to
the same base OAuth error code. Such diagnostics are for troubleshooting and
interoperability support only and MUST NOT alter the base OAuth error value
required by this specification. Examples include diagnostics indicating replay
detection, intended-recipient failure, step-proof verification failure,
profile-disclosure failure, or preserved-state inconsistency.</t>
<t>Recipients and Authorization Servers MUST return protocol-appropriate error
signals for authentication, authorization, profile-validation, and continuity
failures. When the selected profile requires inline <spanx style="verb">act</spanx> disclosure for an
artifact, omission of <spanx style="verb">act</spanx>, or presence of an <spanx style="verb">act</spanx> value that fails that
profile's disclosure rules, MUST be treated as a profile-validation failure.</t>
<t>In HTTP deployments, this typically maps to 400-series status codes and
OAuth-appropriate error values. In non-HTTP deployments, functionally
equivalent protocol-native error signaling MUST be used.</t>
<t>Error responses and logs MUST NOT disclose undisclosed prior actors, full step
proofs, canonical proof inputs, or other sensitive proof material unless the
deployment explicitly requires such disclosure for diagnostics.</t>
<t>When deployments provide <spanx style="verb">error_description</spanx>, <spanx style="verb">error_uri</spanx>, logs, or similar
diagnostics for actor-chain failures, they SHOULD distinguish among underlying
failure causes where doing so does not disclose hidden actors, full proofs, or
other sensitive material.</t>
</section>

<section anchor="jwt-jws-binding" title="JWT / JWS Binding">
<t>This section defines the JWT and JWS wire representation for profile-defined
ActorID values, disclosed <spanx style="verb">act</spanx> structures, step proofs, receiver
acknowledgments, and commitment objects.</t>

<section anchor="actorid-in-jwt" title="ActorID in JWT">
<t>An ActorID is a JSON object with exactly two members:</t>
<t>
<list style="symbols">
<t><spanx style="verb">iss</spanx>: a string containing the issuer identifier; and</t>
<t><spanx style="verb">sub</spanx>: a string containing the subject identifier.</t>
</list>
</t>
<t>The object MUST be serialized using JCS <xref target="RFC8785"></xref> whenever it is included in
profile-defined proof or commitment inputs.</t>
<t>When <spanx style="verb">actp</spanx> is present, the disclosed <spanx style="verb">act</spanx> structure in a JWT is an
ActorChainNode. Newly issued profile-defined nodes MUST contain explicit
<spanx style="verb">iss</spanx> and <spanx style="verb">sub</spanx>, and MAY contain a nested <spanx style="verb">act</spanx> member whose value is the
immediately prior disclosed ActorChainNode. Validators MUST also be able to
normalize a validated inbound node that omits <spanx style="verb">iss</spanx> and inherits the enclosing
issuer context.</t>
</section>

<section anchor="jwt-step-proof" title="Step Proof in JWT">
<t>The <spanx style="verb">actor_chain_step_proof</spanx> token request parameter value MUST be a compact JWS
string <xref target="RFC7515"></xref>. The JWS protected header MUST contain <spanx style="verb">typ=act-step-proof+jwt</spanx>. The
JWS payload MUST be the UTF-8 encoding of a JCS-serialized JSON object.</t>
<t>Verifiers MUST require exact <spanx style="verb">typ=act-step-proof+jwt</spanx> matching for this
artifact class. The <spanx style="verb">alg</spanx> header parameter MUST be present and MUST identify
an asymmetric signature algorithm accepted by local policy. Algorithm
identifiers are defined by JSON Web Algorithms (JWA) <xref target="RFC7518"></xref> or by an
applicable extension. <spanx style="verb">alg=none</spanx> MUST NOT be accepted. If <spanx style="verb">crit</spanx> is present,
verifiers MUST reject unless every listed parameter is understood and
processed.</t>
<t>For all profiles in the verified branch, the payload MUST contain:</t>
<t>
<list style="symbols">
<t><spanx style="verb">ctx</spanx>;</t>
<t><spanx style="verb">acti</spanx>;</t>
<t><spanx style="verb">prev</spanx>;</t>
<t><spanx style="verb">sub</spanx>;</t>
<t><spanx style="verb">target_context</spanx>;</t>
<t><spanx style="verb">act</spanx>; and</t>
<t><spanx style="verb">iat</spanx>, a JWT NumericDate recording when the step proof was signed.</t>
</list>
</t>
<t>When this payload is used as commitment input through <spanx style="verb">step_hash</spanx>, the
<spanx style="verb">step_proof_bytes</spanx> value is the ASCII byte sequence of the exact compact JWS
serialization of the proof artifact.</t>
<t>The <spanx style="verb">ctx</spanx> member value MUST equal the profile-specific step-proof
domain-separation string <spanx style="verb">ds(profile)</spanx> defined in
{{verified-common-processing}}. The <spanx style="verb">prev</spanx> member MUST be the base64url string
value of the prior commitment digest or bootstrap seed, copied directly from
the verified inbound <spanx style="verb">actc.curr</spanx> or bootstrap response, respectively. The
<spanx style="verb">sub</spanx> member MUST be the exact preserved workflow <spanx style="verb">sub</spanx> string for the
same-domain hop being extended. The <spanx style="verb">act</spanx> member MUST be an ActorChainNode
structure and denotes the profile-defined verified actor-visible chain for the
hop. For the Verified Subset Disclosure profile, the returned profiled access
token <spanx style="verb">act</spanx> MAY disclose any ordered subsequence of that verified chain that
disclosure policy permits, or MAY omit <spanx style="verb">act</spanx> entirely when that profile and
local policy permit omission. For the Verified Actor-Only Disclosure profile,
the returned profiled access token <spanx style="verb">act</spanx> MUST contain only the outermost
current actor. The <spanx style="verb">target_context</spanx> member MUST conform to the representation
defined in {{target-context}}. The <spanx style="verb">iat</spanx> value MUST be a JWT NumericDate.</t>
<t>The accepting Authorization Server MUST reject a step proof whose <spanx style="verb">iat</spanx> is
outside a locally configured acceptance window relative to the Authorization
Server's clock. The acceptance window SHOULD be small, typically on the order
of 60 seconds, unless local clock discipline justifies a tighter bound or a
wider bound is explicitly authorized for the workflow. Replay handling under
{{replay-freshness}} continues to apply independently of <spanx style="verb">iat</spanx>.</t>
<t>The JWS algorithm MUST be an asymmetric algorithm. The <spanx style="verb">none</spanx> algorithm MUST
NOT be used. The JWS verification key MUST be trusted under local policy for
the ActorID represented in the proof.</t>
</section>

<section anchor="jwt-hop-ack" title="Receiver Acknowledgment in JWT">
<t>A <spanx style="verb">hop_ack</spanx>, when used in a JWT deployment, MUST be a compact JWS string <xref target="RFC7515"></xref>. The
JWS protected header MUST contain <spanx style="verb">typ=act-hop-ack+jwt</spanx>. The JWS payload MUST
be the UTF-8 encoding of a JCS-serialized JSON object with at least these
members:</t>
<t>Verifiers MUST require exact <spanx style="verb">typ=act-hop-ack+jwt</spanx> matching for this artifact
class. The <spanx style="verb">alg</spanx> header parameter MUST be present and MUST identify an
asymmetric signature algorithm accepted by local policy. Algorithm identifiers
are defined by JWA <xref target="RFC7518"></xref> or by an applicable extension. <spanx style="verb">alg=none</spanx> MUST
NOT be accepted. If <spanx style="verb">crit</spanx> is present, verifiers MUST reject unless every listed
parameter is understood and processed.</t>
<t>
<list style="symbols">
<t><spanx style="verb">ctx</spanx>;</t>
<t><spanx style="verb">acti</spanx>;</t>
<t><spanx style="verb">actp</spanx>;</t>
<t><spanx style="verb">jti</spanx>;</t>
<t><spanx style="verb">inbound_jti</spanx>;</t>
<t><spanx style="verb">iat</spanx>;</t>
<t><spanx style="verb">exp</spanx>;</t>
<t><spanx style="verb">target_context</spanx>;</t>
<t>OPTIONAL <spanx style="verb">presenter</spanx>;</t>
<t><spanx style="verb">recipient</spanx>;</t>
<t>for declared profiles, OPTIONAL <spanx style="verb">act</spanx> as permitted by the selected profile's
disclosure rules for the acknowledged inbound hop;</t>
<t>for verified profiles, <spanx style="verb">inbound_commitment</spanx>; and</t>
<t><spanx style="verb">ack</spanx>.</t>
</list>
</t>
<t>The <spanx style="verb">ctx</spanx> member value MUST equal <spanx style="verb">actor-chain-hop-ack-v1</spanx>. The
<spanx style="verb">recipient</spanx> member MUST be an ActorID object. If <spanx style="verb">presenter</spanx> is present, it
MUST be an ActorID object established under local policy for that hop. The
<spanx style="verb">ack</spanx> member MUST have the value <spanx style="verb">accepted</spanx>. The <spanx style="verb">jti</spanx> member MUST uniquely
identify the <spanx style="verb">hop_ack</spanx> JWT itself. The <spanx style="verb">inbound_jti</spanx> member MUST carry the
<spanx style="verb">jti</spanx> value from the acknowledged inbound token. The <spanx style="verb">iat</spanx> and <spanx style="verb">exp</spanx> members
MUST be JWT NumericDate values, and <spanx style="verb">exp</spanx> SHOULD be short-lived according to
local policy. The <spanx style="verb">target_context</spanx> member MUST conform to the representation
defined in {{target-context}}. For declared profiles, the <spanx style="verb">act</spanx>
member, when present, MUST carry the validated inbound <spanx style="verb">act</spanx> value and
MUST NOT disclose more than the selected profile allowed on the acknowledged
inbound hop. For verified profiles, the <spanx style="verb">inbound_commitment</spanx> member MUST be
copied directly from the verified inbound commitment digest extracted from the
inbound token's <spanx style="verb">actc.curr</spanx>. The JWS signer MUST be the recipient, and the
verification key MUST be trusted under local policy for that recipient
ActorID.</t>
</section>

<section anchor="jwt-commitment" title="Commitment Object in JWT">
<t>The <spanx style="verb">actc</spanx> claim value MUST be a compact JWS string <xref target="RFC7515"></xref>. The JWS
protected header MUST contain <spanx style="verb">typ=act-commitment+jwt</spanx>.</t>
<t>Verifiers MUST require exact <spanx style="verb">typ=act-commitment+jwt</spanx> matching for this
artifact class. The <spanx style="verb">alg</spanx> header parameter MUST be present and MUST identify
an asymmetric signature algorithm accepted by local policy. Algorithm
identifiers are defined by JWA <xref target="RFC7518"></xref> or by an applicable extension.
<spanx style="verb">alg=none</spanx> MUST NOT be accepted. If <spanx style="verb">crit</spanx> is present, verifiers MUST reject unless every
listed parameter is understood and processed.</t>
<t>The JWS payload MUST be the UTF-8 encoding of a JCS-serialized JSON object with
exactly these members:</t>
<t>
<list style="symbols">
<t><spanx style="verb">ctx</spanx>;</t>
<t><spanx style="verb">iss</spanx>;</t>
<t><spanx style="verb">acti</spanx>;</t>
<t><spanx style="verb">actp</spanx>;</t>
<t><spanx style="verb">halg</spanx>;</t>
<t><spanx style="verb">prev</spanx>;</t>
<t><spanx style="verb">step_hash</spanx>; and</t>
<t><spanx style="verb">curr</spanx>.</t>
</list>
</t>
<t>The meaning of those members is defined in the main text. <spanx style="verb">actc</spanx> is a
commitment-ledger artifact, not an access token, and therefore does not use
access-token lifetime claims such as <spanx style="verb">exp</spanx>.</t>
</section>
</section>

<section anchor="security-considerations" title="Security Considerations">
<t>A fuller threat discussion appears in {{threat-model}}. This section keeps only the
security considerations that directly affect interoperable processing or likely
implementation choices.</t>

<section anchor="actor-authentication-and-presenter-binding-are-deployment-specific" title="Actor Authentication and Presenter Binding Are Deployment-Specific">
<t>This specification assumes that Authorization Servers can determine the current
actor for an exchange and that recipients MAY establish the presenting actor for
a hop under local policy when needed. The mechanisms that provide those inputs
are intentionally outside the scope of this specification.</t>
</section>

<section anchor="canonicalization-errors-break-interoperability-and-proof-validity" title="Canonicalization Errors Break Interoperability and Proof Validity">
<t>Any ambiguity in canonical serialization, actor identity representation, target
representation, or proof payload encoding can cause false verification failures
or inconsistent commitment values across implementations.</t>
</section>

<section anchor="target-consistency-is-a-security-boundary" title="Target Consistency Is a Security Boundary">
<t>For verified profiles, the proof-bound target context, accepted Authorization
Server state, and issued access-token authority are one consistency boundary.
Implementations must fail closed if these values cannot be shown to be
identical or properly narrowed.</t>
</section>

<section anchor="commitment-state-is-byte-oriented" title="Commitment State Is Byte-Oriented">
<t>For JWT bindings in this specification, commitment linkage uses the exact
compact-JWS proof artifact bytes. This means commitment state is intentionally
artifact-byte-oriented rather than normalized to semantic proof meaning.
Implementations MUST NOT assume semantically equivalent decoded proofs will
yield identical <spanx style="verb">step_hash</spanx> or <spanx style="verb">curr</spanx> values unless the underlying compact-JWS
bytes are also identical. Parties that need semantic or evidentiary replay
SHOULD log and retain those exact proof artifacts and verification context.</t>
</section>

<section anchor="disclosed-chain-does-not-prevent-payload-abuse" title="Disclosed Chain Does Not Prevent Payload Abuse">
<t>A valid disclosed actor chain does not imply that the application-layer request
content is safe, correct, or policy-conformant. Recipients MUST apply local
payload validation and authorization.</t>
</section>

<section anchor="verified-profiles-depend-on-proof-retention" title="Verified Profiles Depend on Proof Retention">
<t>The evidentiary benefits of the verified profiles depend on retention or
recoverability of step proofs, exchange records, and relevant verification
material. Without such retention, the profiles still provide structured
commitment state, but post hoc provability and non-repudiation are materially
weakened.</t>
<t>Authorization Servers supporting verified profiles SHOULD retain proof state,
exchange records, authoritative workflow chain state, and the historical
verification material needed for later verification for at least the maximum
validity period of the longest-lived relevant token plus a
deployment-configured audit window. Retention policies SHOULD also account for
later verification during or after key rotation.</t>
</section>

<section anchor="subset-disclosure-profiles-reveal-only-a-verified-subset" title="Subset-Disclosure Profiles Reveal Only a Verified Subset">
<t>Recipients using the subset-disclosure profiles can authorize based only on the
disclosed actor chain subset that they verify. They MUST treat undisclosed
prior actors as unavailable and MUST NOT infer adjacency, absence, or exact
chain length from the disclosed subset alone.</t>
<t>In this base JWT/JWS binding, the returned profiled access token is visible to the
current actor and to the next recipient. Therefore, a returned subset-disclosure
token cannot safely reveal an actor identity that the current actor is not
permitted to learn. A future recipient-protected disclosure mechanism MAY relax
that limitation, but it is outside this base specification.</t>
<t>A singleton disclosed actor chain containing only the current actor is a valid
subset-disclosure outcome. In that case, downstream authorization is current-
actor-only even though the underlying profile remains one of the subset
profiles unless <spanx style="verb">actp</spanx> explicitly selects an actor-only profile.</t>
<t>Deployments whose recipient authorization requires prior-path membership or
ordering evidence MUST ensure that the selected profile and disclosure policy
expose that required evidence. They MUST NOT treat actor-only or omitted-<spanx style="verb">act</spanx>
outcomes as sufficient for such path-sensitive authorization.</t>
</section>

<section anchor="cross-domain-re-issuance-must-preserve-chain-state" title="Cross-Domain Re-Issuance Must Preserve Chain State">
<t>A cross-domain Authorization Server that re-issues a local token for the next
recipient must preserve the relevant disclosed actor-chain state and must not
broaden target authority. For <spanx style="verb">sub</spanx>, it can translate the representation only
when doing so preserves the same underlying subject semantics and does not
broaden disclosure. Unknown target-context members are not safe to ignore; they
are either preserved with constraining semantics, mapped safely, or rejected.</t>
</section>

<section anchor="branch-reconstruction-is-an-audit-concern" title="Branch Reconstruction Is an Audit Concern">
<t>This specification allows an Authorization Server to issue multiple successor
tokens from one prior accepted state, but it does not standardize merge or
sibling-discovery semantics across branches. Reconstructing a branched call
graph is therefore a forensic or legal-audit concern rather than a normal
online authorization requirement.</t>
<t>Retained Authorization Server records, timestamps, commitment state, and causal
links among presenting actors, current actors, and subsequent actors can often
reveal much of the effective call graph, but this base specification alone does
not guarantee a complete standardized graph across all branches.</t>
<t>This specification does not standardize per-actor causal timestamps inside
nested <spanx style="verb">act</spanx>. Deployments that need branch and time reconstruction SHOULD rely
on retained Authorization Server records, profiled access token or proof timestamps,
commitment state, and parent-child causal linkage across accepted successor
tokens rather than embedding chronology fields in disclosed <spanx style="verb">act</spanx> entries.</t>
</section>

<section anchor="intended-recipient-checks-reduce-confused-deputy-risk" title="Intended Recipient Checks Reduce Confused-Deputy Risk">
<t>Accepting Authorization Servers MUST ensure that the authenticated current actor
was an intended recipient of the inbound <spanx style="verb">subject_token</spanx>. This reduces a class
of deputy and repurposing attacks, though it does not eliminate all
confused-deputy scenarios.</t>
</section>

<section anchor="chain-depth" title="Chain Depth">
<t>Chain-depth limits reduce resource-exhaustion risk and keep disclosed actor
chains understandable to recipients. The deterministic processing and error
mapping for chain-depth enforcement are defined in {{chain-depth-limits}}.</t>
</section>

<section anchor="key-management" title="Key Management">
<t>Actors SHOULD use short-lived keys and/or hardware-protected keys. Deployments
that require long-term auditability MUST retain, or make durably discoverable,
the historical verification material needed to validate archived step proofs and
receiver acknowledgments after key rotation.</t>
</section>
</section>

<section anchor="privacy" title="Privacy Requirements and Considerations">
<t>This section keeps the privacy requirements that affect protocol behavior.
Additional trust-boundary and operational notes appear in {{trust-audit}}.</t>
<t>Profiles that disclose prior actors inline expose those actors to downstream
recipients. Deployments that do not require full disclosed prior-actor
authorization SHOULD consider one of the subset-disclosure profiles.</t>
<t>The stable actor-chain identifier <spanx style="verb">acti</spanx> correlates all accepted hops within one
workflow instance. Accordingly, <spanx style="verb">acti</spanx> MUST be opaque and MUST NOT encode actor
identity, profile selection, business semantics, or target meaning.</t>
<t>Even in the privacy-preserving profiles, the Authorization Server
processing token exchange observes the authenticated current actor and any
retained chain-related state. Accordingly, these profiles reduce profiled
access token disclosure but do not hide prior actors from the issuing
Authorization Server.</t>
<t>Deployments concerned with minimization SHOULD consider:</t>
<t>
<list style="symbols">
<t>pairwise or pseudonymous actor identifiers;</t>
<t>workflow-local or pairwise <spanx style="verb">sub</spanx> aliases;</t>
<t>omission of auxiliary claims unless receiving policy depends on them; and</t>
<t>the subset-disclosure profiles when partial actor-chain disclosure is
sufficient.</t>
</list>
</t>

<section anchor="subset-disclosure-and-auxiliary-encodings" title="Subset Disclosure and Auxiliary Encodings">
<t>This specification defines subset-disclosure semantics for the Declared
Subset Disclosure profile and the Verified Subset Disclosure profile. In
both profiles, if <spanx style="verb">act</spanx> is disclosed, the recipient-visible <spanx style="verb">act</spanx> is a
profile-defined ordered subsequence of the actor chain for that hop, carried as
a disclosed <spanx style="verb">act</spanx> claim containing only the disclosed subset. A subset
profile MAY also omit <spanx style="verb">act</spanx> entirely.</t>
<t>When a subset profile discloses <spanx style="verb">act</spanx>, that representation is the interoperable
base-wire format for the disclosed subset.</t>
<t>Deployments MAY additionally use an optional selective-disclosure or
recipient-protected encoding technique by agreement, including Selective
Disclosure JWT (SD-JWT) <xref target="RFC9901"></xref>, a future COSE/CBOR companion binding,
or an encrypted envelope, but only as an auxiliary overlay. Such an overlay
MUST NOT replace any required disclosed-subset <spanx style="verb">act</spanx> representation in the
interoperable base-wire format; it MAY only add an equivalent presentation form
whose disclosed value matches the same recipient-visible <spanx style="verb">act</spanx> and does not
change any required validation result.</t>
<t>This specification defines the following actor-chain-specific constraints on
such use:</t>
<t>
<list style="symbols">
<t>for the Declared Subset Disclosure profile, any disclosed actor chain MUST
be an ordered subsequence of the asserted chain state for that hop;</t>
<t>for the Verified Subset Disclosure profile, any disclosed actor chain MUST
be an ordered subsequence of the verified actor-visible chain for that
hop;</t>
<t>if the selected profile uses step proofs or chain commitments, those
artifacts remain bound to the verified hop progression, not to a later
disclosed subset;</t>
<t>a verifier MUST treat undisclosed information as unavailable and MUST require
disclosure of any information needed for authorization; and</t>
<t>an encoding used with a Full Disclosure profile MUST reveal the complete
disclosed actor chain required by that profile to the recipient before
authorization.</t>
</list>
</t>
</section>
</section>

<section anchor="iana" title="IANA Considerations">
<t>This specification does not create a new hash-algorithm registry.
<spanx style="verb">actc</spanx> uses hash algorithm names from the IANA Named
Information Hash Algorithm Registry <xref target="IANA.Hash.Algorithms"></xref>, subject to the
algorithm restrictions defined in this document.</t>

<section anchor="json-web-token-claims-registration" title="JSON Web Token Claims Registration">
<t>This document requests registration of the following claims in the &quot;JSON Web
Token Claims&quot; registry established by <xref target="RFC7519"></xref>:</t>
<texttable>
<ttcol>Claim Name</ttcol>
<ttcol>Claim Description</ttcol>
<ttcol>Change Controller</ttcol>
<ttcol>Specification Document(s)</ttcol>


<c>actc</c>
<c>Proof-bound chain state binding accepted hop progression for the active profile.</c>
<c>IETF</c>
<c>[this document]</c>

<c>acti</c>
<c>Actor-chain identifier preserved across accepted hops.</c>
<c>IETF</c>
<c>[this document]</c>

<c>actp</c>
<c>Actor-chain profile identifier for the issued token.</c>
<c>IETF</c>
<c>[this document]</c>

</texttable></section>

<section anchor="media-type-registration" title="Media Type Registration">
<t>This document requests registration of the following media types in the
&quot;Media Types&quot; registry established by <xref target="RFC6838"></xref>:</t>
<texttable>
<ttcol>Type</ttcol>
<ttcol>Subtype</ttcol>
<ttcol>Application</ttcol>


<c>application</c>
<c>act-step-proof+jwt</c>
<c>OAuth 2.0 Token Exchange actor-chain step proofs.</c>

<c>application</c>
<c>act-commitment+jwt</c>
<c>OAuth 2.0 Token Exchange actor-chain commitments.</c>

<c>application</c>
<c>act-hop-ack+jwt</c>
<c>OAuth 2.0 Token Exchange actor-chain receiver acknowledgments.</c>

</texttable><t>The following registration fields apply to each media type above:</t>
<t>
<list style="symbols">
<t>Required parameters: N/A</t>
<t>Optional parameters: N/A</t>
<t>Encoding considerations: binary</t>
<t>Security considerations: see this document</t>
<t>Interoperability considerations: N/A</t>
<t>Published specification: this document</t>
<t>Fragment identifier considerations: N/A</t>
<t>Additional information: Magic Number(s): N/A; File Extension(s): N/A;
Macintosh File Type Code(s): N/A</t>
<t>Contact: IETF</t>
<t>Intended usage: COMMON</t>
<t>Restrictions on usage: N/A</t>
<t>Author: IETF</t>
<t>Change controller: IETF</t>
</list>
</t>
</section>

<section anchor="oauth-uri-registration" title="OAuth URI Registration">
<t>This document requests registration of the following value in the
&quot;OAuth URI&quot; registry established by <xref target="RFC6749"></xref>:</t>
<texttable>
<ttcol>URI</ttcol>
<ttcol>Description</ttcol>
<ttcol>Change Controller</ttcol>
<ttcol>Specification Document(s)</ttcol>


<c>urn:ietf:params:oauth:grant-type:actor-chain-bootstrap</c>
<c>OAuth grant type for the initial verified profile bootstrap token request.</c>
<c>IETF</c>
<c>[this document]</c>

</texttable></section>

<section anchor="oauth-authorization-server-metadata-registration" title="OAuth Authorization Server Metadata Registration">
<t>This document requests registration of the following metadata names in the
&quot;OAuth Authorization Server Metadata&quot; registry established by <xref target="RFC8414"></xref>:</t>
<texttable>
<ttcol>Metadata Name</ttcol>
<ttcol>Metadata Description</ttcol>
<ttcol>Change Controller</ttcol>
<ttcol>Specification Document(s)</ttcol>


<c>actor_chain_bootstrap_endpoint</c>
<c>Endpoint used to mint bootstrap context for verified profile initial actors.</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_profiles_supported</c>
<c>Supported actor-chain profile identifiers.</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_commitment_hashes_supported</c>
<c>Supported commitment hash algorithm identifiers.</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_receiver_ack_supported</c>
<c>Indicates support for receiver acknowledgments (hop_ack) under this specification.</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_refresh_supported</c>
<c>Indicates support for Refresh-Exchange under this specification.</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_cross_domain_supported</c>
<c>Indicates support for cross-domain re-issuance under this specification.</c>
<c>IETF</c>
<c>[this document]</c>

</texttable></section>

<section anchor="oauth-parameter-registration" title="OAuth Parameter Registration">
<t>This document requests registration of the following parameter names in the
relevant OAuth parameter registry:</t>
<texttable>
<ttcol>Parameter Name</ttcol>
<ttcol>Parameter Usage Location</ttcol>
<ttcol>Change Controller</ttcol>
<ttcol>Specification Document(s)</ttcol>


<c>actor_chain_profile</c>
<c>actor-chain bootstrap endpoint request; OAuth token endpoint request</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_bootstrap_context</c>
<c>actor-chain bootstrap endpoint response; OAuth token endpoint request</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_step_proof</c>
<c>OAuth token endpoint request</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_refresh</c>
<c>OAuth token endpoint request</c>
<c>IETF</c>
<c>[this document]</c>

<c>actor_chain_cross_domain</c>
<c>OAuth token endpoint request</c>
<c>IETF</c>
<c>[this document]</c>

</texttable><t>Part III is informative and defines no conformance requirements. Where Part III
summarizes requirements from Part II, Part II controls.</t>
<t>This part is organized so that it could be separated into an Informational
companion draft in a future revision.</t>
</section>
</section>

<section anchor="motivating-real-world-examples" title="Motivating Real-World Examples">

<section anchor="full-disclosure-emergency-production-change-in-critical-infrastructure" title="Full Disclosure: Emergency Production Change in Critical Infrastructure">
<t>A safety-critical production environment requires a hotfix during an incident.
The workflow is:</t>
<t>
<list style="symbols">
<t><spanx style="verb">A</spanx> = on-call engineer;</t>
<t><spanx style="verb">B</spanx> = incident commander;</t>
<t><spanx style="verb">C</spanx> = security approver;</t>
<t><spanx style="verb">D</spanx> = deployment service; and</t>
<t><spanx style="verb">E</spanx> = runtime control plane.</t>
</list>
</t>
<t>Every downstream hop must see the complete disclosed approval path before
allowing the change to proceed. The deployment service and runtime control
plane both need to verify inline that the on-call engineer initiated the
action, the incident commander approved it, and the security approver signed
off. This is the motivating case for the Full Disclosure profiles: every hop
needs the same complete disclosed lineage for normal online authorization.</t>
</section>

<section anchor="subset-disclosure-regulated-m-a-review" title="Subset Disclosure: Regulated M&amp;A Review">
<t>A large acquisition review passes through multiple highly sensitive business
functions. The workflow is:</t>
<t>
<list style="symbols">
<t><spanx style="verb">A</spanx> = market-intelligence team;</t>
<t><spanx style="verb">B</spanx> = product-strategy review;</t>
<t><spanx style="verb">C</spanx> = internal legal review;</t>
<t><spanx style="verb">D</spanx> = antitrust counsel; and</t>
<t><spanx style="verb">E</spanx> = Chief Executive Officer.</t>
</list>
</t>
<t>Intermediate actors are intentionally compartmentalized and may be forbidden
from learning who else contributed to the decision. The CEO, however, may need
a richer disclosed chain before approving the transaction. This is the
motivating case for the Subset Disclosure profiles: the Authorization Server
retains authoritative workflow state and discloses to each recipient only the
portion of the chain that recipient is allowed to learn.</t>
</section>

<section anchor="actor-only-disclosure-bank-wire-payment-processing-pipeline" title="Actor-Only Disclosure: Bank Wire-Payment Processing Pipeline">
<t>A bank processes high-value wire payments through a sequence of narrowly scoped
control services. The workflow is:</t>
<t>
<list style="symbols">
<t><spanx style="verb">A</spanx> = payment-initiation service;</t>
<t><spanx style="verb">B</spanx> = sanctions-screening service;</t>
<t><spanx style="verb">C</spanx> = fraud-scoring service;</t>
<t><spanx style="verb">D</spanx> = treasury and limit-check service;</t>
<t><spanx style="verb">E</spanx> = payment-release service; and</t>
<t><spanx style="verb">F</spanx> = SWIFT or bank-connector service.</t>
</list>
</t>
<t>Each stage authorizes only on the immediately preceding actor for that hop.
The sanctions service needs to know only that the payment-initiation service
invoked it; the fraud service needs to know only that sanctions invoked it;
and so on. Revealing the entire upstream pipeline to every stage adds no value
to the local authorization decision and unnecessarily exposes internal control
topology. This is the motivating case for the Actor-Only profiles: every hop
sees only the current actor inline, while richer workflow state may still be
retained by the Authorization Server for forensic review, legal audit, or
incident review.</t>
</section>
</section>

<section anchor="deployment-context" title="Deployment Context">
<t><xref target="RFC8693"></xref> defines the top-level <spanx style="verb">act</spanx> claim for the current actor and allows
nested prior actors. However, prior nested <spanx style="verb">act</spanx> claims are informational only
for access-control decisions. In multi-hop systems, especially
service-to-service and agentic systems, that is not sufficient.</t>
<t>Consider:</t>

<figure><artwork type="text">User -&gt; Orchestrator -&gt; Planner -&gt; Tool Agent -&gt; Data API
</artwork></figure>

<t>By the time the request reaches the Data API, the immediate caller may or
may not be visible in the token, and the upstream delegation path is not
standardized as a policy input
and is not bound across successive token exchanges in a way that can be
independently validated or audited. This creates several concrete gaps:</t>
<t>
<list style="symbols">
<t>downstream policy cannot reliably evaluate the full delegation path;</t>
<t>cross-exchange continuity is not standardized;</t>
<t>tampering by an actor and its home Authorization Server is not uniformly addressed;</t>
<t>forensic verification of per-hop participation is not standardized; and</t>
<t>profiled access tokens may disclose more actor-chain information than some
deployments are willing to reveal.</t>
</list>
</t>

<section anchor="when-an-actor-chain-profile-is-needed" title="When an Actor-Chain Profile Is Needed">
<t>Not every OAuth deployment requires an actor-chain profile. The need depends on
whether parties other than the issuing Authorization Server need to reason
about, authorize on, or later prove the actor progression of a multi-hop
workflow.</t>
<t>Plain OAuth 2.0 Token Exchange without this profile family can be sufficient
when all participating components remain within one trust domain and one
Authorization Server, recipients authorize only on the immediate access token,
there is no later need to prove per-hop participation for audit or dispute
resolution, and local conventions for any nested <spanx style="verb">act</spanx> use are self-consistent
across the participating implementations. In that case, adding <spanx style="verb">actp</spanx>, <spanx style="verb">acti</spanx>,
or <spanx style="verb">actc</spanx> adds wire and processing overhead without a clear consumer.</t>
<t>Within one trust domain, an actor-chain profile becomes useful when recipients
need standardized workflow continuity, when multi-step services or agents need
downstream authorization based on actor progression, when approval paths or
privileged actions need later review, when compartmentalized workflows need
recipient-specific disclosure, or when incident review or chargeback processes
need reconstruction of who acted and in what order.</t>
<t>The disclosure mode follows recipient need-to-know. A recipient that needs the
complete disclosed path points toward a Full Disclosure profile. A recipient
that needs only an Authorization-Server-selected subset points toward a Subset
Disclosure profile. A recipient that needs only the immediate upstream actor
points toward an Actor-Only Disclosure profile.</t>
<t>The evidence model follows audit and non-repudiation needs. Declared variants
fit deployments where the issuing Authorization Server is the sole trust anchor
and the non-collusion assumption between an actor and its home Authorization
Server is acceptable. Verified variants add actor-signed step proofs and
cumulative commitment state in <spanx style="verb">actc</spanx> so that later participants and auditors
can verify accepted hop progression with less reliance on Authorization-Server
assertion alone.</t>
<t>Across trust domains, verified profiles are often useful because preserved
<spanx style="verb">actc</spanx> carries cumulative commitment state forward across Authorization Server
boundaries. Cross-domain hops that preserve, rather than extend, accepted chain
state use the cross-domain re-issuance flow defined in
{{cross-domain-reissuance}} and do not append the foreign Authorization Server
to the chain. Disclosure mode remains a per-recipient policy decision at the
issuing or re-issuing Authorization Server.</t>
<t>Action sensitivity can further influence configuration. Higher-impact reads,
state-changing actions, actor-spawning actions, and recursive orchestration can
justify a verified profile, <spanx style="verb">hop_ack</spanx>, <spanx style="verb">request_id</spanx> in <spanx style="verb">target_context</spanx>,
stricter chain-depth limits, stronger retention, and presenter binding.
Action sensitivity does not change the six profiles; it guides how the selected
profile is configured and when stronger evidence is warranted.</t>
<t>In summary, if no party outside the issuing Authorization Server needs to
reason about actor progression, an actor-chain profile is usually not needed.
Otherwise, the disclosure mode is selected by what each recipient is permitted
to learn, and the evidence model is selected by whether later independent proof
of per-hop participation is required. Multi-domain deployments often use a
verified profile together with cross-domain re-issuance. Action sensitivity can
further influence whether hop acknowledgments, request identifiers, stricter
chain-depth limits, and presenter binding are appropriate.</t>
</section>
</section>

<section anchor="threat-model" title="Threat Model">
<t>This specification defines a multi-hop, multi-actor delegation model across one
or more trust domains. The security properties provided depend on the selected
profile, the trust relationship among participating Authorization Servers,
and the availability of step proofs or exchange records where relied upon.</t>

<section anchor="assets" title="Assets">
<t>The protocol seeks to protect the following assets:</t>
<t>
<list style="symbols">
<t>continuity of the delegation path;</t>
<t>integrity of prior-actor ordering and membership;</t>
<t>continuity of the actor represented as current for each hop when such
continuity is disclosed or otherwise established under local policy;</t>
<t>binding of each hop to the intended target;</t>
<t>resistance to replay of previously accepted hop state;</t>
<t>audit evidence for later investigation and proof; and</t>
<t>minimization of prior-actor disclosure where privacy-preserving profiles are
used.</t>
</list>
</t>
</section>

<section anchor="adversaries" title="Adversaries">
<t>Relevant adversaries include:</t>
<t>
<list style="symbols">
<t>an external attacker that steals or replays a token;</t>
<t>a malicious actor attempting to insert, omit, reorder, or repurpose hop
state;</t>
<t>a malicious actor colluding with its home Authorization Server;</t>
<t>a malicious downstream recipient attempting to over-interpret or misuse an
inbound token;</t>
<t>an untrusted or compromised upstream Authorization Server in a multi-domain
path; and</t>
<t>an unsolicited victim service reached by a validly issued token without
having agreed to participate.</t>
</list>
</t>
</section>

<section anchor="assumptions" title="Assumptions">
<t>This specification assumes:</t>
<t>
<list style="symbols">
<t>verifiers can validate token signatures and issuer trust;</t>
<t>the authenticated actor identity used in token exchange is bound under
local trust policy to the actor identity represented in profile-defined
proofs; and</t>
<t>deployments that rely on later proof verification retain, or can discover,
the verification material needed to validate archived step proofs and exchange
records.</t>
</list>
</t>
</section>

<section anchor="security-goals" title="Security Goals">
<t>The protocol aims to provide the following properties:</t>
<t>
<list style="symbols">
<t>in the Declared Full Disclosure profile, silent insertion, removal,
reordering, or modification of prior actors is prevented under the assumption
that an actor does not collude with its home Authorization Server;</t>
<t>in the Declared Subset Disclosure profile, profiled access tokens reveal
only a disclosed ordered subset of actors selected by the Authorization
Server, and authorization is limited to that disclosed subset;</t>
<t>in the Verified Subset Disclosure profile, each
accepted hop is bound to an actor-signed proof over the exact actor-visible
chain for that hop and to cumulative commitment state, while profiled access tokens
reveal only an ordered subset of that actor-visible chain selected by the
Authorization Server;</t>
<t>in the Verified Full Disclosure profile, the actor-visible chain equals the
complete disclosed actor chain at each hop, preserving complete disclosed-chain authorization while
improving detectability, provability, and non-repudiation; and</t>
<t>in the Verified Actor-Only Disclosure profile, profiled access tokens disclose
only the current actor while preserving current-actor continuity where that
continuity is disclosed or otherwise established under local policy,
together with cumulative commitment state for later verification.</t>
</list>
</t>
</section>

<section anchor="non-goals" title="Non-Goals">
<t>This specification does not by itself provide:</t>
<t>
<list style="symbols">
<t>integrity or safety guarantees for application payload content;</t>
<t>complete prevention of confused-deputy behavior;</t>
<t>concealment of prior actors from the Authorization Server that processes
token exchange;</t>
<t>standardized merge or branch-selection semantics across branched work; or</t>
<t>universal inline prevention of every invalid token that could be issued by a
colluding actor and its home Authorization Server.</t>
</list>
</t>
</section>

<section anchor="residual-risks" title="Residual Risks">
<t>Even when all checks succeed, a valid token chain does not imply that the
requested downstream action is authorized by local business policy. As specified
in Part II, recipients evaluate authorization using only the actor-chain
information actually disclosed to them by the artifact together with token
subject, intended target, and local policy.</t>
<t>Deployments that depend on independently verifiable provenance for high-risk
operations typically require synchronous validation of commitment-linked proof
state or otherwise treat the issuing Authorization Server as the sole trust
anchor.</t>
<t>In the Verified Subset Disclosure and Verified Actor-Only Disclosure profiles,
a current actor signs only the exact actor-visible chain available at that hop.
Those profiles therefore provide non-repudiation over the signed actor-visible chain
and linked commitment state, not over hidden prefix semantics against a rogue or
colluding Authorization Server.</t>
</section>
</section>

<section anchor="trust-audit" title="Trust Boundaries and Audit Guidance">
<t>The trust model and visible-disclosure properties of the six profiles are
defined in the main specification text and {{threat-model}}. This section focuses on
operational retention and forensic guidance rather than restating those profile
summaries.</t>
<t>Authorization Servers supporting these profiles are expected to retain records
keyed by <spanx style="verb">acti</spanx> and <spanx style="verb">jti</spanx>.</t>
<t>For verified profiles, the retention period is expected to be at least the
maximum validity period of the longest-lived relevant token plus a deployment-
configured audit window, and to remain sufficient to validate historical proofs
across key rotation.</t>
<t>For verified profiles, such records usually include:</t>
<t>
<list style="symbols">
<t>prior token reference;</t>
<t>authenticated actor identity accepted for the exchange or proof-validation step;</t>
<t>step proof reference or value;</t>
<t>issued token reference;</t>
<t>commitment state;</t>
<t>requested audience or target context; and</t>
<t>timestamps.</t>
</list>
</t>
<t>For subset-disclosure profiles, retained records are expected to also allow
reconstruction of the verified actor-visible chain for each hop and the
disclosed subset issued for each recipient. Collecting all such accepted hop
evidence for one <spanx style="verb">acti</spanx>, including retained tokens, proofs, commitments, and
exchange records, can reconstruct the accepted hop sequence, including
repeated-actor revisits, and can often reveal much of the effective call
graph, but this specification does not by itself yield a complete standardized
graph across related branches. If a deployment also relies on a hidden
full-chain prefix not signed by every acting intermediary, the Authorization
Server is expected to retain the
additional state needed to reconstruct that hidden prefix for later audit.</t>
<t>Actors are also expected to retain local records sufficient to support replay
detection, incident investigation, and later proof of participation.</t>
</section>

<section anchor="design-rationale-and-relationship-to-other-work" title="Design Rationale and Relationship to Other Work">
<t>This document complements <xref target="RFC8693"></xref> by defining chain-aware token-exchange
profiles. It also fits alongside the broader SPICE service-to-service and
attestation work <xref target="I-D.ietf-spice-s2s-protocol"></xref>
<xref target="I-D.draft-mw-spice-transitive-attestation"></xref> and composes with companion
SPICE provenance work: Actor Chain addresses <spanx style="strong">WHO</spanx> acted, Intent Chain
<xref target="I-D.draft-mw-spice-intent-chain"></xref> addresses <spanx style="strong">WHAT</spanx> was produced or
transformed, and Inference Chain <xref target="I-D.draft-mw-spice-inference-chain"></xref>
addresses <spanx style="strong">HOW</spanx> a result was computed.</t>
<t>This specification defines six profiles instead of one deployment mode
so that implementations can choose among complete disclosed actor chain-based
authorization, trust-first partial disclosure, explicit actor-only operation,
stronger commitment-state accountability, recipient-specific commitment-backed
partial disclosure, and verified actor-only disclosure without changing the
core progression model.</t>
<t>The base specification remains linear. Branching, richer disclosure mechanisms,
and evidence-discovery protocols remain future work because they require
additional identifiers, validation rules, and interoperability work.</t>

<section anchor="relationship-to-tls-session-bound-access-tokens" title="Relationship to TLS-Session-Bound Access Tokens">
<t>Actor-chain validation and TLS-session-bound presentation address different
replay points in a multi-hop on-behalf-of flow. TLS-session-bound access tokens
bind presentation of an OAuth access token to a specific mTLS connection using
a proof based on TLS Exporter material and the access-token hash
<xref target="I-D.mw-oauth-tls-session-bound-tokens"></xref>.</t>
<t>In an A-to-B-to-C flow, TLS-session-bound presentation lets B verify that the
A-to-B token was presented on the current A-to-B mTLS connection. This protects
against replay of that token, or that token together with an old presentation
proof, on a different connection.</t>
<t>Actor-chain validation protects successor-token issuance. When B exchanges the
inbound token for a successor token targeted to C, the Authorization Server
authenticates B as the current actor, validates that B was an intended recipient
of the inbound <spanx style="verb">subject_token</spanx>, and enforces profile-specific chain-continuity,
disclosure, replay, and freshness rules.</t>
<t>Because an Authorization Server normally cannot recompute the TLS Exporter
value for a prior B-terminated connection, deployments that require
Authorization-Server-visible evidence of prior-hop presentation can require B,
or a verifier co-located with B, to submit a signed confirmation that the
inbound token and its session-binding proof were verified before chain
extension.</t>
</section>

<section anchor="relationship-to-oauth-identity-and-authorization-chaining-across-domains" title="Relationship to OAuth Identity and Authorization Chaining Across Domains">
<t>OAuth Identity and Authorization Chaining Across Domains defines a pattern for
preserving identity and authorization information when a request crosses OAuth
trust-domain boundaries using a JWT authorization grant and a subsequent access
token request in the target domain <xref target="I-D.ietf-oauth-identity-chaining"></xref>.</t>
<t>This specification addresses a different, complementary problem: representing,
preserving, validating, and selectively disclosing the actor progression of a
multi-hop workflow across successive token exchanges. In particular, this
specification defines the <spanx style="verb">actp</spanx> profile signal, the stable <spanx style="verb">acti</spanx> workflow
identifier, profile-controlled disclosed <spanx style="verb">act</spanx> processing, append-only actor
progression, and, for verified profiles, actor-signed step proofs and cumulative
<spanx style="verb">actc</spanx> commitment state.</t>
<t>Where both specifications are used in the same deployment, an actor-chain token
or actor-chain state can be carried through a cross-domain exchange according
to local trust policy and the preservation rules in this document. This
document does not require use of OAuth Identity and Authorization Chaining
Across Domains, and that document does not define the actor-chain profile,
disclosure, or commitment semantics defined here.</t>
</section>
</section>

<section anchor="design-decisions" title="Design Decisions">
<t>This section records key design decisions made in the core specification so
that future revisions can preserve the underlying interoperability and security
rationale even if the document is later split.</t>

<section anchor="why-disclosed-actor-chain-state-is-carried-in-nested-act" title="Why disclosed actor chain state is carried in nested act">
<t>The interoperable JWT form uses nested <spanx style="verb">act</spanx> as the single authoritative
inline carrier for disclosed actor-chain state. This avoids maintaining separate
disclosed actor-chain claims and prevents dual-truth bugs in which two different
claims could describe different disclosed histories.</t>
</section>

<section anchor="why-top-level-sub-remains-the-workflow-subject" title="Why top-level sub remains the workflow subject">
<t>Top-level <spanx style="verb">sub</spanx> identifies the workflow subject of the token rather than the
current actor. This keeps profiled access token semantics aligned with RFC 8693 and
allows the current actor to remain visible in <spanx style="verb">act</spanx>. Replacing <spanx style="verb">sub</spanx> with the
current actor or with a constant marker would make token subject semantics much
less clear for recipients and auditors.</t>
</section>

<section anchor="why-privacy-sensitive-profiles-use-stable-subject-aliases" title="Why privacy-sensitive profiles use stable subject aliases">
<t>Subset-disclosure operation can hide actors only if other visible fields do not
reveal the hidden subject indirectly. The Authorization Server therefore chooses a stable
workflow-subject representation at bootstrap, such as a pairwise or
workflow-local alias, and preserves it for the workflow.</t>
</section>

<section anchor="why-subset-disclosure-is-recipient-specific-and-authorization-server-driven" title="Why subset disclosure is recipient-specific and Authorization-Server-driven">
<t>Different recipients in the same workflow can have different need-to-know. The
Authorization Server is therefore the policy decision and enforcement point for disclosure and
may issue a narrower or broader disclosed nested <spanx style="verb">act</spanx> to different recipients,
so long as each returned token remains consistent with the active profile and
any disclosed <spanx style="verb">act</spanx> is a permitted profile-conformant disclosure of the
accepted chain state for that hop.</t>
</section>

<section anchor="why-actor-only-is-a-separate-profile-even-though-subset-can-express-it" title="Why actor-only is a separate profile even though subset can express it">
<t>Actor-only disclosure can be expressed as a special case of subset disclosure,
but this document gives it distinct profile identifiers so that the intended
policy remains explicit and machine-readable to current and future
implementations. A token carrying <spanx style="verb">actp=declared-actor-only</spanx> or
<spanx style="verb">actp=verified-actor-only</spanx> therefore tells every conforming actor and
recipient that profiled access tokens in that workflow expose only the outermost
current actor inline, even though the Authorization Server may retain richer
hidden workflow state for later audit or forensics.</t>
</section>

<section anchor="why-branching-is-represented-across-tokens-not-inside-one-token" title="Why branching is represented across tokens, not inside one token">
<t>Each token carries one disclosed path in nested <spanx style="verb">act</spanx>. Branching is represented
by issuing multiple successor tokens that share a prior workflow state. This
keeps token syntax simple while still supporting forensic and legal-audit
reconstruction of a broader call graph from retained Authorization Server records and related
artifacts.</t>
</section>

<section anchor="why-actc-uses-parent-pointer-commitments-instead-of-merkle-trees" title="Why actc uses parent-pointer commitments instead of Merkle trees">
<t>The base commitment mechanism is hop-oriented: each accepted successor step
binds exactly one parent commitment state and one new verified hop. Allowing
multiple successors to share the same parent forms a hash-linked workflow graph
without requiring Merkle-tree ordering rules, sibling proof transmission, or
merge semantics. Future work could add Merkle-style aggregation if deployments
need compact set commitments across many branches.</t>
</section>

<section anchor="why-actp-and-actc-use-short-claim-names" title="Why actp and actc use short claim names">
<t>These claims appear in every token issued under this specification, and <spanx style="verb">actc</spanx>
also travels in later verified hops. The document therefore uses short claim
names to limit repeated per-hop overhead on the wire. Their meanings are fixed
only by this specification, and the main body defines those semantics before
IANA registration.</t>
</section>

<section anchor="why-rfc-8414-metadata-discovery-is-reused" title="Why RFC 8414 metadata discovery is reused">
<t>The specification reuses the standard RFC 8414 authorization-server metadata
endpoint for discovery. This avoids inventing a new discovery API and keeps
capability negotiation in the well-known OAuth metadata channel that
implementations already use.</t>
</section>

<section anchor="privacy-goals-and-limits" title="Privacy goals and limits">
<t>Subset disclosure limits per-hop visibility, but it does not hide information
from the issuing Authorization Server or from colluding parties that pool tokens, proofs,
acknowledgments, and logs. The design therefore treats complete branch and call
graph reconstruction primarily as a forensics or legal-audit concern rather
than an online authorization requirement.</t>
</section>
</section>

<section anchor="compact-examples" title="Compact End-to-End Examples">

<section anchor="example-1-declared-full-disclosure-in-one-domain" title="Example 1: Declared Full Disclosure in One Domain">
<t>Assume <spanx style="verb">A</spanx>, <spanx style="verb">B</spanx>, and <spanx style="verb">C</spanx> are governed by <spanx style="verb">AS1</spanx>.</t>
<t>
<list style="numbers">
<t><spanx style="verb">A</spanx> requests a token for <spanx style="verb">B</spanx> under the Declared Full Disclosure profile.</t>
<t><spanx style="verb">AS1</spanx> issues <spanx style="verb">T_A</spanx> with <spanx style="verb">act=EncodeVisibleChain([A])</spanx> and <spanx style="verb">aud=B</spanx>.</t>
<t><spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx> and presents <spanx style="verb">T_A</spanx>.</t>
<t><spanx style="verb">B</spanx> validates <spanx style="verb">T_A</spanx>, verifies continuity, and exchanges <spanx style="verb">T_A</spanx> at <spanx style="verb">AS1</spanx> for
a token to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">AS1</spanx> authenticates <spanx style="verb">B</spanx>, verifies that <spanx style="verb">B</spanx> was an intended recipient of the
inbound token, appends <spanx style="verb">B</spanx>, and issues <spanx style="verb">T_B</spanx> with
<spanx style="verb">act=EncodeVisibleChain([A,B])</spanx> and <spanx style="verb">aud=C</spanx>.</t>
<t><spanx style="verb">B</spanx> validates that the returned disclosed actor chain is exactly the prior chain plus <spanx style="verb">B</spanx>.</t>
<t><spanx style="verb">B</spanx> presents <spanx style="verb">T_B</spanx> to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">C</spanx> validates the token and authorizes based on the disclosed actor chain <spanx style="verb">[A,B]</spanx>.</t>
</list>
</t>
</section>

<section anchor="example-2-declared-subset-disclosure" title="Example 2: Declared Subset Disclosure">
<t>Assume <spanx style="verb">A</spanx>, <spanx style="verb">B</spanx>, and <spanx style="verb">C</spanx> use the Declared Subset Disclosure profile and
accept the issuing Authorization Server as the trust anchor for disclosure policy.</t>
<t>
<list style="numbers">
<t><spanx style="verb">A</spanx> requests a token for <spanx style="verb">B</spanx> under the Declared Subset Disclosure profile.</t>
<t><spanx style="verb">AS1</spanx> issues <spanx style="verb">T_A</spanx> with a recipient-specific <spanx style="verb">act</spanx>
intended for <spanx style="verb">B</spanx>, or omits <spanx style="verb">act</spanx> entirely according to local policy.</t>
<t><spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx> and presents <spanx style="verb">T_A</spanx>.</t>
<t><spanx style="verb">B</spanx> validates the token and uses only the disclosed actor chain, if any,
for actor-chain authorization. If <spanx style="verb">act</spanx> is omitted, this specification
provides no inline actor-chain input beyond what is disclosed; any
additional authorization inputs come from local policy.</t>
<t><spanx style="verb">B</spanx> exchanges <spanx style="verb">T_A</spanx> at <spanx style="verb">AS1</spanx> for a token to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">AS1</spanx> appends <spanx style="verb">B</spanx> to the accepted workflow state for that hop, applies disclosure
policy for <spanx style="verb">C</spanx>, and issues <spanx style="verb">T_B</spanx> with a recipient-specific <spanx style="verb">act</spanx>, or
with no <spanx style="verb">act</spanx>, according to local policy.</t>
<t><spanx style="verb">B</spanx> presents <spanx style="verb">T_B</spanx> to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">C</spanx> validates the token and authorizes only on the disclosed actor chain, if any. If
<spanx style="verb">act</spanx> is omitted or does not disclose <spanx style="verb">B</spanx>, this specification provides no inline current-
actor disclosure for that hop, and <spanx style="verb">C</spanx> treats undisclosed actors as unavailable.</t>
</list>
</t>
</section>

<section anchor="example-2b-declared-actor-only-disclosure" title="Example 2b: Declared Actor-Only Disclosure">
<t>Assume <spanx style="verb">A</spanx>, <spanx style="verb">B</spanx>, and <spanx style="verb">C</spanx> use the Declared Actor-Only Disclosure profile.</t>
<t>
<list style="numbers">
<t><spanx style="verb">A</spanx> requests a token for <spanx style="verb">B</spanx> under the Declared Actor-Only Disclosure profile.</t>
<t><spanx style="verb">AS1</spanx> issues <spanx style="verb">T_A</spanx> with <spanx style="verb">act=EncodeVisibleChain([A])</spanx> and <spanx style="verb">aud=B</spanx>.</t>
<t><spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx> and presents <spanx style="verb">T_A</spanx>.</t>
<t><spanx style="verb">B</spanx> validates that the disclosed <spanx style="verb">act</spanx> identifies only the current actor
<spanx style="verb">A</spanx> and authorizes only on that actor and local policy.</t>
<t><spanx style="verb">B</spanx> exchanges <spanx style="verb">T_A</spanx> at <spanx style="verb">AS1</spanx> for a token to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">AS1</spanx> issues <spanx style="verb">T_B</spanx> with <spanx style="verb">act=EncodeVisibleChain([B])</spanx> and <spanx style="verb">aud=C</spanx>.</t>
<t><spanx style="verb">B</spanx> presents <spanx style="verb">T_B</spanx> to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">C</spanx> validates the token and authorizes only on the disclosed current actor
<spanx style="verb">B</spanx> and local policy.</t>
</list>
</t>
</section>

<section anchor="example-3-verified-full-disclosure-across-two-domains" title="Example 3: Verified Full Disclosure Across Two Domains">
<t>Assume <spanx style="verb">A</spanx> and <spanx style="verb">B</spanx> are governed by <spanx style="verb">AS1</spanx>, while <spanx style="verb">C</spanx> is governed by <spanx style="verb">AS2</spanx>.</t>
<t>
<list style="numbers">
<t><spanx style="verb">A</spanx> obtains bootstrap context from <spanx style="verb">AS1</spanx>, signs <spanx style="verb">chain_sig_A</spanx>, and receives
<spanx style="verb">T_A</spanx> with <spanx style="verb">act=EncodeVisibleChain([A])</spanx> and <spanx style="verb">actc</spanx>.</t>
<t><spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx> with <spanx style="verb">T_A</spanx>.</t>
<t><spanx style="verb">B</spanx> validates <spanx style="verb">T_A</spanx>, constructs <spanx style="verb">[A,B]</spanx>, signs <spanx style="verb">chain_sig_B</spanx>, and exchanges
<spanx style="verb">T_A</spanx> at <spanx style="verb">AS1</spanx> for a token to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">AS1</spanx> verifies <spanx style="verb">chain_sig_B</spanx> submitted as <spanx style="verb">actor_chain_step_proof</spanx>,
updates the commitment, and issues <spanx style="verb">T_B</spanx> with
<spanx style="verb">act=EncodeVisibleChain([A,B])</spanx> and <spanx style="verb">aud=C</spanx>.</t>
<t>Because <spanx style="verb">C</spanx> does not trust <spanx style="verb">AS1</spanx> directly, <spanx style="verb">B</spanx> performs a second exchange at
<spanx style="verb">AS2</spanx>.</t>
<t><spanx style="verb">AS2</spanx> preserves <spanx style="verb">actp</spanx>, <spanx style="verb">acti</spanx>, <spanx style="verb">act=EncodeVisibleChain([A,B])</spanx>, and
<spanx style="verb">actc</spanx>, and issues a local token trusted by <spanx style="verb">C</spanx> that still
represents <spanx style="verb">B</spanx>.</t>
<t><spanx style="verb">C</spanx> validates the local token, sees the disclosed actor chain <spanx style="verb">[A,B]</spanx>, and
authorizes accordingly.</t>
</list>
</t>
</section>

<section anchor="example-4-verified-actor-only-disclosure" title="Example 4: Verified Actor-Only Disclosure">
<t>Assume <spanx style="verb">A</spanx>, <spanx style="verb">B</spanx>, and <spanx style="verb">C</spanx> use the Verified Actor-Only Disclosure profile.</t>
<t>
<list style="numbers">
<t><spanx style="verb">A</spanx> obtains bootstrap context, signs <spanx style="verb">chain_sig_A</spanx> over disclosed actor chain <spanx style="verb">[A]</spanx>,
and receives <spanx style="verb">T_A</spanx> with <spanx style="verb">act=EncodeVisibleChain([A])</spanx> and <spanx style="verb">actc</spanx>.</t>
<t><spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx> with <spanx style="verb">T_A</spanx>.</t>
<t><spanx style="verb">B</spanx> validates <spanx style="verb">T_A</spanx>, verifies that <spanx style="verb">A</spanx> is the presenter, constructs the
verified actor-visible chain <spanx style="verb">[A,B]</spanx>, signs <spanx style="verb">chain_sig_B</spanx>, and exchanges
<spanx style="verb">T_A</spanx> at its home Authorization Server to obtain <spanx style="verb">T_B</spanx> for <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">T_B</spanx> contains the updated <spanx style="verb">actc</spanx> and disclosed <spanx style="verb">act=EncodeVisibleChain([B])</spanx>.</t>
<t><spanx style="verb">B</spanx> presents <spanx style="verb">T_B</spanx> to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">C</spanx> validates the token and authorizes based only on the disclosed current actor
<spanx style="verb">B</spanx> and local policy. <spanx style="verb">C</spanx> does not infer prior-actor identity or count
from undisclosed information or from <spanx style="verb">actc</spanx> alone.</t>
</list>
</t>
</section>

<section anchor="example-5-verified-subset-disclosure" title="Example 5: Verified Subset Disclosure">
<t>Assume <spanx style="verb">A</spanx>, <spanx style="verb">B</spanx>, and <spanx style="verb">C</spanx> use the Verified Subset Disclosure profile.</t>
<t>
<list style="numbers">
<t><spanx style="verb">A</spanx> obtains bootstrap context, signs <spanx style="verb">chain_sig_A</spanx>, and receives <spanx style="verb">T_A</spanx> with
a recipient-specific <spanx style="verb">act</spanx>, or with no <spanx style="verb">act</spanx>, plus <spanx style="verb">actc</spanx> intended
for <spanx style="verb">B</spanx> according to local policy.</t>
<t><spanx style="verb">A</spanx> calls <spanx style="verb">B</spanx> and presents <spanx style="verb">T_A</spanx>.</t>
<t><spanx style="verb">B</spanx> validates the token and uses only the disclosed actor chain, if any, for actor-chain
authorization. If <spanx style="verb">act</spanx> is omitted, this specification provides no inline actor-chain
input beyond what is disclosed; any additional authorization inputs come from local policy.</t>
<t><spanx style="verb">B</spanx> signs <spanx style="verb">chain_sig_B</spanx> over the exact actor-visible chain that <spanx style="verb">B</spanx>
verified on the inbound hop, with <spanx style="verb">B</spanx> appended, and exchanges <spanx style="verb">T_A</spanx> at its
home Authorization Server alongside <spanx style="verb">chain_sig_B</spanx> as <spanx style="verb">actor_chain_step_proof</spanx> to obtain <spanx style="verb">T_B</spanx>
for <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">AS1</spanx> verifies that submitted chain state, applies disclosure policy for <spanx style="verb">C</spanx>,
and issues <spanx style="verb">T_B</spanx> with a recipient-specific <spanx style="verb">act</spanx>, or with no <spanx style="verb">act</spanx>,
and updated <spanx style="verb">actc</spanx>.</t>
<t><spanx style="verb">B</spanx> presents <spanx style="verb">T_B</spanx> to <spanx style="verb">C</spanx>.</t>
<t><spanx style="verb">C</spanx> validates the token and authorizes only on the disclosed actor chain, if any. If
<spanx style="verb">act</spanx> is omitted or does not disclose <spanx style="verb">B</spanx>, this specification provides no inline current-
actor disclosure for that hop, and <spanx style="verb">C</spanx> treats undisclosed actors as unavailable.</t>
<t>If later audit is needed, the verified actor-visible chain for the hop can
be reconstructed from retained step proofs together with exchange records
and, when subset disclosure omitted <spanx style="verb">act</spanx>, any retained Authorization-Server
workflow records needed to supply hidden continuity context.</t>
</list>
</t>
</section>
</section>

<section anchor="illustrative-wire-format-example" title="Illustrative Wire-Format Example">
<t>This section shows one abbreviated decoded JWT payload together with one
abbreviated decoded <spanx style="verb">actc</spanx> JWS payload. The values are
illustrative and signatures are omitted for readability.</t>

<section anchor="decoded-access-token-payload-example" title="Decoded Access Token Payload Example">

<figure><artwork type="json">{
  &quot;iss&quot;: &quot;https://as.example&quot;,
  &quot;sub&quot;: &quot;svc:planner&quot;,
  &quot;act&quot;: {
    &quot;iss&quot;: &quot;https://as.example&quot;,
    &quot;sub&quot;: &quot;svc:planner&quot;,
    &quot;act&quot;: {
      &quot;iss&quot;: &quot;https://as.example&quot;,
      &quot;sub&quot;: &quot;svc:orchestrator&quot;
    }
  },
  &quot;aud&quot;: &quot;https://api.example&quot;,
  &quot;exp&quot;: 1760000000,
  &quot;jti&quot;: &quot;2b2b6f0d3f0f4d7a8c4c3c4f9e9b1a10&quot;,
  &quot;acti&quot;: &quot;6cb5f0c14ab84718a69d96d31d95f3c4&quot;,
  &quot;actp&quot;: &quot;verified-full&quot;,
  &quot;actc&quot;: &quot;&lt;compact JWS string&gt;&quot;
}
</artwork></figure>

</section>

<section anchor="decoded-actc-jws-example" title="Decoded actc JWS Example">
<t>Protected header:</t>

<figure><artwork type="json">{&quot;alg&quot;:&quot;ES256&quot;,&quot;typ&quot;:&quot;act-commitment+jwt&quot;}
</artwork></figure>

<t>Payload:</t>

<figure><artwork type="json">{
  &quot;ctx&quot;: &quot;actor-chain-commitment-v1&quot;,
  &quot;iss&quot;: &quot;https://as.example&quot;,
  &quot;acti&quot;: &quot;6cb5f0c14ab84718a69d96d31d95f3c4&quot;,
  &quot;actp&quot;: &quot;verified-full&quot;,
  &quot;halg&quot;: &quot;sha-256&quot;,
  &quot;prev&quot;: &quot;SGlnaGx5SWxsdXN0cmF0aXZlUHJldkRpZ2VzdA&quot;,
  &quot;step_hash&quot;: &quot;z7mq8c0u9b2C0X5Q2m4Y1q3r7n6s5t4u3v2w1x0y9z8&quot;,
  &quot;curr&quot;: &quot;Vb8mR6b2vS5h6S8Y6j5X4r3w2q1p0n9m8l7k6j5h4g3&quot;
}
</artwork></figure>

<t>On the wire, the <spanx style="verb">actc</spanx> claim carries the usual compact-JWS
form:</t>

<figure><artwork type="text">BASE64URL(protected-header) &quot;.&quot; BASE64URL(payload) &quot;.&quot; BASE64URL(signature)
</artwork></figure>

</section>
</section>

<section anchor="canonicalization-test-vectors" title="Canonicalization Test Vectors">
<t>The following illustrative vectors are intended to reduce interoperability
failures caused by divergent canonicalization. They are not exhaustive, but
they provide concrete byte-for-byte examples for common JWT/JCS ActorID and
<spanx style="verb">target_context</spanx> inputs.</t>

<section anchor="jwt-jcs-actorid-example" title="JWT / JCS ActorID Example">
<t>Input object:</t>

<figure><artwork type="json">{&quot;iss&quot;:&quot;https://as.example&quot;,&quot;sub&quot;:&quot;svc:planner&quot;}
</artwork></figure>

<t>JCS serialization (UTF-8 bytes rendered as hex):</t>

<figure><artwork type="text">7b22697373223a2268747470733a2f2f61732e6578616d706c65222c22737562223a227376633a706c616e6e6572227d
</artwork></figure>

<t>SHA-256 over those bytes:</t>

<figure><artwork type="text">7a14a23707a3a723fd6437a4a0037cc974150e2d1b63f4d64c6022196a57b69f
</artwork></figure>

</section>

<section anchor="jwt-jcs-target-context-example" title="JWT / JCS target_context Example">
<t>Input object:</t>

<figure><artwork type="json">{&quot;aud&quot;:&quot;https://api.example&quot;,&quot;method&quot;:&quot;invoke&quot;,&quot;resource&quot;:&quot;calendar.read&quot;}
</artwork></figure>

<t>JCS serialization (UTF-8 bytes rendered as hex):</t>

<figure><artwork type="text">7b22617564223a2268747470733a2f2f6170692e6578616d706c65222c226d6574686f64223a22696e766f6b65222c227265736f75726365223a2263616c656e6461722e72656164227d
</artwork></figure>

<t>SHA-256 over those bytes:</t>

<figure><artwork type="text">911427869c76f397e096279057dd1396fe2eda1ac9e313b357d9cecc44aa811e
</artwork></figure>

</section>
</section>

<section anchor="implementation-conformance-checklist-informative" title="Implementation Conformance Checklist (Informative)">
<t>This checklist is an implementation aid. Conformance is defined in
{{conformance}} and in the normative sections referenced there.</t>
<t>Implementers can use the following checklist to verify that they have
addressed the main requirements for each supported profile:</t>
<texttable>
<ttcol>Requirement</ttcol>
<ttcol>Draft section reference</ttcol>
<ttcol>Implemented [ ]</ttcol>


<c>Stable generation, uniqueness, retention-period non-reuse, and preservation of acti, using a CSPRNG with at least 122 bits of entropy</c>
<c>{{actor-chain-identifier}}</c>
<c>[ ]</c>

<c>Local policy for actor authentication or presenter binding, if relied upon</c>
<c>{{actor-authentication}}</c>
<c>[ ]</c>

<c>Exact ActorID equality over (iss, sub)</c>
<c>{{actor-identity}}</c>
<c>[ ]</c>

<c>Canonical serialization for all proof and commitment inputs</c>
<c>{{canonicalization}}; {{target-context}}; {{canonicalization-test-vectors}}</c>
<c>[ ]</c>

<c>Single logical chain-extending recipient and target-context comparison</c>
<c>{{target-context}}; {{verified-common-processing}}</c>
<c>[ ]</c>

<c>Intended-recipient validation during token exchange</c>
<c>{{intended-recipient}}</c>
<c>[ ]</c>

<c>Replay and freshness handling for tokens and step proofs</c>
<c>{{replay-freshness}}</c>
<c>[ ]</c>

<c>Configurable chain-depth enforcement</c>
<c>{{chain-depth-limits}}</c>
<c>[ ]</c>

<c>Exact append-only checks for full-disclosure profiles</c>
<c>{{declared-full}}; {{verified-full}}</c>
<c>[ ]</c>

<c>Correct ordered-subsequence validation for subset-disclosure profiles</c>
<c>{{declared-subset}}; {{verified-subset}}</c>
<c>[ ]</c>

<c>Current-actor-only validation for actor-only profiles</c>
<c>{{declared-actor-only}}; {{verified-actor-only}}</c>
<c>[ ]</c>

<c>Exact commitment verification for verified profiles</c>
<c>{{commitment-function}}; {{verified-full}}; {{verified-subset}}; {{verified-actor-only}}</c>
<c>[ ]</c>

<c>Proof-key binding between ActorID and proof signer under local trust policy</c>
<c>{{proof-keys}}</c>
<c>[ ]</c>

<c>Non-broadening Refresh-Exchange processing, if supported</c>
<c>{{refresh-exchange}}</c>
<c>[ ]</c>

<c>Correct binding and one-time redemption of bootstrap context for verified workflows</c>
<c>{{verified-bootstrap-context}}; {{verified-initial-step}}</c>
<c>[ ]</c>

<c>Policy for when hop_ack is optional or required</c>
<c>{{hop-ack}}</c>
<c>[ ]</c>

<c>Preserve-state handling for cross-domain re-issuance and Refresh-Exchange, including represented-current-actor authorization</c>
<c>{{preserve-state-exchanges}}; {{cross-domain-reissuance}}; {{refresh-exchange}}</c>
<c>[ ]</c>

<c>Cross-domain preserve-or-reject handling for unmapped target_context members</c>
<c>{{target-context}}; {{cross-domain-reissuance}}</c>
<c>[ ]</c>

<c>Privacy-preserving handling of logs and error messages</c>
<c>{{error-handling}}; {{privacy}}</c>
<c>[ ]</c>

</texttable></section>

<section anchor="future-considerations" title="Future Considerations">

<section anchor="terminal-receipts-and-result-attestations" title="Terminal Receipts and Result Attestations">
<t>This specification defines special handling for the first actor in order to
initialize chain state. It does not define corresponding terminal-hop semantics
for a final recipient that performs work locally and does not extend the chain
further.</t>
<t>Deployments that need richer terminal execution or result evidence can compose
this specification with companion SPICE provenance work, such as Intent Chain
<xref target="I-D.draft-mw-spice-intent-chain"></xref> and Inference Chain
<xref target="I-D.draft-mw-spice-inference-chain"></xref>, to provide complementary WHAT and HOW
evidence. However, this specification itself does not define the terminal
receipt/execution/result artifact or the precise composition rules by which
such companion artifacts satisfy terminal-hop evidence requirements.</t>
<t>Future work could define:</t>
<t>
<list style="symbols">
<t>a terminal receipt proving that the recipient accepted the request;</t>
<t>an execution attestation proving that the recipient executed a specific
operation; and</t>
<t>a result attestation binding an outcome or result digest to the final
commitment state.</t>
</list>
</t>
</section>

<section anchor="bootstrap-receipts-and-portable-initial-state-evidence" title="Bootstrap Receipts and Portable Initial-State Evidence">
<t>This specification does not define a bootstrap-signed receipt artifact. Later
audit of bootstrap processing therefore relies on Authorization Server records,
the bootstrap response, the initial step proof, and the first issued profiled access token.</t>
<t>Future work could define a portable bootstrap receipt or bootstrap
attestation artifact if deployments need independently portable evidence of
workflow initialization outside Authorization Server logs.</t>
</section>

<section anchor="receiver-acceptance-and-unsolicited-victim-mitigation" title="Receiver Acceptance and Unsolicited Victim Mitigation">
<t>This specification deliberately does not append a recipient merely because that
recipient was contacted. It also defines an optional <spanx style="verb">hop_ack</spanx> extension that
lets a recipient prove accepted responsibility for a hop.</t>
<t>However, this specification still does not by itself prevent a malicious actor
from sending a validly issued token to an unsolicited victim service. Future
work can define stronger receiver-driven protections, including:</t>
<t>
<list style="symbols">
<t>stronger result attestations for completed terminal work;</t>
<t>a challenge-response model for high-risk terminal hops; and</t>
<t>recipient-issued nonces or capabilities that would need to be bound into
the final accepted hop.</t>
</list>
</t>
</section>

<section anchor="recipient-protected-disclosure-mechanisms" title="Recipient-Protected Disclosure Mechanisms">
<t>The normative subset-disclosure rules in Privacy Requirements and
Considerations define the base disclosed <spanx style="verb">act</spanx> representation. Future work could
specify recipient-protected or selectively revealable presentation mechanisms,
such as SD-JWT <xref target="RFC9901"></xref>, COSE/CBOR bindings, encrypted envelopes, or
zero-knowledge techniques, without changing the base disclosure semantics.</t>
</section>

<section anchor="semantic-equivalent-commitment-inputs" title="Semantic-Equivalent Commitment Inputs">
<t>This version binds commitment linkage to exact compact-JWS proof bytes.
Future work could define an optional semantic-equivalence commitment input,
for example by hashing a canonical semantic proof object while still retaining
artifact-byte evidence for provenance and non-repudiation.</t>
</section>

<section anchor="branching-and-fan-out" title="Branching and Fan-Out">
<t>This specification defines one disclosed path per issued token and does not
standardize merge or sibling-discovery semantics across multiple descendants
that share earlier workflow history.</t>
<t>An Authorization Server could nevertheless mint multiple accepted successor
tokens from one prior accepted state. Such branching is represented across
multiple tokens, not inside one token's nested <spanx style="verb">act</spanx> structure. Later
reconstruction of the resulting call graph is primarily a forensic or legal-
audit concern.</t>
<t>Future work could define explicit branch identifiers, parent-child workflow
correlation, tree-structured commitment verification, inclusion proofs, partial
disclosure across branches, and later merge behavior. Such future work could
also help correlate related <spanx style="strong">WHO</spanx>, <spanx style="strong">WHAT</spanx>, and <spanx style="strong">HOW</spanx> evidence across
companion Actor Chain, Intent Chain <xref target="I-D.draft-mw-spice-intent-chain"></xref>, and
Inference Chain <xref target="I-D.draft-mw-spice-inference-chain"></xref> deployments.</t>
</section>

<section anchor="evidence-discovery-and-governance-interoperability" title="Evidence Discovery and Governance Interoperability">
<t>Proof-bound profiles derive much of their value from later verification of
step proofs and exchange records. Future work could standardize interoperable
evidence discovery, retention, and verification-material publication.</t>
<t>Any such specification should define, at minimum, evidence object typing,
authorization and privacy controls for cross-domain retrieval, stable lookup
keys such as <spanx style="verb">jti</spanx> or <spanx style="verb">acti</spanx>, error handling, and retention expectations.</t>
</section>
</section>

<section anchor="use-cases" title="Out-of-Scope Use Cases">
<t>This section is informative. Per-hop request binding - cryptographic
evidence that a request physically traversed each claimed hop - is out of
scope for this document. End-to-end path proof in the data plane, such as
nested message signatures applied by each actor in turn, requires a companion
mechanism such as <xref target="RFC9421"></xref> or <xref target="RFC9449"></xref> and is not addressed here.</t>
</section>

<section anchor="relationship-to-rfc8693" title="Relationship to RFC 8693: Detailed Comparison">
<t>This section is informative. <xref target="RFC8693"></xref> defines token exchange and
introduces <spanx style="verb">act</spanx> for delegation, with nested <spanx style="verb">act</spanx> described as a convention.
It treats <spanx style="verb">act</spanx> as optional and defines no normative processing rules for
construction, extension, or validation across a sequence of exchanges. This
document does not replace <xref target="RFC8693"></xref>; with <spanx style="verb">actp</spanx> absent, it adds no new
requirements. With <spanx style="verb">actp</spanx> present, the following constraints apply:</t>
<texttable>
<ttcol><xref target="RFC8693"></xref> behavior</ttcol>
<ttcol>This document</ttcol>


<c>act is optional.</c>
<c>Processing follows the rules here when used; actp makes the governing semantics explicit.</c>

<c>Nested act is a convention.</c>
<c>Nested act is normative structural form; recipients can rely on its shape.</c>

<c>No append-only rule.</c>
<c>Prior actors are not inserted, reordered, deleted, or modified.</c>

<c>No cross-exchange correlation.</c>
<c>acti provides a stable workflow instance identifier across hops.</c>

<c>No integrity binding beyond the issuing Authorization Server's signature.</c>
<c>actc provides cumulative commitment state that can survive Authorization Server boundary crossings.</c>

<c>No prior-actor disclosure control.</c>
<c>actp profiles scope disclosure; the actor-chain disclosure boundary prevents leakage through other claims.</c>

<c>No disclosure-regime signal to the recipient.</c>
<c>actp allows recipients to apply the correct interpretation.</c>

</texttable></section>

</middle>

<back>
<references title="Informative References">
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml-ids/reference.I-D.draft-mw-spice-intent-chain.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9421.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml-ids/reference.I-D.ietf-spice-arch.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8785.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8414.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7519.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml-ids/reference.I-D.draft-mw-spice-inference-chain.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml-ids/reference.I-D.mw-oauth-tls-session-bound-tokens.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml-ids/reference.I-D.ietf-oauth-identity-chaining.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7662.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6920.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9901.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml-ids/reference.I-D.draft-mw-spice-transitive-attestation.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9449.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8693.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7515.xml"?>
<reference anchor="IANA.Hash.Algorithms" target="https://www.iana.org/assignments/named-information">
  <front>
    <title>Named Information Hash Algorithm Registry</title>
    <author>
      <organization>IANA</organization>
    </author>
    <date></date>
  </front>
</reference>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7518.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6838.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6749.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml-ids/reference.I-D.ietf-spice-s2s-protocol.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8174.xml"?>
<?rfc include="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9334.xml"?>
</references>

</back>

</rfc>
