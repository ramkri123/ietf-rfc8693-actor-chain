<?xml version="1.0" encoding="utf-8"?>
<!-- name="GENERATOR" content="github.com/mmarkdown/mmark Mmark Markdown Processor - mmark.miek.nl" -->
<rfc version="3" ipr="trust200902" docName="draft-mw-spice-actor-chain-00" submissionType="IETF" category="info" xml:lang="en" xmlns:xi="http://www.w3.org/2001/XInclude" indexInclude="true">

<front>
<title abbrev="SPICE-ACTOR-CHAIN">Cryptographically Verifiable Actor Chain for OAuth 2.0 Token Exchange</title><seriesInfo value="draft-mw-spice-actor-chain-00" stream="IETF" status="informational" name="Internet-Draft"></seriesInfo>
<author initials="A." surname="Prasad" fullname="A Prasad"><organization>Oracle</organization><address><postal><street></street>
</postal><email>a.prasad@oracle.com</email>
</address></author><author initials="R." surname="Krishnan" fullname="Ram Krishnan"><organization>JPMorgan Chase &amp; Co</organization><address><postal><street></street>
</postal><email>ramkri123@gmail.com</email>
</address></author><author initials="D." surname="Lopez" fullname="Diego R. Lopez"><organization>Telefonica</organization><address><postal><street></street>
</postal><email>diego.r.lopez@telefonica.com</email>
</address></author><author initials="S." surname="Addepalli" fullname="Srinivasa Addepalli"><organization>Aryaka</organization><address><postal><street></street>
</postal><email>srinivasa.addepalli@aryaka.com</email>
</address></author><date/>
<area>Security</area>
<workgroup>SPICE</workgroup>
<keyword>actor chain</keyword>
<keyword>spice</keyword>
<keyword>rfc8693</keyword>
<keyword>token exchange</keyword>
<keyword>workload identity</keyword>
<keyword>delegation</keyword>
<keyword>AI agents</keyword>

<abstract>
<t>This document defines an extension to OAuth 2.0 Token Exchange {{!RFC8693}} that addresses the problem of <strong>Delegation Auditability Gaps</strong> in multi-hop service environments. Current standards treat prior actors in a delegation chain as &quot;informational only,&quot; providing no cryptographic proof of the actual delegation path. This document proposes a new <tt>actor_chain</tt> claim — a <strong>Cryptographically Verifiable Actor Chain</strong> — that replaces the informational-only nested <tt>act</tt> claim with a tamper-evident, ordered record of all actors. This solution enables high-assurance data-plane policy enforcement and forensic auditability, particularly for dynamic AI agent-to-agent workloads where susceptibility to <strong>prompt injection attacks</strong> can lead to unauthorized delegation paths.</t>
</abstract>

</front>

<middle>

<section anchor="introduction"><name>Introduction</name>
<t>This document defines an extension to OAuth 2.0 Token Exchange {{!RFC8693}} to support high-assurance identity delegation through cryptographically verifiable actor chains.</t>
<t>In modern multi-service and AI-agent environments, a workload often delegates its authority to another agent, which may in turn delegate to others. While {{!RFC8693}} provides the <tt>act</tt> (actor) claim to represent delegation, it explicitly restricts prior actors in a nested chain to be &quot;informational only,&quot; excluding them from access control considerations. This creates a significant <strong>Delegation Auditability Gap</strong>: Relying Parties cannot verify the full path of authority, and attackers can potentially hide lateral movement or <strong>prompt injection-induced hijacking</strong> within unverified informational claims.</t>
<t>By providing a standardized <strong>Cryptographically Verifiable Actor Chain</strong>, this extension replaces the informal nested <tt>act</tt> structure with a policy-enforceable, ordered, and tamper-evident record of all participants. This establishes a <strong>delegation</strong> axis of accountability, ensuring that any service in a global delegation chain can be verified for identity, integrity, and (optionally) physical residency.</t>
<t>This solution addresses several critical gaps in {{!RFC8693}}:</t>

<ol spacing="compact">
<li><strong>Cryptographic Audit Trail</strong>: Proves that each prior actor actually participated in the delegation chain and that the sequence has not been tampered with.</li>
<li><strong>Data-Plane Policy Enforcement</strong>: Enables Relying Parties to write fine-grained authorization policies based on any actor in the path (e.g., &quot;originating actor must be X&quot;).</li>
<li><strong>Dynamic AI Agent Topologies</strong>: Provides a scalable architecture for the unpredictable and deep delegation chains common in autonomous agent networks.</li>
<li><strong>Data-Plane Efficiency</strong>: Uses a flat, ordered array structure optimized for high-throughput parsing and indexing in cloud-native proxies (e.g., Envoy).</li>
</ol>
<t>This specification is part of a three-axis &quot;Truth Stack&quot; for AI agent governance:</t>
<table>
<thead>
<tr>
<th align="left">Specification</th>
<th align="left">Axis</th>
<th align="left">Question Answered</th>
<th align="left">STRIDE Coverage</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Actor Chain</strong> (this document)</td>
<td align="left">Identity</td>
<td align="left">WHO delegated to whom?</td>
<td align="left">Spoofing, Repudiation, Elevation of Privilege</td>
</tr>

<tr>
<td align="left"><strong>Intent Chain</strong> ({{!I-D.draft-mw-spice-intent-chain}})</td>
<td align="left">Content</td>
<td align="left">WHAT was produced and transformed?</td>
<td align="left">Repudiation, Tampering</td>
</tr>

<tr>
<td align="left"><strong>Inference Chain</strong> ({{!I-D.draft-mw-spice-inference-chain}})</td>
<td align="left">Computation</td>
<td align="left">HOW was the output computed?</td>
<td align="left">Spoofing (computational), Tampering (model)</td>
</tr>
</tbody>
</table><table>
<thead>
<tr>
<th align="left">Chain</th>
<th align="left">Plane</th>
<th align="left">Token Content</th>
<th align="left">Full Chain</th>
<th align="left">Primary Consumer</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Actor</strong></td>
<td align="left">Data Plane</td>
<td align="left">Full chain inline</td>
<td align="left">In token</td>
<td align="left">Every Relying Party (real-time authorization)</td>
</tr>

<tr>
<td align="left"><strong>Intent</strong></td>
<td align="left">Audit Plane</td>
<td align="left">merkle root only</td>
<td align="left">External registry</td>
<td align="left">Audit systems, forensic investigators</td>
</tr>

<tr>
<td align="left"><strong>Inference</strong></td>
<td align="left">Audit Plane</td>
<td align="left">merkle root only</td>
<td align="left">External registry</td>
<td align="left">Auditors, compliance systems</td>
</tr>
</tbody>
</table><t>This extension is designed to be backward-compatible and format-agnostic, supporting both JSON/JWS (JWT {{!RFC7519}}) and CBOR/COSE (CWT {{!RFC8392}}) representations.</t>
</section>

<section anchor="terminology"><name>Terminology</name>
<t>The key words &quot;MUST&quot;, &quot;MUST NOT&quot;, &quot;REQUIRED&quot;, &quot;SHALL&quot;, &quot;SHALL NOT&quot;, &quot;SHOULD&quot;, &quot;SHOULD NOT&quot;, &quot;RECOMMENDED&quot;, &quot;NOT RECOMMENDED&quot;, &quot;MAY&quot;, and &quot;OPTIONAL&quot; in this document are to be interpreted as described in BCP 14 {{!RFC2119}} {{!RFC8174}} when, and only when, they appear in all capitals, as shown here.</t>
<t>This document leverages the terminology defined in OAuth 2.0 Token Exchange {{!RFC8693}}, the SPICE Architecture {{!I-D.ietf-spice-arch}}, and the RATS Architecture {{!RFC9334}}.</t>

<dl spacing="compact">
<dt>Actor Chain:</dt>
<dd>A Cryptographically Verifiable Actor Chain — an ordered sequence of Actor Chain Entries representing the complete delegation path from the originating actor to the current actor. The token carries identity entries inline and a merkle root (<tt>actor_chain_root</tt>) binding those entries to per-actor cryptographic signatures stored in an external registry.</dd>
<dt>Actor Chain Entry:</dt>
<dd>A JSON object or CBOR map identifying a single actor in the delegation chain, including its identity claims (<tt>sub</tt>, <tt>iss</tt>, <tt>iat</tt>) and an optional Proof of Residency (<tt>por</tt>). The token carries these entries inline for data-plane policy enforcement.</dd>
<dt>Actor Chain Root:</dt>
<dd>The merkle root hash of the complete set of per-actor signatures (<tt>chain_sig</tt> values) in the actor chain, included in the OAuth token as the <tt>actor_chain_root</tt> claim. This binds the data-plane token to the audit-plane evidence.</dd>
<dt>Actor Chain Registry:</dt>
<dd>A service endpoint that stores the full per-actor signature evidence (including <tt>chain_digest</tt> and <tt>chain_sig</tt> for each entry). Referenced by the <tt>actor_chain_registry</tt> claim in the token. Implementations MAY use a SCITT transparency log {{!I-D.ietf-scitt-architecture}} or equivalent ordered log.</dd>
<dt>Chain Depth:</dt>
<dd>The total number of Actor Chain Entries in an actor chain. Used by policy engines to enforce maximum delegation depth.</dd>
<dt>Proof of Residency (PoR):</dt>
<dd>A cryptographic proof (as defined in {{!I-D.draft-mw-spice-transitive-attestation}}) binding a workload to a specific, verified local environment. When present in an Actor Chain Entry, it provides hardware-rooted assurance of the actor's execution context.</dd>
</dl>
</section>

<section anchor="the-problem-rfc-8693-actor-limitations"><name>The Problem: RFC 8693 Actor Limitations</name>

<section anchor="single-actor-semantics"><name>Single-Actor Semantics</name>
<t>Section 4.1 defines the <tt>act</tt> claim as a JSON object identifying <strong>the</strong> current actor. While nesting is permitted to represent prior actors, the specification explicitly limits their utility. Only the outermost <tt>act</tt> claim—representing the current actor—is relevant for access control. All prior actors exist solely for informational purposes.</t>
<t>This design was appropriate for traditional web service delegation where chains are short (typically one or two hops) and the identity of the immediate caller is sufficient for authorization. It is insufficient for the emerging class of workloads described below.</t>
</section>

<section anchor="ai-agent-delegation-chains"><name>AI Agent Delegation Chains</name>
<t>Modern AI systems increasingly operate as networks of specialized agents. A typical interaction may involve:</t>

<artwork><![CDATA[User -> Orchestrator Agent -> Planning Agent -> Tool Agent -> Data API
]]></artwork>
<t>At each hop, the agent performs a token exchange ({{!RFC8693}}) to obtain credentials appropriate for calling the next service. Under current {{!RFC8693}} semantics, by the time the request reaches the Data API, only the Tool Agent is identified as the actor. The Orchestrator Agent and Planning Agent—which may have been manipulated via <strong>prompt injection</strong> into delegating authority they should not have—are invisible to policy enforcement.</t>
<t>This creates several concrete risks:</t>

<ul spacing="compact">
<li><strong>Lateral Movement</strong>: A compromised agent deep in the chain can impersonate the authority of the originating actor without any cryptographic evidence of the actual delegation path.</li>
<li><strong>Policy Bypass</strong>: Fine-grained policies like &quot;only allow data access when the orchestrator is a known, trusted entity&quot; cannot be expressed because the orchestrator's identity is not available for policy evaluation at the data plane.</li>
<li><strong>Audit Gaps</strong>: Post-incident forensic analysis cannot reliably reconstruct the delegation path because the nested <tt>act</tt> claims are self-reported and unsigned.</li>
</ul>
</section>

<section anchor="structural-limitations-of-nested-act"><name>Structural Limitations of Nested <tt>act</tt></name>
<t>Beyond the semantic restriction, the nested object structure of <tt>act</tt> in {{!RFC8693}} has practical limitations:</t>

<ol spacing="compact">
<li><strong>Parsing Complexity</strong>: Each prior actor requires traversing one additional level of JSON nesting. In high-throughput data-plane proxies (e.g., Envoy, Istio sidecars), deep nesting imposes parsing overhead.</li>
<li><strong>Indexing</strong>: It is not possible to efficiently query &quot;the actor at position N&quot; without recursively unwinding the nested structure.</li>
<li><strong>Size Predictability</strong>: The depth of nesting is unbounded, making it difficult to predict token sizes and allocate parsing buffers.</li>
<li><strong>No Integrity</strong>: Each nested <tt>act</tt> is a plain JSON object with no signature or hash binding. Any intermediary could insert, remove, or reorder prior actors without detection.</li>
</ol>
</section>
</section>

<section anchor="the-solution-the-cryptographically-verifiable-actor-chain-claim"><name>The Solution: The Cryptographically Verifiable <tt>actor_chain</tt> Claim</name>

<section anchor="overview"><name>Overview</name>
<t>This document defines a new claim, <tt>actor_chain</tt>, that provides a Cryptographically Verifiable Actor Chain. When used in a JWT, its value is a JSON array of Actor Chain Entries. When used in a CWT, its value is a CBOR array of Actor Chain Entries. The array is ordered chronologically: index 0 represents the originating actor, and the last index represents the current actor.</t>
<t>The Authorization Server (AS) validates each actor at token exchange time and constructs the <tt>actor_chain</tt>. Each actor cryptographically signs the cumulative chain state at its point of participation, producing a per-entry <tt>chain_sig</tt>. The full signed entries are stored in an external registry (the Actor Chain Registry), while the token carries only the identity entries and a merkle root (<tt>actor_chain_root</tt>) binding them to the signed evidence. The AS's signature over the entire token (JWS or COSE) provides data-plane integrity.</t>
<t>This architecture separates data-plane concerns (fast access control using identity entries) from audit-plane concerns (per-actor non-repudiation using stored signatures), following the same pattern as the companion Intent Chain {{!I-D.draft-mw-spice-intent-chain}} and Inference Chain {{!I-D.draft-mw-spice-inference-chain}} specifications:</t>
<table>
<thead>
<tr>
<th align="left">Chain</th>
<th align="left">Question</th>
<th align="left">Token (data plane)</th>
<th align="left">Registry (audit plane)</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Actor Chain</strong> (this document)</td>
<td align="left">WHO participated?</td>
<td align="left">Identity entries + merkle root</td>
<td align="left">Per-actor <tt>chain_sig</tt></td>
</tr>

<tr>
<td align="left"><strong>Intent Chain</strong> {{!I-D.draft-mw-spice-intent-chain}}</td>
<td align="left">WHAT was produced?</td>
<td align="left">Content refs + merkle root</td>
<td align="left">Per-entry <tt>intent_sig</tt></td>
</tr>

<tr>
<td align="left"><strong>Inference Chain</strong> {{!I-D.draft-mw-spice-inference-chain}}</td>
<td align="left">HOW was it computed?</td>
<td align="left">Model refs + merkle root</td>
<td align="left">Per-entry <tt>inference_sig</tt></td>
</tr>
</tbody>
</table><t>By requiring per-actor signatures in all three chains and storing them in registries with merkle roots in tokens, a Relying Party obtains O(1) data-plane verification while retaining full per-actor non-repudiation for audit.</t>
</section>

<section anchor="claim-definition"><name>Claim Definition</name>

<section anchor="token-claims-data-plane"><name>Token Claims (Data Plane)</name>
<t>The following claims are included in the OAuth token:</t>

<dl spacing="compact">
<dt>actor_chain:</dt>
<dd>REQUIRED. A JSON array of Actor Chain Entry objects. Each element is a JSON object with the following members.</dd>
<dt>actor_chain_root:</dt>
<dd>RECOMMENDED. A string containing the merkle root hash (SHA-256, Base64url-encoded) of the complete set of per-actor chain signatures. This binds the inline identity entries to the signed evidence stored in the Actor Chain Registry. Deployments that do not require audit-plane non-repudiation MAY omit this claim; in that case, the AS's JWT signature provides data-plane integrity for the <tt>actor_chain</tt> entries. Deployments requiring governance alignment with the Intent Chain {{!I-D.draft-mw-spice-intent-chain}} and Inference Chain {{!I-D.draft-mw-spice-inference-chain}} specifications MUST include this claim.</dd>
<dt>actor_chain_registry:</dt>
<dd>RECOMMENDED. A URI identifying the Actor Chain Registry endpoint where the full per-actor signature evidence can be retrieved. MUST be present whenever <tt>actor_chain_root</tt> is present.</dd>
<dt>sid:</dt>
<dd>RECOMMENDED. A string identifying the session to which this token belongs. The Actor Chain Registry, Intent Registry ({{!I-D.draft-mw-spice-intent-chain}}), and Inference Registry ({{!I-D.draft-mw-spice-inference-chain}}) are partitioned by this identifier. MUST be present whenever <tt>actor_chain_registry</tt> is present. When deployed alongside the Intent Chain, the <tt>sid</tt> value MUST equal the <tt>session.session_id</tt> value defined in {{!I-D.draft-mw-spice-intent-chain}}. The same <tt>sid</tt> is carried forward during each token exchange, ensuring all registry entries for a given interaction can be retrieved as a unit.</dd>
</dl>
<t>To support diverse privacy requirements, Selective Disclosure (SD-JWT) is configurable at two levels of granularity:
- <strong>Per-Actor</strong>: An Authorization Server MAY choose to hide entire Actor Chain Entries or only specific actors in the chain.
- <strong>Per-Field</strong>: Within a single Actor Chain Entry, a subset of members (e.g., <tt>sub</tt>) MAY be hidden using an <tt>_sd</tt> claim while others (e.g., <tt>iat</tt> or <tt>por</tt>) remain in cleartext.</t>
</section>

<section anchor="actor-chain-entry-members"><name>Actor Chain Entry Members</name>

<dl spacing="compact">
<dt>sub:</dt>
<dd>REQUIRED (or selectively disclosed). A string identifying the actor, as defined in {{!RFC7519}} Section 4.1.2.</dd>
<dt>iss:</dt>
<dd>REQUIRED (or selectively disclosed). A string identifying the issuer of the actor's identity, as defined in {{!RFC7519}} Section 4.1.1.</dd>
<dt>iat:</dt>
<dd>REQUIRED (or selectively disclosed). The time at which this actor was appended to the chain, represented as a NumericDate as defined in {{!RFC7519}} Section 4.1.6.</dd>
<dt>por:</dt>
<dd>OPTIONAL. A JSON object containing a Proof of Residency binding this actor to a verified execution environment. The structure of this object is defined in {{!I-D.draft-mw-spice-transitive-attestation}}.</dd>
</dl>
</section>

<section anchor="registry-entry-members-audit-plane"><name>Registry Entry Members (Audit Plane)</name>
<t>The following fields are stored per-entry in the Actor Chain Registry and are NOT included in the token:</t>

<dl spacing="compact">
<dt>chain_digest:</dt>
<dd>REQUIRED. A Base64url-encoded cumulative cryptographic hash (SHA-256). For an entry at index <tt>N</tt>, the hash is computed over the canonical serialization of the union of the current entry's identity claims (e.g., <tt>sub</tt>, <tt>iss</tt>, <tt>iat</tt>, <tt>_sd</tt> hashes) and the <tt>chain_digest</tt> of the preceding entry (index <tt>N-1</tt>). For the entry at index 0, the hash is computed over its identity claims alone.</dd>
</dl>
<blockquote><t>When Selective Disclosure is used, the SD-JWT Disclosure strings (the cleartext salts/values) MUST NOT be included in the canonical serialization used for hashing. The <tt>chain_digest</tt> is computed exclusively over the Actor Chain Entry object, which contains the stable <tt>_sd</tt> hashes.</t>
</blockquote>
<dl spacing="compact">
<dt>chain_sig:</dt>
<dd>REQUIRED. A compact JWS {{!RFC7515}} or COSE_Sign1 {{!RFC9052}} signature produced by this actor's private key over the <tt>chain_digest</tt> value. The JWS header MUST include the <tt>jwk</tt> or <tt>kid</tt> member to identify the signing key.</dd>
</dl>
</section>
</section>

<section anchor="example-token-data-plane"><name>Example Token (Data Plane)</name>
<t>The token carries identity entries inline for policy enforcement and a merkle root binding them to per-actor signatures stored in the registry. No per-actor signatures appear in the token itself.</t>

<sourcecode type="json"><![CDATA[{
  "aud": "https://data-api.example.com",
  "iss": "https://auth.example.com",
  "exp": 1700000100,
  "nbf": 1700000000,
  "sub": "user@example.com",
  "sid": "session-123",
  "actor_chain_root": "sha256:9f86d08...",
  "actor_chain_registry":
    "https://registry.example.com/actor-chains/session-123",
  "actor_chain": [
    {
      "sub": "https://orchestrator.example.com",
      "iss": "https://auth.example.com",
      "iat": 1700000010,
      "por": {
        "wia_kid": "spiffe://example.com/wia/node-1",
        "env_hash": "sha256:abc123..."
      }
    },
    {
      "sub": "https://planner.example.com",
      "iss": "https://auth.example.com",
      "iat": 1700000030
    },
    {
      "sub": "https://tool-agent.example.com",
      "iss": "https://auth.example.com",
      "iat": 1700000050,
      "por": {
        "wia_kid": "spiffe://example.com/wia/node-3",
        "env_hash": "sha256:ghi789..."
      }
    }
  ]
}
]]></sourcecode>
</section>

<section anchor="example-registry-entries-audit-plane"><name>Example Registry Entries (Audit Plane)</name>
<t>The Actor Chain Registry stores the full per-actor signature evidence:</t>

<sourcecode type="json"><![CDATA[{
  "session_id": "session-123",
  "actor_chain_root": "sha256:9f86d08...",
  "entries": [
    {
      "sub": "https://orchestrator.example.com",
      "iss": "https://auth.example.com",
      "iat": 1700000010,
      "chain_digest": "sha256:mno345...",
      "chain_sig": "eyJhbGciOiJFUzI1NiIsImt..."
    },
    {
      "sub": "https://planner.example.com",
      "iss": "https://auth.example.com",
      "iat": 1700000030,
      "chain_digest": "sha256:def456...",
      "chain_sig": "eyJhbGciOiJFUzI1NiIsImt..."
    },
    {
      "sub": "https://tool-agent.example.com",
      "iss": "https://auth.example.com",
      "iat": 1700000050,
      "chain_digest": "sha256:jkl012...",
      "chain_sig": "eyJhbGciOiJFUzI1NiIsImt..."
    }
  ]
}
]]></sourcecode>
<t>The merkle tree is constructed from the <tt>chain_sig</tt> values as leaf nodes, ordered by chain index (index 0 is the leftmost leaf). The cumulative hash-chain structure (<tt>chain_digest</tt>) ensures that this ordering is cryptographically enforced — reordering entries changes their digests, which changes their signatures, which changes the merkle root. The resulting root hash is included in the token as <tt>actor_chain_root</tt>. An auditor can reconstruct the merkle tree from the registry entries and verify it matches the root in the token.</t>
</section>

<section anchor="token-exchange-flow"><name>Token Exchange Flow</name>
<t>When an actor (Service B) receives a token containing an <tt>actor_chain</tt> and needs to call a downstream service (Service C), the following token exchange flow occurs:</t>

<ol spacing="compact">
<li><strong>Service B</strong> computes its <tt>chain_digest</tt> over the existing chain entries from the received token and signs it with its own key, producing <tt>chain_sig</tt>.</li>
<li><strong>Service B</strong> sends a token exchange request to the Authorization Server (AS) per {{!RFC8693}} Section 2.1.</li>
<li>The <tt>subject_token</tt> contains the existing <tt>actor_chain</tt>.</li>
<li>The <tt>actor_token</tt> identifies Service B and includes its <tt>chain_digest</tt> and <tt>chain_sig</tt>.</li>
<li><t>The AS validates the existing <tt>actor_chain</tt>:</t>

<ul spacing="compact">
<li>Verifies the JWT signature on the <tt>subject_token</tt>.</li>
<li>Validates actor identities through its own policy (e.g., client registration, mTLS certificate).</li>
<li>Enforces any <tt>max_chain_depth</tt> policy.</li>
</ul></li>
<li>The AS validates Service B's <tt>chain_digest</tt> and <tt>chain_sig</tt>.</li>
<li>The AS stores Service B's signed entry (<tt>chain_digest</tt>, <tt>chain_sig</tt>) in the Actor Chain Registry.</li>
<li>The AS recomputes the merkle root over all <tt>chain_sig</tt> values (existing + Service B's).</li>
<li><t>The AS constructs a new token with:</t>

<ul spacing="compact">
<li>The extended <tt>actor_chain</tt> array (identity entries only: <tt>sub</tt>, <tt>iss</tt>, <tt>iat</tt>, optional <tt>por</tt>).</li>
<li>The updated <tt>actor_chain_root</tt> (new merkle root).</li>
<li>The <tt>actor_chain_registry</tt> URI.</li>
</ul></li>
<li>The AS signs the entire JWT and issues the token.</li>
</ol>

<section anchor="disclosure-propagation-in-sd-jwt"><name>Disclosure Propagation in SD-JWT</name>
<t>When Selective Disclosure (SD-JWT) {{!I-D.ietf-oauth-selective-disclosure-jwt}} is used for any Actor Chain Entry, the Authorization Server (AS) acts as the Discloser during token exchange. The propagation of the underlying cleartext identities is managed as follows:</t>

<ol spacing="compact">
<li><strong>Disclosure Inclusion</strong>: When the AS issues a token for a downstream recipient (Service B), it determines based on policy which hidden identifiers in the <tt>actor_chain</tt> should be visible to that recipient. For each visible actor, the AS appends the corresponding SD-JWT Disclosure string (containing the salt and cleartext value) to the issued token.</li>
<li><strong>Hop-by-Hop Visibility</strong>: In a chain <tt>a -&gt; b -&gt; c -&gt; d -&gt; e</tt>, if <tt>a</tt> is to be visible to <tt>b</tt>, <tt>c</tt>, and <tt>d</tt>, the AS includes <tt>a</tt>'s disclosure string in the tokens issued to <tt>b</tt>, <tt>c</tt>, and <tt>d</tt> during their respective token exchanges.</li>
<li><strong>Selective Redaction</strong>: When the final exchange occurs for recipient <tt>e</tt> (the end of the trust zone), the AS simply omits the disclosure string for <tt>a</tt> from the issued token.</li>
<li><strong>Validation</strong>: Each recipient in the chain can independently verify the hash of any disclosed claim against the <tt>_sd</tt> array in the Actor Chain Entry. If a disclosure is missing, the recipient sees only the hash, but can still verify the entry's geographic and residency properties through other claims.</li>
</ol>
<t>This mechanism ensures that the &quot;key&quot; to unlock a hidden identity is passed only to authorized actors in the chain, while the underlying cryptographically signed JWT structure remains identical for all recipients.</t>
</section>
</section>

<section anchor="data-plane-policy-enforcement"><name>Data-Plane Policy Enforcement</name>
<t>Unlike the nested <tt>act</tt> claim in {{!RFC8693}}, the <tt>actor_chain</tt> claim is explicitly designed to be used in access control decisions. Relying Parties and data-plane proxies MAY apply authorization policies based on any entry in the actor chain.</t>

<section anchor="policy-examples"><name>Policy Examples</name>
<t>The following are illustrative examples of policies that become expressible with the <tt>actor_chain</tt> claim:</t>
<t><strong>Origin-Based Policy</strong>: Allow access only if the originating actor (index 0) is a trusted orchestrator:</t>

<artwork><![CDATA[actor_chain[0].sub == "https://orchestrator.example.com"
]]></artwork>
<t><strong>Domain Restriction</strong>: Deny access if any actor in the chain belongs to an untrusted domain:</t>

<artwork><![CDATA[for_all(entry in actor_chain):
  entry.iss in ["https://auth.example.com",
                 "https://auth.partner.com"]
]]></artwork>
<t><strong>Chain Depth Limit</strong>: Reject tokens with delegation chains longer than a configured maximum:</t>

<artwork><![CDATA[len(actor_chain) <= 5
]]></artwork>
<t><strong>Residency Requirement</strong>: Require that all actors in the chain have a valid Proof of Residency:</t>

<artwork><![CDATA[for_all(entry in actor_chain):
  entry.por is present AND entry.por is valid
]]></artwork>
<t><strong>Path-Based Policy</strong>: Allow access only through a specific delegation path:</t>

<artwork><![CDATA[actor_chain[0].sub == "https://orchestrator.example.com" AND
actor_chain[1].sub == "https://planner.example.com"
]]></artwork>
</section>

<section anchor="integration-with-data-plane-proxies"><name>Integration with Data-Plane Proxies</name>
<t>The flat array structure of <tt>actor_chain</tt> is designed for efficient processing by data-plane proxies such as Envoy, Istio sidecars, and API gateways. Proxies can:</t>

<ol spacing="compact">
<li>Extract the <tt>actor_chain</tt> array from the JWT payload with a single JSON path expression.</li>
<li>Iterate linearly over the entries without recursive descent.</li>
<li>Index specific entries by position (e.g., <tt>actor_chain[0]</tt> for the originator).</li>
<li>Compute <tt>len(actor_chain)</tt> for depth-based policies without parsing nested structures.</li>
<li>Emit structured log entries per Actor Chain Entry for distributed tracing and forensic analysis.</li>
</ol>
</section>
</section>
</section>

<section anchor="chain-integrity-verification"><name>Chain Integrity Verification</name>
<t>Verification of the actor chain operates at two levels, reflecting the data-plane / audit-plane separation.</t>

<section anchor="data-plane-verification"><name>Data-Plane Verification</name>
<t>A Relying Party receiving a token with the <tt>actor_chain</tt> claim MUST perform the following verification steps at request time:</t>

<ol>
<li><t><strong>JWT Signature Verification</strong>: Verify the outer JWT signature per standard JWT processing rules. This provides integrity for the entire token, including the <tt>actor_chain</tt> and <tt>actor_chain_root</tt> (if present).</t>
</li>
<li><t><strong>Structural Validation</strong>: Verify that <tt>actor_chain</tt> is a JSON array with at least one element. Verify that each element contains the required identity fields (<tt>sub</tt>, <tt>iss</tt>, <tt>iat</tt>).</t>
</li>
<li><t><strong>PoR Verification</strong> (if present):</t>

<ul spacing="compact">
<li>Verify each PoR assertion according to {{!I-D.draft-mw-spice-transitive-attestation}}.</li>
</ul></li>
<li><t><strong>Policy Evaluation</strong>:</t>

<ul spacing="compact">
<li>Apply local authorization policy against the verified actor chain.</li>
</ul></li>
</ol>
<t>If any verification step fails, the Relying Party MUST reject the token.</t>
</section>

<section anchor="tiered-verification"><name>Tiered Verification</name>
<t>The appropriate level of actor chain checking depends on the risk level of the operation:</t>
<table>
<thead>
<tr>
<th align="left">Risk Level</th>
<th align="left">Data-Plane (sync)</th>
<th align="left">Audit-Plane (async)</th>
<th align="left">Use Case</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left">Low</td>
<td align="left">Verify JWT signature</td>
<td align="left">—</td>
<td align="left">Read operations</td>
</tr>

<tr>
<td align="left">Medium</td>
<td align="left">Verify JWT signature + actor identities</td>
<td align="left">—</td>
<td align="left">Create/update</td>
</tr>

<tr>
<td align="left">High</td>
<td align="left">Verify JWT signature + actor identities</td>
<td align="left">Full forensic verification via registry</td>
<td align="left">Delete, transfer, admin</td>
</tr>

<tr>
<td align="left">Critical</td>
<td align="left">Verify JWT signature + actor identities</td>
<td align="left">Full forensic + cross-chain (intent + inference)</td>
<td align="left">Regulatory, cross-border settlements</td>
</tr>
</tbody>
</table></section>

<section anchor="audit-plane-verification-forensic"><name>Audit-Plane Verification (Forensic)</name>
<t>For forensic analysis, regulatory compliance, or zero-trust verification, an auditor retrieves the full chain from the Actor Chain Registry and performs:</t>

<ol>
<li><t><strong>Retrieve entries</strong>: Fetch the full actor chain entries from the <tt>actor_chain_registry</tt> URI.</t>
</li>
<li><t><strong>Per-Entry Signature Verification</strong>: For each entry at index <tt>i</tt>:</t>

<ul spacing="compact">
<li><strong>Compute expected digest</strong>: If <tt>i == 0</tt>, compute <tt>expected_digest = SHA-256(canonical_json({sub, iss, iat}))</tt> from the entry's own identity claims. If <tt>i &gt; 0</tt>, compute <tt>expected_digest = SHA-256(canonical_json(actor_chain[0..i-1]) || identity_claims_i)</tt>.</li>
<li><strong>Verify chain_digest</strong>: Confirm that the entry's <tt>chain_digest</tt> matches <tt>expected_digest</tt>.</li>
<li><strong>Verify chain_sig</strong>: Verify <tt>chain_sig</tt> against <tt>chain_digest</tt> using the actor's public key (discoverable via <tt>iss</tt> JWKS endpoint or SPIFFE trust bundle).</li>
</ul></li>
<li><t><strong>merkle Root Verification</strong>: Reconstruct the merkle tree from the <tt>chain_sig</tt> leaf nodes and verify that the computed root matches the <tt>actor_chain_root</tt> in the original token.</t>
</li>
</ol>
<t>This two-tier verification model ensures that data-plane latency remains O(1) while full per-actor non-repudiation is available on demand.</t>
</section>
</section>

<section anchor="relation-to-other-ietf-work"><name>Relation to Other IETF Work</name>
<t>This proposal extends and complements several ongoing efforts:</t>
<table>
<thead>
<tr>
<th align="left">Specification</th>
<th align="left">Relationship</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>RFC 8693</strong> {{!RFC8693}}</td>
<td align="left">This document extends {{!RFC8693}} by defining <tt>actor_chain</tt> as a replacement for the informational-only nested <tt>act</tt> claim. The <tt>actor_chain</tt> claim is backward-compatible: an AS MAY populate both <tt>act</tt> (for legacy consumers) and <tt>actor_chain</tt> (for chain-aware consumers).</td>
</tr>

<tr>
<td align="left"><strong>Transitive Attestation</strong> {{!I-D.draft-mw-spice-transitive-attestation}}</td>
<td align="left">Provides the platform attestation proof (agent to local WIA) that complements the delegation proof (agent to actor-chain) provided by this document.</td>
</tr>

<tr>
<td align="left"><strong>SPICE Architecture</strong> {{!I-D.ietf-spice-arch}}</td>
<td align="left">Defines the overarching workload identity architecture within which this extension operates.</td>
</tr>

<tr>
<td align="left"><strong>WIMSE Architecture</strong> {{!I-D.ietf-wimse-arch}}</td>
<td align="left">This proposal aligns with the WIMSE delegation and impersonation patterns for distributed microservices architectures.</td>
</tr>

<tr>
<td align="left"><strong>Attestation-Based Auth</strong> {{!I-D.ietf-oauth-attestation-based-client-auth}}</td>
<td align="left">Provides the client-to-AS attestation mechanism that can be leveraged to populate the hardware-rooted <tt>por</tt> claims in Actor Chain Entries.</td>
</tr>

<tr>
<td align="left"><strong>SCITT</strong> {{!I-D.ietf-scitt-architecture}}</td>
<td align="left">Verifiable actor chains can be recorded in SCITT transparency logs to provide long-term, tamper-proof auditability of delegation paths.</td>
</tr>

<tr>
<td align="left"><strong>RATS</strong> {{!RFC9334}}</td>
<td align="left">Provides the attestation foundation for PoR assertions embedded in Actor Chain Entries.</td>
</tr>

<tr>
<td align="left"><strong>DPoP</strong> {{!RFC9449}}</td>
<td align="left"><tt>actor_chain</tt> complements DPoP by providing delegation-chain context alongside proof-of-possession.</td>
</tr>
</tbody>
</table>
<section anchor="delegation-vs-platform-attestation"><name>Delegation vs. Platform Attestation</name>
<t>This specification addresses the <strong>delegation</strong> axis of agent-to-agent communication, providing a cryptographically verifiable trail of identity delegation across a network of services. In contrast, Transitive Attestation {{!I-D.draft-mw-spice-transitive-attestation}} addresses the <strong>platform attestation</strong> axis of an agent's relationship with its local hosting environment (e.g., a Workload Identity Agent on a TEE-enabled node).</t>
<t>By embedding platform attestation Proofs of Residency (PoR) within delegation Actor Chain Entries, a Relying Party gains end-to-end assurance that every entity in a global delegation chain is both a recognized identity and is executing within a verified, secure environment.</t>
</section>

<section anchor="backward-compatibility-with-rfc-8693"><name>Backward Compatibility with RFC 8693</name>
<t>An Authorization Server implementing this extension SHOULD populate both the <tt>act</tt> claim (per {{!RFC8693}} Section 4.1) and the <tt>actor_chain</tt> claim in issued tokens. This ensures that:</t>

<ul spacing="compact">
<li><strong>Legacy consumers</strong> that understand only <tt>act</tt> continue to function correctly, seeing the current actor in the top-level <tt>act</tt> claim.</li>
<li><strong>Chain-aware consumers</strong> can use <tt>actor_chain</tt> for fine-grained policy enforcement and audit.</li>
</ul>
<t>The <tt>act</tt> claim, when present alongside <tt>actor_chain</tt>, MUST identify the same entity as the last entry in the <tt>actor_chain</tt> array.</t>
</section>
</section>

<section anchor="scalability-considerations"><name>Scalability Considerations</name>

<section anchor="token-size"><name>Token Size</name>
<t>The <tt>actor_chain</tt> identity entries are embedded inline in the JWT. Each Actor Chain Entry adds approximately 150-300 bytes (identity claims only; no signatures). For a chain of depth 5, this adds approximately 0.75-1.5KB to the token. The <tt>actor_chain_root</tt> adds a fixed 44 bytes regardless of chain depth. Authorization Servers SHOULD enforce <tt>max_chain_depth</tt> to bound token size. A RECOMMENDED default maximum of 10 entries limits the actor chain contribution to approximately 3KB.</t>
<t>For the CWT representation, Actor Chain Entries use CBOR maps with integer keys, and <tt>chain_sig</tt> in the registry uses COSE_Sign1 {{!RFC9052}}, further reducing overhead.</t>
</section>

<section anchor="signature-verification-cost"><name>Signature Verification Cost</name>
<t>Data-plane signature verification cost is O(1) — the Relying Party verifies only the outer JWT signature. The <tt>actor_chain_root</tt> is trusted as part of the signed payload. Per-actor signature verification is deferred to the audit plane, where it is O(n) but occurs asynchronously and only when needed.</t>
</section>

<section anchor="verification-caching"><name>Verification Caching</name>
<t>Relying Parties MAY cache actor chain verification results, keyed by the <tt>actor_chain_root</tt> (which uniquely identifies the complete chain). A cached verification result is valid for the lifetime of the enclosing JWT (<tt>exp</tt> claim).</t>
</section>

<section anchor="trust-model"><name>Trust Model</name>
<t>The <tt>actor_chain</tt> provides two layers of integrity serving different planes:</t>

<ol>
<li><t><strong>Data plane</strong> — AS outer signature (JWT/CWT): The Authorization Server signs the entire token, including the <tt>actor_chain</tt> identity entries and the <tt>actor_chain_root</tt>. If the Relying Party trusts the AS, this single signature provides sufficient assurance for real-time access control decisions.</t>
</li>
<li><t><strong>Audit plane</strong> — Per-actor signatures (registry): Each actor's <tt>chain_sig</tt> in the registry proves that specific actor participated in this specific delegation path. This evidence is independently verifiable and non-repudiable — a compromised AS cannot fabricate an actor's <tt>chain_sig</tt> without that actor's private key.</t>
</li>
</ol>
<t>The <tt>actor_chain_root</tt> binds these two planes: it is a signed claim in the token AND the merkle root of the registry signatures. Any tampering with either plane is detectable.</t>

<section anchor="multi-as-identity-federation"><name>Multi-AS Identity Federation</name>
<t>In federated deployments, a delegation chain may span multiple Authorization Servers. For example: <tt>b (AS1) -&gt; a (AS1) -&gt; c (AS1) -&gt; f (AS2) -&gt; d (AS2) -&gt; e (AS2)</tt>.</t>
<t>The data-plane token is signed by whichever AS issued the final token (AS2 in this example). For real-time access control, the Relying Party trusts AS2. For audit-plane verification — such as confirming that <tt>b</tt> actually initiated the chain through AS1 — the auditor retrieves the registry entries and verifies each actor's <tt>chain_sig</tt> against that actor's public key, discoverable via the <tt>iss</tt> claim (JWKS endpoint) or SPIFFE ID (trust bundle). No single AS needs to be trusted for the entire chain.</t>
<t>Federated deployments SHOULD:</t>

<ul spacing="compact">
<li>Publish actor signing keys via standard discovery mechanisms (JWKS endpoints, SPIFFE trust bundles).</li>
<li>Include <tt>kid</tt> or <tt>jwk</tt> in each <tt>chain_sig</tt> JWS header for key resolution.</li>
<li>Enforce cross-domain trust policies on the <tt>iss</tt> field of each Actor Chain Entry.</li>
</ul>
</section>
</section>

<section anchor="chain-integrity"><name>Chain Integrity</name>
<t>The hash-chain structure in the registry provides tamper evidence for the entire delegation path. Insertion, deletion, or reordering of entries invalidates the <tt>chain_digest</tt> and <tt>chain_sig</tt> fields of all subsequent entries and changes the merkle root. The <tt>actor_chain_root</tt> in the signed token anchors this evidence — any alteration to the registry entries produces a different merkle root that no longer matches the token.</t>
</section>

<section anchor="replay-protection"><name>Replay Protection</name>
<t>Each Actor Chain Entry includes an <tt>iat</tt> (issued-at) timestamp. Relying Parties SHOULD enforce a maximum age on Actor Chain Entries to prevent replay of stale chains. Additionally, the standard JWT claims <tt>exp</tt> and <tt>nbf</tt> on the enclosing token provide overall token-level freshness.</t>
</section>

<section anchor="chain-depth-limits"><name>Chain Depth Limits</name>
<t>Unbounded actor chains pose a risk of token size explosion and processing overhead. Authorization Servers SHOULD enforce a configurable maximum chain depth (<tt>max_chain_depth</tt>). A RECOMMENDED default maximum is 10 entries. Relying Parties MAY independently enforce their own chain depth limits.</t>
</section>

<section anchor="key-management"><name>Key Management</name>
<t>Each actor in the chain signs its <tt>chain_digest</tt> with its own private key. Actors SHOULD use short-lived keys and/or hardware-protected keys (e.g., via the PoR mechanism). The same signing key MAY be used for the actor's <tt>chain_sig</tt> in the actor chain, its contributions to the Intent Chain {{!I-D.draft-mw-spice-intent-chain}}, and the Inference Chain {{!I-D.draft-mw-spice-inference-chain}}, providing a unified key management model across all three governance chains.</t>
</section>

<section anchor="privacy-of-prior-actors"><name>Privacy of Prior Actors</name>
<t>The <tt>actor_chain</tt> exposes the identities of all actors in the delegation path to every Relying Party that receives the token. While this is necessary for certain audit and policy requirements, it may conflict with privacy goals. For example, in a chain <tt>a -&gt; b -&gt; c -&gt; d -&gt; e</tt>, the originating actor <tt>a</tt> may require its identity to be hidden from <tt>e</tt> while still ensuring the integrity of the delegation.</t>
<t>Deployments SHOULD consider the following anonymization and privacy-preserving techniques:</t>

<section anchor="pseudonymous-identifiers"><name>Pseudonymous Identifiers</name>
<t>Instead of using globally unique or stable identifiers (like email addresses or client IDs), the Authorization Server (AS) can issue pairwise pseudonyms for actors. In the chain <tt>a -&gt; b -&gt; c -&gt; d -&gt; e</tt>:
- The AS replaces <tt>sub: &quot;a&quot;</tt> with a pseudonym <tt>sub: &quot;pseudo-xyz&quot;</tt> that is only meaningful to the AS.
- Relying Party <tt>e</tt> sees that the chain started with a verified actor, but does not know it was <tt>a</tt>.
- The AS maintains a mapping to allow for forensic reconstruction if authorized.</t>
</section>

<section anchor="selective-disclosure-sd-jwt"><name>Selective Disclosure (SD-JWT)</name>
<t>The <tt>actor_chain</tt> MAY be implemented using Selective Disclosure for JWTs {{!I-D.ietf-oauth-selective-disclosure-jwt}}. This allows individual Actor Chain Entries to be hashed with unique salts, enabling verification of the entry's integrity without revealing its cleartext content unless a corresponding disclosure is provided.</t>
</section>

<section anchor="identity-bridging-for-anonymity"><name>Identity Bridging for Anonymity</name>
<t>An Identity Bridge {{!I-D.ietf-spice-arch}} MAY act as an &quot;Anonymizer&quot; by performing a token exchange that replaces sensitive predecessor entries in the <tt>actor_chain</tt> with generic or pseudonymous identifiers, while still vouching for the chain's security properties.</t>
</section>

<section anchor="targeted-selective-disclosure"><name>Targeted Selective Disclosure</name>
<t>A more advanced privacy model involves disclosing actor identities only to trusted intermediate parties. In the chain <tt>a -&gt; b -&gt; c -&gt; d -&gt; e</tt>, actor <tt>a</tt> may be visible to <tt>b</tt>, <tt>c</tt>, and <tt>d</tt>, but hidden from <tt>e</tt>.</t>
<t>This is achieved through <strong>Targeted Disclosure</strong> using SD-JWT:
- The AS generates the <tt>actor_chain</tt> with actor <tt>a</tt>'s identity hashed and salted.
- Alongside the JWT, the AS provides a &quot;Disclosure&quot; string (the salt and cleartext value) for actor <tt>a</tt>.
- When the AS issues a token for <tt>b</tt>, <tt>c</tt>, or <tt>d</tt>, it includes this disclosure string. This allows these &quot;internal&quot; actors to &quot;unlock&quot; the hash and see <tt>a</tt>'s true identity.
- When the final token is exchanged for recipient <tt>e</tt>, the AS (or the last internal actor <tt>d</tt>) simply omits the disclosure string for actor <tt>a</tt>.
- Actor <tt>e</tt> receives the same JWT but without the &quot;key&quot; to unlock <tt>a</tt>. <tt>e</tt> can verify the cryptographic integrity of the entire chain (proving that a verified, though anonymous, actor started it) without ever seeing <tt>a</tt>'s identity.</t>
<table>
<thead>
<tr>
<th align="left">Recipient</th>
<th align="left">JWT Payload</th>
<th align="left">Disclosure provided?</th>
<th align="left">Interpretation</th>
</tr>
</thead>

<tbody>
<tr>
<td align="left"><strong>Actor b</strong></td>
<td align="left"><tt>_sd: [hash_a]</tt></td>
<td align="left"><strong>Yes</strong></td>
<td align="left"><tt>b</tt> sees <tt>sub: a</tt></td>
</tr>

<tr>
<td align="left"><strong>Actor e</strong></td>
<td align="left"><tt>_sd: [hash_a]</tt></td>
<td align="left"><strong>No</strong></td>
<td align="left"><tt>e</tt> sees <tt>sub: &lt;hidden&gt;</tt></td>
</tr>
</tbody>
</table></section>

<section anchor="encryption-jwe"><name>Encryption (JWE)</name>
<t>The entire <tt>actor_chain</tt> claim can be encrypted using JWE {{!RFC7516}} so that only the final intended audience <tt>e</tt> can decrypt it, preventing intermediate actors like <tt>b</tt>, <tt>c</tt>, and <tt>d</tt> from seeing the IDs of their predecessors. Alternatively, the chain can be nestedly encrypted for different parties in the path.</t>
</section>
</section>

<section anchor="confused-deputy-mitigation"><name>Confused Deputy Mitigation</name>
<t>A confused deputy attack—where a legitimate actor is tricked into delegating to a malicious downstream—is detectable because the malicious downstream actor's identity appears in the chain, providing forensic evidence of the attack path. The malicious actor's own cryptographic <tt>chain_sig</tt> (retrievable from the registry) provides non-repudiable evidence of its participation.</t>
</section>
</section>

<section anchor="iana-considerations"><name>IANA Considerations</name>

<section anchor="json-web-token-claims-registration"><name>JSON Web Token Claims Registration</name>
<t>This document requests registration of the following claims in the &quot;JSON Web Token Claims&quot; registry established by {{!RFC7519}}:</t>

<ul>
<li><strong>Claim Name</strong>: <tt>actor_chain</tt></li>
<li><strong>Claim Description</strong>: A Cryptographically Verifiable Actor Chain — an ordered array of actor identity entries representing the complete delegation chain.</li>
<li><strong>Change Controller</strong>: IETF</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>actor_chain_root</tt></t>
</li>
<li><t><strong>Claim Description</strong>: merkle root hash of per-actor cryptographic signatures in the actor chain.</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>actor_chain_registry</tt></t>
</li>
<li><t><strong>Claim Description</strong>: URI of the Actor Chain Registry where per-actor signature evidence is stored.</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
</ul>
</section>

<section anchor="cbor-web-token-claims-registration"><name>CBOR Web Token Claims Registration</name>
<t>This document requests registration of the following claims in the &quot;CBOR Web Token (CWT) Claims&quot; registry established by {{!RFC8392}}:</t>

<ul>
<li><strong>Claim Name</strong>: <tt>actor_chain</tt></li>
<li><strong>Claim Description</strong>: A Cryptographically Verifiable Actor Chain.</li>
<li><strong>CBOR Key</strong>: TBD (e.g., 40)</li>
<li><strong>Claim Type</strong>: array</li>
<li><strong>Change Controller</strong>: IETF</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>actor_chain_root</tt></t>
</li>
<li><t><strong>Claim Description</strong>: merkle root hash of per-actor COSE_Sign1 signatures.</t>
</li>
<li><t><strong>CBOR Key</strong>: TBD (e.g., 41)</t>
</li>
<li><t><strong>Claim Type</strong>: tstr</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
<li><t><strong>Claim Name</strong>: <tt>actor_chain_registry</tt></t>
</li>
<li><t><strong>Claim Description</strong>: URI of Actor Chain Registry.</t>
</li>
<li><t><strong>CBOR Key</strong>: TBD (e.g., 42)</t>
</li>
<li><t><strong>Claim Type</strong>: tstr</t>
</li>
<li><t><strong>Change Controller</strong>: IETF</t>
</li>
<li><t><strong>Specification Document(s)</strong>: [this document]</t>
</li>
</ul>
</section>
</section>

<section anchor="design-rationale"><name>Design Rationale</name>
<t>This section summarizes why the data-plane / audit-plane separation with merkle root binding was chosen for the actor chain.</t>

<section anchor="why-not-inline-per-actor-signatures"><name>Why Not Inline Per-Actor Signatures?</name>
<t>An earlier design included <tt>chain_digest</tt> and <tt>chain_sig</tt> directly in each Actor Chain Entry within the token. This approach was rejected for three reasons:</t>

<ol>
<li><t><strong>Token size</strong>: Each JWS <tt>chain_sig</tt> adds ~200-300 bytes per entry. For a chain of depth 6, this adds ~1.5-2KB to every token — significant for high-throughput data planes and constrained IoT devices.</t>
</li>
<li><t><strong>Data-plane latency</strong>: Verifying O(n) signatures per request is unnecessary when the Relying Party trusts the issuing AS. Most data-plane decisions require only the actor identities, not cryptographic proof of participation.</t>
</li>
<li><t><strong>Redundancy</strong>: The AS already signs the entire token. Inline per-actor signatures duplicate the integrity guarantee for the common case where the RP trusts the AS.</t>
</li>
</ol>
</section>

<section anchor="why-per-actor-signatures-at-all"><name>Why Per-Actor Signatures At All?</name>
<t>Per-actor signatures are essential for governance because:</t>

<ol>
<li><t><strong>Non-repudiation</strong>: An actor cannot deny having participated in a delegation chain. This is critical for regulatory compliance and post-incident forensic analysis.</t>
</li>
<li><t><strong>Multi-AS trust</strong>: In federated deployments (e.g., <tt>b (AS1) -&gt; a (AS1) -&gt; c (AS1) -&gt; f (AS2) -&gt; d (AS2) -&gt; e (AS2)</tt>), no single AS can vouch for the entire chain. Per-actor signatures allow an auditor to verify each actor's participation independently of any AS.</t>
</li>
<li><t><strong>Governance alignment</strong>: The Intent Chain and Inference Chain specifications already require per-actor signatures for content and computation provenance. Without per-actor signatures in the actor chain, the WHO dimension of the governance framework would lack the same level of assurance as WHAT and HOW.</t>
</li>
</ol>
</section>

<section anchor="why-the-merkle-root"><name>Why the merkle Root?</name>
<t>The merkle root (<tt>actor_chain_root</tt>) provides the binding between the data-plane token and the audit-plane registry without inflating the token. It has constant size (44 bytes) regardless of chain depth. An auditor verifies the merkle root by reconstructing it from the registry's <tt>chain_sig</tt> values — if the computed root matches the token's <tt>actor_chain_root</tt>, the registry evidence is proven authentic.</t>
<t>This is the same pattern used by the Intent Chain (<tt>intent_root</tt>) and Inference Chain (<tt>inference_root</tt>), creating a unified, architecturally consistent governance framework across all three chains.</t>
</section>

<section anchor="registry-availability"><name>Registry Availability</name>
<t>Deployments where the Actor Chain Registry is unavailable (network partition, registry outage) do not affect data-plane operation — the token's AS-signed <tt>actor_chain</tt> entries are sufficient for real-time access control. The <tt>actor_chain_root</tt> in the signed token acts as a commitment: even during a registry outage, the signed evidence cannot be tampered with because the root is fixed at token issuance.</t>
<t>For high-availability requirements, deployments SHOULD:</t>

<ul spacing="compact">
<li>Replicate registry entries across availability zones.</li>
<li>Use append-only log services designed for high durability (e.g., SCITT transparency logs).</li>
<li>Cache recently-verified registry entries at the audit plane.</li>
</ul>
</section>

<section anchor="registry-hosting"><name>Registry Hosting</name>
<t>The Actor Chain Registry is an append-only log partitioned by session (<tt>sid</tt>). Per-session entries accumulate as token exchanges occur (one entry per actor per session), and each entry is small (~400-600 bytes including <tt>chain_digest</tt> and compact JWS <tt>chain_sig</tt>).</t>
<t>A federated IAM/IdM platform (e.g., Keycloak, Microsoft Entra, Okta, PingFederate) is a natural host for the Actor Chain Registry because:</t>

<ul spacing="compact">
<li>The Authorization Server already mediates every token exchange ({{!RFC8693}}) and can append registry entries as a side-effect of token issuance.</li>
<li>IAM platforms already manage the signing key infrastructure (JWKS endpoints, SPIFFE trust bundles) needed to verify <tt>chain_sig</tt> values.</li>
<li>Federation and cross-domain trust — the core of multi-AS actor chains — are the IAM's primary competency.</li>
</ul>
<t>Most enterprise IAM/IdM platforms support configurable data stores. To host the Actor Chain Registry, the data store MUST be configured for append-only semantics:</t>

<ul spacing="compact">
<li><strong>No update or delete</strong> of registry entries after creation.</li>
<li><strong>Session-scoped partitioning</strong> (<tt>sid</tt>) for isolation and efficient retrieval.</li>
<li><strong>Immutable storage backends</strong> such as append-only database tables, write-once object storage (e.g., S3 Object Lock), Kafka topics with log compaction disabled, or SCITT transparency logs.</li>
</ul>

<section anchor="credential-isolation"><name>Credential Isolation</name>
<t>Registry entries MUST NOT contain OAuth tokens, bearer credentials, or signing keys. The relationship between tokens and registry entries is one-directional: the token references the registry via the <tt>actor_chain_registry</tt> URI claim, but the registry MUST NOT store or reference the token itself. This separation ensures that compromise of the registry does not expose bearer credentials that could be used for unauthorized access.</t>
<t>An IAM/IdM platform that co-locates token issuance and registry storage MUST enforce strict access control boundaries between the token store and the registry store. The token MUST NOT be reconstructable from registry entries alone — registry entries contain only identity claims, cumulative hashes, and per-actor signatures, never the complete token payload or the AS signing key.</t>
</section>

<section anchor="registry-discovery"><name>Registry Discovery</name>
<t>The token carries <tt>actor_chain_registry</tt>, <tt>intent_registry</tt> ({{!I-D.draft-mw-spice-intent-chain}}), and <tt>inference_registry</tt> ({{!I-D.draft-mw-spice-inference-chain}}) URI claims. In a federated IAM deployment, these URIs MAY follow two patterns:</t>
<t><strong>Unified endpoint</strong> — all three registries hosted under the same IAM endpoint, differentiated by chain type:</t>

<artwork><![CDATA[https://iam.example.com/governance/{session_id}/actor
https://iam.example.com/governance/{session_id}/intent
https://iam.example.com/governance/{session_id}/inference
]]></artwork>
<t><strong>Per-chain-type endpoints</strong> — each registry hosted separately, with inference proofs potentially on a different storage service:</t>

<artwork><![CDATA[https://iam.example.com/governance/{session_id}/actor
https://iam.example.com/governance/{session_id}/intent
https://proofs.example.com/inference/{session_id}
]]></artwork>
<t>The per-chain-type pattern is RECOMMENDED for deployments where inference proof sizes require a separate storage backend. Regardless of the pattern, the registry URIs in the token MUST resolve to endpoints that return the complete set of entries for the given session.</t>
</section>
</section>
</section>

</middle>

<back>

</back>

</rfc>
